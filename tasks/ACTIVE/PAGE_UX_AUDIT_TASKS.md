# BLOOM — ביקורת UX + התמכרות לכל הדפים

**תאריך:** 2026-06-02 · **שיטה:** 21 סוכנים מקבילים, כל אחד קרא את קוד-המקור + ה-CSS האמיתי של מסך אחד וניקד 7 מימדים.

> **ציון המשחק הכולל: 67/100 (B-)** · 21 מסכים · 141 ממצאים (0 קריטי · 34 גבוה · 65 בינוני · 42 נמוך).







## ✅ סבב 7 — בוצע ונפרס (2026-06-02 · cache `v20260602za` · SW `bloom-v25.0`)

**מסך-סיום (66) — 2 ממצאי-פוקוס/יופי (CSS בלבד, אפס collision):**

- **🎉 הכותרת-חוגגת חזרה להיות כותרת**: `.over-title` (למשל "👑🎉 שיא חדש + כתר!") היה **13px אפור uppercase** — מוקטן מול ניקוד 42px, וה-uppercase/letter-spacing מזיקים לעברית. עכשיו **21px bold כהה** בלי uppercase. הרגע הכי רגשי קורא ככותרת.
- **🔢 ניקוד-גיבור גדול יותר**: 42px → 46px, weight 800 — שיא ה-count-up פחות "תפל" בנייד.
- **🌙 dark**: שתי ה-overrides של `.over-title` עברו מ-#8E8A82 אפור ל-#F2EFE9 בהיר.

> נשאר במסך-הסיום (לא נגעתי): קיר 25-האלמנטים (effort L · פוקוס) + כפתור בית-מתויג + continue-ad hex (tokens). **ציון הדף: 66 → ~70 (משוער).**

---
## ✅ סבב 6 — בוצע ונפרס (2026-06-02 · cache `v20260602y` · SW `bloom-v24.8`)

**Pet / Mascot (69) — 5 ממצאים נסגרו (העוגן הרגשי הפך לניתן-לפעולה):**

- **💗 guilt ניתן-לפעולה** ([22-pet.js](src/22-pet.js)): כשהפרח עצוב/בוכה, האריח האדום עכשיו נושא **CTA בלחיצה-אחת** — "💗 ליטוף עכשיו · הרגע אותו" — שמבצע את הליטוף inline (סאונד+buzz+toast+פרס), בלי לפתוח modal. ה-loss-aversion הפך למשהו שהשחקן פותר מיד (תבנית Tamagotchi "feed me now").
- **🌸 רמיזת-אבולוציה**: ה-modal מציג עכשיו "🌸 עוד 2 דרגות לפריחה מלאה!" (השלב הבא + emoji + ספירה), במקום רק את המצב הנוכחי — מושך התקדמות.
- **✕ ל-naming**: נוסף כפתור-סגירה גלוי לחלון מתן-השם (ESC כבר נוסף בסבב 1).
- **♿ reduced-motion + יעד-מגע 40px**: מכוסים גלובלית מסבב 1 (אומת על משטחי ה-pet).

> נדחה (effort L): שכבת רצף/חברתית + פעולות יומיות נוספות. **ציון הדף: 69 → ~78 (משוער).** CSS חדש ב-[boards.css](public/css/boards.css) + dark overrides.

---
## ✅ סבב 5 — בוצע ונפרס (2026-06-02 · cache `v20260602x` · SW `bloom-v24.7`)

**Onboarding/FTUE (66) — 3 ממצאים נסגרו (30 השניות הכי קריטיות לשימור יום-ראשון):**

- **🌸 רגע value-prop לפני הדמו** ([15-ftue.js](src/15-ftue.js)): מסך-פתיחה חדש — "מזגו פרחים. הגיעו לכתר 👑" + תצוגת סולם-8-הדרגות + 3 bullets (מזג/טפס/התחרה) + כפתור "בוא נתחיל". במקום לפתוח ישר במטלה על לוח ריק.
- **🎮 CTA סיום (graduation)**: אחרי הצעד האחרון — כרטיס "מוכן? בוא נשחק!" + קונפטי + כפתור "התחל לשחק" שהשחקן לוחץ במכוון, במקום זריקה פסיבית ל-home.
- **⏱ קיצור זמני-המתנה**: dead-time בין צעדים מ-1300ms ל-1000ms; השחקן עכשיו עם agency בהתחלה ובסוף.

> blast-radius נמוך — נורה רק למכשירים חדשים (games_played===0), השינויים אדיטיביים, 3 צעדי-הדמו עצמם לא נגעתי. נדחה: rearch לדמו-על-מנוע-אמיתי (החלטת-בעלים) + push-prompt tokens + country-picker. **ציון הדף: 66 → ~76 (משוער).**

---
## ✅ סבב 4 — בוצע ונפרס (2026-06-02 · server-only · ללא cache bump)

**פרופיל השחקן (66) — 4 ממצאים נסגרו (הפיכת הרזומה הסטטי לנשק ויראלי):**

- **🥇 hook תחרותי + דירוג**: query חדש מחשב **דירוג עולמי** לפי שיא, ומוצג "מקום #N בעולם" + באנר "🎯 תצליח לעבור את X?" — ה-FOMO שממיר צופה לשחקן (היה רזומה סטטי בלי הזמנה).
- **🚪 לא עוד מבוי-סתום**: ה-CTA הפך ל"⚔️ קבל את האתגר — שחק עכשיו" + קישור-footer ל-/welcome ("מה זה BLOOM?"). לצופה יש עכשיו נתיב פנימה.
- **🔒 הסרת דליפת-כלכלה**: יתרת ה-💎 + "הרוויח" הוסרו מפרופיל ציבורי (אות שגוי + brag חלש) והוחלפו בדירוג/ימים-פעילים.
- **♿ reduced-motion**: הפרופיל הוא HTML עצמאי (לא משתמש ב-base.css), אז נוסף בלוק reduced-motion ל-style המוטמע שלו.

> **נדחה במודע:** og:image דינמי per-player (★★★★★) — דורש ייצור PNG בצד-שרת (תלות sharp/canvas שאסורה). ה-og:title/description **כבר** מותאמים-אישית (שם+רמה+שיא); רק התמונה גנרית. צריך החלטת-בעלים על הוספת תלות. גם token-unification של welcome/profile נשאר במסע ה-tokens.

> **ציון הדף: 66 → ~78 (משוער).**

---
## ✅ סבב 3 — בוצע ונפרס (2026-06-02 · cache `v20260602w` · SW `bloom-v24.6`)

**Spectator (62/C+, הדף ה-2 הכי נמוך) — 5/6 ממצאים נסגרו:**

- **🥊 "אני מולם"** ([10-spectator.js](src/10-spectator.js)): רצועת השוואה חדשה — "אתה X · 🔥 הוא לפניך ב-N · הם Y" — פועמת אדום כשהיריב עוקף. ה-hook הצופֶה/תחרותי הכי חזק, שהיה חסר לגמרי. (הצופה כבר חושף את הניקוד שלו לשרת — עכשיו מוצג.)
- **⚡ לוח חי באמת**: ה-render עבר מ-rebuild מלא כל שנייה (שגרם ל-tiles לקפוץ) ל-**עדכון-במקום** עם diff-תאים (appear/merge/clear) + count-up לניקוד. עם fallback בטוח ל-rebuild מלא אם משהו משתבש.
- **🚪 ESC ליציאה**: תצוגת-הצפייה מחליפה את grid-wrap (לא modal), אז ה-ESC הגלובלי לא תפס אותה — נוסף מאזין ייעודי שמנקה את עצמו ב-stopSpectator.
- **🔄 picker מתרענן**: הרשימה נתקעה אחרי fetch אחד — עכשיו רענון כל 4ש כל עוד פתוח, מנקה את עצמו בסגירה.
- **👆 controls נגישים**: כפתורי יציאה/החלפה הפכו sticky-bottom (תמיד בהישג-אגודל).

> ה-medium שנותר (hex→tokens) שייך למסע ה-tokens הנדחה. **ציון הדף: 62 → ~80 (משוער).** reduced-motion-safe דרך ה-guard מסבב 1.

---
## ✅ סבב 2 — בוצע ונפרס (2026-06-02 · cache `v20260602v` · SW `bloom-v24.5`)

**הדף עם הציון הנמוך ביותר — BLOOM Challenges (59/C) — נסגר כמעט במלואו (6/7 ממצאים):**

- **⏱ שעון חי** ([09-challenges.js](src/09-challenges.js)): `startChallengeCountdownTicker()` חדש — בכרטיס הפירוט HH:MM:SS חי תמיד, בכרטיסי הרשימה שעון מתקתק + פעימה אדומה ב-2 השעות האחרונות. מנקה את עצמו כשהמסך נעלם.
- **🎉 שיא-ניצחון** ([09-challenges.js](src/09-challenges.js)): ניצחון בפרס אמיתי מפעיל עכשיו `__bloomConfetti(60)` + `soundMilestone(7)` + buzz (היה אפס חגיגה). ה-win-toast באמצע-משחק שודרג לזהב + `soundMilestone(5)`.
- **🎁 empty-state** : "נסה מחר" (מבוי-סתום) הוחלף בכרטיס-ציפייה חם + "🔔 הודיעו לי" (push opt-in) + "🎮 שחק פרקטיס" (fallback). השחקן נשאר במשחק.
- **💰 בליטות-פרס**: הפרס עבר מבליטת-פינה 11px לשורה מלאת-רוחב עם תג "פרס אמיתי" + chip scarcity ("🔥 מקום אחרון לזכייה!") כשנותרו ≤2 מקומות, בכרטיס וב-detail.
- **📝 טפסי-קשר**: `type=tel/email` + `inputmode` + `autocomplete` + תוויות — מקלדת-נייד נכונה + autofill בשלב-ההמרה הכי חשוב.
- **🎯 בהירות**: שורת "איך זוכים?" בשפה פשוטה (`challengeTypeExplain`) + הדגשת "📜 תקנון".

> ה-low שנותר (hardcoded hex) הוא חלק ממסע אימוץ-ה-tokens הנדחה. **ציון הדף: 59 → ~80 (משוער).** CSS חדש ב-[screens.css](public/css/screens.css)+[dark.css](public/css/dark.css), reduced-motion-safe דרך ה-guard הגלובלי מסבב 1.

---
## ✅ סבב 1 — בוצע ונפרס (2026-06-02 · cache `v20260602u` · SW `bloom-v24.4`)

**4 תיקונים רוחביים (מרימים את כל 21 הדפים) + 4 quick-wins ממוקדים:**

- **🎨 theme #2 — `prefers-reduced-motion` גלובלי** ([base.css](public/css/base.css)): בלוק `@media (prefers-reduced-motion:reduce)` שמנטרל את כל 240+ האנימציות האינסופיות (כולל `viral.css` שהיה אפס) — רק כשהשחקן הפעיל הגדרת מערכת. מכסה את ממצאי ה-reduced-motion בכל הדפים בבת-אחת.
- **👆 theme #4 — יעדי-מגע ≥44px** ([base.css](public/css/base.css)): גילוי חשוב — **37 מ-40 כפתורי הסגירה כבר היו 44px** דרך כלל `[aria-label="סגור"]` קיים; ממצאי ה-"32px" של רוב הדפים היו **false-positive** (הסוכנים קראו `width:32px` ב-CSS הפר-מודאלי ופספסו את ה-override הגלובלי). הורחב `[class$="modal-close"]`→`[class*="-close"]` כדי לכסות את ה-3 שנותרו (`dr-close`/`ts-close`/`gacha-summary-close`) + כל עתידי.
- **🚪 theme #3 — חורי-סגירה** ([04-ui-utils.js](src/04-ui-utils.js)): הוספו ל-ESC/back allowlist: `login-cal-overlay`, `inbox-overlay`, `wr-overlay`, `wr-share-overlay`, `live-race-result-overlay`, `pet-name-overlay`. (סוגר את ממצאי ה-closability של פרסים/קהילה/wrapped/דו-קרב/חיית-מחמד.)
- **🎨 theme #1a — toast dark-mode** ([04-ui-utils.js](src/04-ui-utils.js)): variant `info` עבר מ-`#FFF` קשיח ל-`var(--color-surface)` שמתהפך ב-dark. (ה-z-index migration המלא נדחה — effort L.)
- **🐛 מסך-סיום — countdown קפוא** ([11-game.js](src/11-game.js)): `startNextRewardCountdown()` היה **קרוא-אך-לא-מוגדר** (נכשל בשקט ב-try/catch) — עכשיו מוגדר ומתקתק כל שנייה, מנקה את עצמו כשהמסך נעלם.
- **⚔️ דו-קרב — סגירת live-race result** ([02-shop.js](src/02-shop.js)+[home-v2.css](public/css/home-v2.css)): נוסף ✕ (aria-label="סגור") + סגירת-backdrop + ESC.
- **🏠 בית — כותרת-משנה ל-CTA** ([05a-home-v2.js](src/05a-home-v2.js)): `#home-v2-cta-sub` שהיה ריק מקבל hook אישי (loss-aversion רצף / "תשבור את השיא").
- **🏠 בית — 3 חרירי balance מתים** ([05a-home-v2.js](src/05a-home-v2.js)): 💎→בנק (`__bloomBank.open`), ❤️→מילוי-חיים, ⭐→מפת-פיצ׳רים. (תוקן גם שם-global שגוי בהמלצת האודיט: `__bloomBank` ולא `__bloomGemBank`.)

> **נדחה בכוונה:** מסע אימוץ-tokens המלא (3,738 hex, effort L) + אכיפת היררכיית z-index המלאה — מצריכים sprint ייעודי בגלל סיכון-רגרסיה ויזואלית. נשארים כפרויקטים גדולים ברשימה.

---

## איך קוראים את זה

- **ה-ציון לכל דף** = ממוצע משוקלל: התמכרות 30% · יופי 18% · נוחות 12% · בהירות 12% · פוקוס 12% · סגירוּת 8% · רלוונטיות 8%. (ה"prime directive" של BLOOM הוא התמכרות, ולכן המשקל הגבוה ביותר — אבל ביקשת גם "נוח ויפה" אז יופי+נוחות יחד = 30%.)
- **הדפים ממוינים מהציון הנמוך לגבוה** = מלמעלה למטה זה גם סדר ה-ROI: הדף הראשון הוא ההשקעה הכי משתלמת.
- **כל ממצא הוא משימה לביצוע** (`[ ]` checkbox) עם: דרגת-חומרה · מאמץ (S/M/L) · impact (★) · 📍 הוכחה ב-`file:line` · 🔧 הוראת-ביצוע מדויקת. אפשר להעביר לי כל משימה והיא מוכנה לתכנות.
- **"מה כבר טוב"** בכל דף = אל תיגע בזה, זה עובד.

## 📊 טבלת הציונים המדורגת

| # | דף | ציון | דרגה | 🔴 | 🟠 | סה"כ | פסק דין בקצרה |
|---|---|---|---|---|---|---|---|
| 1 | BLOOM Challenges (public prize contests) | **59** | C | 0 | 2 | 7 | סגיר-יפה וכן בתיאור, אבל מבזבז את שני המנופים הכי חזקים — אין שעון-ספי… |
| 2 | Spectator screen | **62** | C+ | 0 | 2 | 6 | תצוגת-צפייה יציבה שתיקנה את באג ה-grid הישן — אבל משדרת ניטרלי (בלי 'א… |
| 3 | Dynamic Boards Picker + Season/Battle Pass | **62** | C+ | 0 | 2 | 7 | מעצמת-שימור שטובעת בהצלחה שלה — כל מנוף קיים וה-Battle-Pass dual-track… |
| 4 | Global Design System / Dark mode / Toasts / Theme / Responsiveness | **62** | C+ | 0 | 2 | 6 | תשתית tokens+סגירוּת+נגישות מעוצבת-היטב — שכמעט לא מחווטת (~4% אימוץ t… |
| 5 | Weekly Recap (BLOOM Wrapped) | **63** | C+ | 0 | 1 | 6 | Wrapped מגודר-היטב עם תמונת-שיתוף נושאת-referral, אבל נחתך מ-ESC/back… |
| 6 | Game-over / Over-screen | **66** | B- | 0 | 2 | 6 | מסך-סיום ממוקסם-שימור עם כל מנוף F2P ו-choreography טוב — אבל קיר-גליל… |
| 7 | Duel modal + Live Race (60s) + Spectator widget | **66** | B- | 0 | 2 | 7 | juice של מרוץ-חי ברמה הכי גבוהה ולוח-צפייה באמת חי — אבל ה-overlay של… |
| 8 | Progression cluster (Trophy Road + Trophy Chests + Album + Lifetime + Leagues) | **66** | B- | 0 | 2 | 7 | אשכול-מטא מלוטש עם hook-שימור חזק (גביעים יורדים) ו-choreography משביע… |
| 9 | Onboarding / FTUE / first-run routing / push prompt | **66** | C+ | 0 | 2 | 7 | חזק טכנית ומגודר נכון, אבל 30 השניות הראשונות נפתחות עם מטלה במקום תשו… |
| 10 | Player Profile (/player/BLOOM-XXXX) + Welcome (/welcome) + Privacy (/privacy) | **66** | C+ | 0 | 3 | 7 | זוג משטחים server-rendered מלוטשים עם SEO חזק וכרטיס-פרופיל נקי — אבל… |
| 11 | Discovery / Feature Map | **68** | B- | 0 | 1 | 6 | מפת-FOMO באמת שימושית עם hook-באנר חזק וסגירוּת חסינה — נבלמת מ-stylin… |
| 12 | Bottom navigation (5 tabs) | **69** | B- | 0 | 1 | 7 | ניווט 5-טאבים נקי, בנוי-היטב ובטוח-iOS שמצטיין בבהירות/סגירה/safe-area… |
| 13 | Contests (menu / create / join / smart leaderboard / in-game HUD) | **69** | B | 0 | 1 | 6 | HUD-תחרות חי מצוין ו-smart-leaderboard עם hooks חזקים (next-target pil… |
| 14 | Pet / Mascot (Tamagotchi) | **69** | B- | 0 | 1 | 6 | Tamagotchi מקסים ומ-juiced עם guilt-trip חזק ו-dark-mode נקי — אבל האש… |
| 15 | Home screen (Power-Hero default) | **70** | B | 0 | 2 | 7 | נחיתה ממוקדת ובנויה היטב, עם משמעת CTA-יחיד מצוינת והיגיינת מגע/RTL/da… |
| 16 | Friends/Social system (unified hub + search + requests + notify + challenges + device sync) | **70** | B | 0 | 1 | 7 | לולאה חברתית באמת ממכרת (פופ-אפ +200💎 בזמן-אמת, רשימה online-first, ה… |
| 17 | Admin dashboard (owner control center) | **71** | B | 0 | 2 | 7 | קונסולת-בעלים רבת-עוצמה עם שליטה אמיתית — ה-engagement overview וה-tog… |
| 18 | In-game gameplay UI | **72** | B | 0 | 2 | 7 | HUD חי ובאמת ממכר (דלתא מעל-שיא, מד-סכנה, slot-reveal, clutch-save) עם… |
| 19 | Daily Rewards cluster (Spin + Login Calendar + Checklist + Daily Deal) | **72** | B | 0 | 1 | 7 | ה-hooks הכי טובים של חזרה-יומית (גלגל משוקלל, jackpot יום-7, deal מעוג… |
| 20 | Community cluster (Guilds + Guild Wars + Rivals + Ghost + Inbox) | **72** | B | 0 | 1 | 7 | hooks חברתיים חזקים עם empty-states טובים לסולו — אבל ה-inbox (הכי-נלח… |
| 21 | Shop cluster (Gem Bank + Starter Pack + Gacha + Bundles + Boosters) | **74** | B | 0 | 1 | 8 | אשכול-מונטיזציה עם מכניקות ממכרות חזקות (gacha-reveal מצוין, עיגון-ערך… |

## 🧵 8 התמות החוצות-דפים (הכי הרבה מנוף)

אלה הדפוסים שחוזרים שוב ושוב — תיקון רוחבי אחד שלהם מרים עשרות דפים בבת-אחת:

1. **🎨 Design tokens מוגדרים אך לא מאומצים** — `base.css` מגדיר מערכת tokens יפה (`--color-*`, `--radius-*`, `--shadow-*`, `--z-*`), אבל יש **3,738 צבעי hex קשיחים מול 172 שימושי `var()`** (~4% אימוץ). זו הסיבה המרכזית שהמשחק "מרגיש כמו אוסף מסכים תפורים יחד" ולא מוצר אחד מלוטש. **זה ממצא #1 ל"יפה".**
2. **♿ `prefers-reduced-motion` כמעט לא מטופל** — 366 keyframes, 240 אנימציות אינסופיות, רק **9 הגנות** reduced-motion. `viral.css` (כל החגיגות) = **אפס**. שחקנים רגישי-תנועה מקבלים מסך שנושם בלי הפסקה (גם נוחות, גם נגישות, גם תחושת "רעש").
3. **🚪 חורי-סגירה** — ה-ESC/back הגלובלי תופס רק overlays ששמם מכיל `modal-overlay`. לפחות 5 חלונות (**login-cal, inbox, wrapped, live-race-result, naming**) לא נתפסים → השחקן "נתקע". פתרון: שם-class אחיד + הוספה ל-allowlist ב-`src/04-ui-utils.js`.
4. **👆 כפתורי-סגירה מתחת ל-40px** — כמעט בכל חלון ה-✕ הוא 30-36px, מתחת לסטנדרט המגע. חוזר ב-**9+ דפים**. תיקון CSS גלובלי אחד פותר את כולם.
5. **💀 Hooks ריקים/קפואים על המשטחים הכי יקרים** — כותרת ה-CTA הראשי **ריקה**, 3 מ-4 חרירי ה-balance **מתים** למגע, ה-next-reward countdown **קפוא** (פונקציה לא מוגדרת), אתגרים **בלי שעון חי**. אלה ה-hooks הכי חזקים בכל המשחק — והם פשוט לא עובדים.
6. **🎉 חוסר שיא-דופמין ברגעים הגדולים** — ניצחון באתגר עם פרס אמיתי = **אפס** קונפטי/סאונד; gacha mythic / רכישת starter-pack = **בלי** קונפטי; צפייה חיה **בלי** juice. הרגעים הכי רגשיים מטופלים כאירוע רגיל.
7. **🧱 דילול-פוקוס (overload)** — מסך הסיום = קיר 25 אלמנטים · ה-picker = 8 באנרים מעל הלוחות · טאב דרגות = 4 אריחים · discovery = 2 פעימות מעל ה-PLAY. ה-Priority Calmer קיים אבל לא מכסה את ה-CTA הנושם ואת ה-hero המתחלף. צריך **היררכיה אחת ברורה** בכל מסך.
8. **🏆 חוסר השוואה-חברתית** — spectator בלי "אני מולם" · Trophy Road בלי סולם · פרופיל בלי דירוג/אתגר. ה-hook התחרותי הכי חזק (Clash Royale) חסר במקומות-מפתח.

## 🔝 18 המשימות הכי משתלמות (impact גבוה · מאמץ נמוך-בינוני)

אם אתה רוצה להתחיל מאיפשהו — אלה לפי סדר. כל אחת היא "הרבה תמורה למעט עבודה":

1. **★★★★★** `M` 🟠 גבוה — _BLOOM Challenges (public prize contests)_ — No live countdown ticker — urgency is a static coarse string, killing the loss-aversion hook
2. **★★★★★** `M` 🟠 גבוה — _BLOOM Challenges (public prize contests)_ — The win moment has zero dopamine peak — no confetti, no sound, no haptic
3. **★★★★★** `M` 🟠 גבוה — _Spectator screen_ — No 'me vs them' comparison — the single biggest voyeuristic/competitive hook is missing
4. **★★★★★** `M` 🟠 גבוה — _Onboarding / FTUE / first-run routing / push prompt_ — No value-prop / welcome moment before the tutorial — straight into a chore
5. **★★★★★** `M` 🟠 גבוה — _Onboarding / FTUE / first-run routing / push prompt_ — FTUE ends with NO commitment CTA — silently dumps player on home
6. **★★★★★** `M` 🟠 גבוה — _Player Profile (/player/BLOOM-XXXX) + Welcome (/welcome) + Privacy (/privacy)_ — Profile has zero competitive/FOMO hook — it brags but never invites a challenge
7. **★★★★★** `M` 🟠 גבוה — _Pet / Mascot (Tamagotchi)_ — Guilt is emotional but not ACTIONABLE — no 'feed me now' CTA or time-specific loss-aversion
8. **★★★★★** `M` 🟠 גבוה — _Admin dashboard (owner control center)_ — No search/filter on the 200+ key config table — finding one knob means scrolling a wall
9. **★★★★** `S` 🟠 גבוה — _Weekly Recap (BLOOM Wrapped)_ — ESC / browser-back do NOT close either Wrapped modal
10. **★★★★** `S` 🟠 גבוה — _Game-over / Over-screen_ — Next-reward countdown is frozen — startNextRewardCountdown() is never defined
11. **★★★★** `S` 🟠 גבוה — _Duel modal + Live Race (60s) + Spectator widget_ — Live Race result overlay has NO ESC / backdrop / ✕ close — only one button
12. **★★★★** `S` 🟠 גבוה — _Player Profile (/player/BLOOM-XXXX) + Welcome (/welcome) + Privacy (/privacy)_ — Profile page has NO way back into the game shell or to your own profile — it is a viral dead-end for the viewer
13. **★★★★** `S` 🟠 גבוה — _Home screen (Power-Hero default)_ — The dominant CTA has an empty subtitle — biggest unused hook on the most-tapped element
14. **★★★★** `S` 🟠 גבוה — _Home screen (Power-Hero default)_ — 3 of 4 balance-bar slots are dead — gems/lives/level do nothing on tap
15. **★★★★** `S` 🟠 גבוה — _In-game gameplay UI_ — Persistent gameplay animations ignore prefers-reduced-motion → discomfort under pressure
16. **★★★★** `S` 🟠 גבוה — _In-game gameplay UI_ — Tile SVG is a fixed 30px inside a fitGrid cell that grows to ~60-70px → small, lonely glyphs that waste the board
17. **★★★★** `S` 🟠 גבוה — _Community cluster (Guilds + Guild Wars + Rivals + Ghost + Inbox)_ — Inbox panel cannot be closed with ESC or browser-back
18. **★★★★** `S` 🟠 גבוה — _Shop cluster (Gem Bank + Starter Pack + Gacha + Bundles + Boosters)_ — No confetti / climax FX on the highest-value monetization moments

### 🏗️ פרויקטים גדולים (מאמץ L — שווים אבל דורשים sprint)

- **★★★★★** 🟠 גבוה — _Dynamic Boards Picker + Season/Battle Pass_ — Picker header buries the actual boards under up to 8 stacked banners
- **★★★★★** 🟠 גבוה — _Progression cluster (Trophy Road + Trophy Chests + Album + Lifetime + Leagues)_ — Trophy Road has no social/ladder comparison — half the Clash Royale hook is missing
- **★★★★★** 🟠 גבוה — _Player Profile (/player/BLOOM-XXXX) + Welcome (/welcome) + Privacy (/privacy)_ — Profile OG/Twitter card is generic — a shared profile shows the same static image for everyone, killing the viral hook
- **★★★★★** 🟠 גבוה — _Bottom navigation (5 tabs)_ — Badges signal 'new tile mounted', NOT 'reward waiting to be claimed' — the #1 addiction miss
- **★★★★** 🟠 גבוה — _Global Design System / Dark mode / Toasts / Theme / Responsiveness_ — Design tokens are defined but barely adopted — the system is aspirational, not enforced
- **★★★★** 🟡 בינוני — _Weekly Recap (BLOOM Wrapped)_ — Grade & stats are volume-only — no growth/personalization (weak vs Spotify Wrapped)
- **★★★★** 🟠 גבוה — _Game-over / Over-screen_ — Over-screen is a 25+ element scroll wall — no single dominant moment after the score
- **★★★★** 🟡 בינוני — _Onboarding / FTUE / first-run routing / push prompt_ — Scripted demo means the player never makes a real choice or feels real agency
- **★★★★** 🟡 בינוני — _Pet / Mascot (Tamagotchi)_ — Only two thin daily actions and no social/streak layer — the loop caps out fast
- **★★★** 🟡 בינוני — _Duel modal + Live Race (60s) + Spectator widget_ — Zero design-token usage — entire duel modal + result overlays are hardcoded hex inline styles
- **★★★** 🟡 בינוני — _Daily Rewards cluster (Spin + Login Calendar + Checklist + Daily Deal)_ — Cluster is four visual silos with no unified 'daily rewards' identity or cross-linking

---

# 📄 ביקורת מפורטת לכל דף (ממוין לפי ROI)

## 1. BLOOM Challenges (public prize contests) — ציון `59/100` (C)

> **הקשר:** challenges mode (public prize contests)
>
> **מימדים:** התמכרות **4** · יופי **6** · נוחות **5** · בהירות **7** · פוקוס **7** · סגירוּת **9** · רלוונטיות **8**

**פסק דין:** סגיר-יפה וכן בתיאור, אבל מבזבז את שני המנופים הכי חזקים — אין שעון-ספירה חי ואפס חגיגה בניצחון עם פרס אמיתי. **כדי להעלות:** הוסף ticker חי (פעם בשנייה), הוסף קונפטי+סאונד+buzz בניצחון, החיה את ה-empty-state, והגדל את בליטות-הפרס.

**✅ מה כבר טוב (אל תיגע):**
- Closability is best-in-class: pre-enter and claim modals use .info-modal with a 44px-min .info-close (base.css:1809-1814), backdrop-tap dismiss (09-challenges.js:192,250), explicit cancel button, AND the global ESC/popstate handler covers both .info-modal and #challenge-screen (04-ui-utils.js:930,935) routing through the back button so cleanup is preserved.
- Strong dark-mode parity — 32 challenge-specific overrides in dark.css (cards, prize banner, warn, rules, standings, claim inputs, prize chip) so the whole surface is themed, not just the light path.
- Honest single-attempt framing is clearly communicated: the pre-enter .challenge-warn (09-challenges.js:177-179) spells out 'no reset, no pause, no return', which is the core scarcity contract of the feature.
- Relevance is well-gated: action button correctly branches across winnersFull / isWinner / completed / inProgress / not-entered states (09-challenges.js:130-141) so a player never sees an irrelevant CTA, and entered cards get a distinct green .entered style.
- Standings + history tab show real social proof — top-3 winner names with scores in history (09-challenges.js:62-66) and a top-10 standings list with crowns on the detail screen (09-challenges.js:119-126).

**🛠️ משימות לביצוע (7):**

- [x] ✅ **בוצע (סבב 2)** · **🟠 גבוה · מאמץ `M` · ★★★★★ · התמכרות** — **No live countdown ticker — urgency is a static coarse string, killing the loss-aversion hook**
  - **הבעיה:** challengeTimeLeft() (08-contest-helpers.js:407-417) returns only the largest coarse unit ('3 ימים' / '5 שעות' / '20 דקות' / 'פחות מדקה') and is baked once into static innerHTML on both the list card (09-challenges.js:59) and the detail sub-line (09-challenges.js:145). There is no setInterval ticker, no seconds, and no visual escalation as the deadline nears. A single-attempt real-prize contest is the strongest FOMO lever in the whole app and it currently reads like a calm calendar note. A player who opens a challenge with 47 minutes left sees the same flat '20 דקות' label they'd see with 50 minutes left, then it silently goes stale.
  - 📍 **הוכחה:** `08-contest-helpers.js:407-417 (coarse units, no seconds); 09-challenges.js:59 (rendered once, never updated); 09-challenges.js:145 ('נותרו ' + challengeTimeLeft(...) static in detail sub)`
  - 🔧 **לעשות:** In showChallengeDetail (09-challenges.js:142) wrap the time-left in an element id='chal-countdown' and start a 1s setInterval that recomputes a HH:MM:SS string (reuse the formatCountdown pattern from the daily-reset countdown). When ms < 1h, add a class .challenge-countdown-urgent that turns the pill red and pulses (new keyframe in screens.css, gated by prefers-reduced-motion). Tear the interval down when #challenge-screen leaves the DOM. Mirror a compact live mm:ss on each list card when <2h remain. Result: the deadline visibly ticks down, escalating to a red pulse in the final hour — the 'play now or lose the prize forever' pull the feature is built for.
- [x] ✅ **בוצע (סבב 2)** · **🟠 גבוה · מאמץ `M` · ★★★★★ · התמכרות** — **The win moment has zero dopamine peak — no confetti, no sound, no haptic**
  - **הבעיה:** Winning a real prize is the single highest-emotion event the feature can produce, yet the entire challenge flow contains zero celebration: grep confirms no showConfetti, no soundMilestone, no buzz() anywhere in 09-challenges.js. The mid-game win is a plain dark-green spectator-toast div (09-challenges.js:335-342) with no animation. The result screen winner state is just a green over-title + score + banner (09-challenges.js:384-392). Compared to nearly every other stage (trophy/spin/league all fire showConfetti + soundMilestone(7) + buzz on their wins), the prize-contest win feels flatter than merging two tiles.
  - 📍 **הוכחה:** `09-challenges.js (grep: 0 hits for showConfetti/soundMilestone/buzz/confetti); 09-challenges.js:335-342 (win toast = static green div); 09-challenges.js:384-392 (result winner = plain HTML)`
  - 🔧 **לעשות:** In renderChallengeResult (09-challenges.js:384) when isWinner, fire window.__bloomConfetti(60) (the global exposed from 14-events.js), soundMilestone(7), and buzz([40,30,60,30,90]) 250ms after the overlay mounts. Upgrade showChallengeWinToast (09-challenges.js:335) to a gold-gradient slide-in card with a 👑 overshoot pop animation + soundMilestone(5) + buzz, respecting prefers-reduced-motion. Result: crossing the prize threshold feels like THE moment of the game — drives screenshot-share behavior and return-tomorrow pull.
- [x] ✅ **בוצע (סבב 2)** · **🟡 בינוני · מאמץ `S` · ★★★★ · נוחות** — **Contact forms are placeholder-only with no input typing or autocomplete — friction at the highest-value step**
  - **הבעיה:** The winner claim form (09-challenges.js:240-242 and 395-397) uses placeholder-only inputs with no persistent <label>, no type='tel'/type='email', no inputmode, and no autocomplete attributes. On a phone the phone field pops the alpha keyboard instead of the numpad, and the email field gets autocapitalization. The placeholder disappears on focus so a player mid-typing loses the field's meaning. This is the single most important conversion step in the feature (winner actually claiming a real prize) and it's the most friction-laden.
  - 📍 **הוכחה:** `09-challenges.js:240-242 (cc-name/cc-phone/cc-email inputs, placeholder-only, no type/inputmode); 09-challenges.js:395-397 (cr- result form, same); screens.css:606-612 (.challenge-claim-form input, no label styling)`
  - 🔧 **לעשות:** In showChallengeClaim (09-challenges.js:240-242) and renderChallengeResult (09-challenges.js:395-397) add type='tel' inputmode='tel' autocomplete='tel' to the phone field, type='email' inputmode='email' autocomplete='email' autocapitalize='off' to the email field, and autocomplete='name' to the name field. Add small persistent .contest-form-label captions above each input (the style already exists at screens.css:76-82). Result: correct mobile keyboards, native autofill, and labels that survive focus — measurably higher claim completion.
- [x] ✅ **בוצע (סבב 2)** · **🟡 בינוני · מאמץ `M` · ★★★★ · התמכרות** — **Empty active-list state is a dead-end that tells the player to leave**
  - **הבעיה:** When no challenges are active the list shows 'אין אתגרים פעילים כרגע. נסה מחר.' (09-challenges.js:46) — literally instructing the player to stop and come back. That's a churn nudge, not a retention one. There is no fallback CTA to the daily/practice/contest modes, no 'get notified when the next prize drops' hook, and no preview of an upcoming challenge to anticipate.
  - 📍 **הוכחה:** `09-challenges.js:44-48 (empty-state copy 'נסה מחר' with no CTA)`
  - 🔧 **לעשות:** In renderChallengeListBody (09-challenges.js:44) replace the bare empty string with a card that (a) keeps a warm line ('האתגר הבא בקרוב 🎁'), (b) offers a 'הפעל התראה' push opt-in via window.__bloomMaybeAskPush('challenge_drop'), and (c) shows a secondary CTA routing to practice/daily so the session continues. If the server can return the next scheduled starts_at, show '⏰ מתחיל בעוד HH:MM' as anticipation. Result: an empty challenge tab keeps the player in-game and primes the next prize-drop return.
- [x] ✅ **בוצע (סבב 2)** · **🟡 בינוני · מאמץ `M` · ★★★★ · יופי** — **Prize prominence is weak in the list — the reward is an 11px corner pill**
  - **הבעיה:** The prize — the entire reason this feature exists — is rendered as a small 11px amber pill tucked into the top-right corner of each card (.challenge-card-prize, screens.css:501-508; 09-challenges.js:73), competing visually with the card name. There's no prize-value hierarchy, no 'real prize' badge, and no scarcity emphasis on how few winners remain. The detail screen does better with a gradient banner (screens.css:527-539) but even there the 'winners remaining' info is buried as plain meta text in the sub-line (09-challenges.js:145), not a scarcity chip.
  - 📍 **הוכחה:** `screens.css:501-508 (.challenge-card-prize 11px corner pill); 09-challenges.js:73; 09-challenges.js:145 (winners count as plain '· N זוכים' in sub)`
  - 🔧 **לעשות:** Restructure the list card (09-challenges.js:70-81) so the prize gets its own full-width row below the name with a larger (14px) value, a 🎁 gift icon, and a 'פרס אמיתי' micro-badge. Add a scarcity chip when winnersCount - winnersFilled <= 2 reading 'נשאר מקום אחד לזכייה!' in a pulsing red pill. On the detail screen, promote the winners-remaining count out of the sub-line (09-challenges.js:145) into a dedicated scarcity strip above the prize banner. Use design tokens for the amber (--color-warning) so it stays theme-consistent. Result: the prize and its scarcity dominate the card, sharpening the want.
- [x] ✅ **בוצע (סבב 2)** · **⚪ נמוך · מאמץ `S` · ★★★ · בהירות** — **Type/rules clarity gap — challenge-type label is jargon and rules are easy to skip**
  - **הבעיה:** The type pill shows terse labels like 'Top 5' or 'ראשון לדרגה X' (08-contest-helpers.js:397-405) with no one-line explanation of how to win, so a new player can't tell in <3s whether they need a high score, to be first, or to cross a threshold. The full rules sit in a collapsed .challenge-rules box capped at 140px with overflow scroll (screens.css:551-559) and the 'קראתי את התקנון' checkbox (09-challenges.js:183) is only required when rulesText exists — so most challenges have no comprehension gate at all before the irreversible single attempt.
  - 📍 **הוכחה:** `08-contest-helpers.js:397-405 (terse type labels); screens.css:551-559 (.challenge-rules 140px scroll box); 09-challenges.js:183 (checkbox conditional on rulesText)`
  - 🔧 **לעשות:** Add a one-line 'how to win' helper under the type pill in showChallengeDetail (09-challenges.js:145) — e.g. race → 'הראשון שמגיע ל-N נקודות זוכה', top_n → 'N הניקודים הגבוהים ביותר זוכים'. Build this from challengeType in challengeTypeLabel's sibling helper, not from admin rulesText, so it always renders. Keep the detailed rules box for fine print. Result: a player understands the win condition instantly before spending their one and only attempt.
- [ ] **⚪ נמוך · מאמץ `M` · ★★★ · יופי** — **Hardcoded hex colors throughout instead of design tokens — visual drift + theme-maintenance debt**
  - **הבעיה:** The entire challenge surface is built on hardcoded hex literals — purple #6B5BD3 (screens.css:537), amber #FAC775/#854F0B (screens.css:502-503), ink #1C1A18 (screens.css:500), inline #BA7517 in JS (09-challenges.js:66) — rather than the base.css :root tokens (--color-accent, --color-warning, --radius-md, --shadow-glow). Border-radius is also ad-hoc (14px cards, 16px banner, 12px warn, 10px chip) instead of the --radius-sm/md/lg scale. This is why every new surface needs a parallel hand-written dark.css block (32 overrides) and risks drifting from the rest of the app's palette.
  - 📍 **הוכחה:** `screens.css:537 (#6B5BD3); screens.css:502-503 (#FFF5E1/#854F0B/#FAC775); 09-challenges.js:66 (inline color:#BA7517); radii 14/16/12/10px across screens.css:483,532,543,589`
  - 🔧 **לעשות:** Migrate the .challenge-* rules in screens.css to design tokens: accent purple → var(--color-accent), amber → var(--color-warning), text → var(--color-text)/var(--color-text-muted), and the four ad-hoc radii → var(--radius-md)/var(--radius-lg). Replace the inline color:#BA7517 in 09-challenges.js:66 with a CSS class. This is the proof-of-pattern token migration the codebase already started (§2.2). Result: consistent palette with the rest of BLOOM and far fewer hand-written dark overrides needed.

---

## 2. Spectator screen — ציון `62/100` (C+)

> **הקשר:** watch live contest players (contest mode only)
>
> **מימדים:** התמכרות **5** · יופי **6** · נוחות **6** · בהירות **8** · פוקוס **7** · סגירוּת **6** · רלוונטיות **8**

**פסק דין:** תצוגת-צפייה יציבה שתיקנה את באג ה-grid הישן — אבל משדרת ניטרלי (בלי 'אני מולם', בלי juice) אז מיידעת בלי לְמַכֵּר. **כדי להעלות:** הוסף השוואת 'אתה X · הם Y' עם פעימה כשעוברים, החיה את הלוח (flash-merge + count-up), חבר ESC/back, ועבור ל-tokens.

**✅ מה כבר טוב (אל תיגע):**
- Robust reconnection handling: spectatorTick distinguishes a network throw (never auto-close, just show '🔄 מתחבר מחדש…') from a server 404 (only declares game-over after 8 sustained misses ≈8s), with a recovery path that clears the banner — far more forgiving than the old 2-tick close (src/10-spectator.js:145-218).
- The grid-collapse bug is genuinely fixed and documented: .spectator-grid > .grid gets an explicit width:min(280px,80vw) + aspect-ratio 4/6 + direction:ltr so the borrowed game .grid template doesn't collapse to 0x0 (public/css/screens.css:915-934).
- Smart exit-label routing — the exit button text adapts to entry context ('חזור למשחק שלי' mid-game / 'חזור ללוח התחרות' from leaderboard / 'צא מהצפייה' default) and stopSpectator resumes the saved game vs re-opens the board vs returns to game-over accordingly (src/10-spectator.js:249-338).
- The tier-ladder micro-narrative (8 emoji dots, current 'on', next pulsing pink, label 'דרגה 5 · עוד 3 לכתר 👑') converts a passive scoreboard into a goal-anchored story and reuses snap.tier with zero extra server data (src/10-spectator.js:256-276).

**🛠️ משימות לביצוע (6):**

- [x] ✅ **בוצע (סבב 3)** · **🟠 גבוה · מאמץ `M` · ★★★★★ · התמכרות** — **No 'me vs them' comparison — the single biggest voyeuristic/competitive hook is missing**
  - **הבעיה:** The spectator header shows only the watched player's live score + tier (src/10-spectator.js:277-288). The watcher already exposes their own score to the server (spectatorHeartbeat sends watcherLastScore, src/10-spectator.js:124-130) and knows their own contest standing, but the view never tells the watcher 'they're 4,200 ahead of you' or 'you'd still beat them'. Watching a rival is only compulsive when it's framed as a threat or an opportunity against ME. As built it's a neutral TV broadcast — informative, not addictive.
  - 📍 **הוכחה:** `src/10-spectator.js:287 renders only `<div class="spectator-header-score">` for the target; no delta vs getLastFinalScore(s.code) or the watcher's contest score is computed or shown.`
  - 🔧 **לעשות:** In renderSpectatorView (src/10-spectator.js:220-295), compute a delta = targetLiveScore − myContestScore (read my score via the same getLastFinalScore/contest-data path used in spectatorHeartbeat). Add a pill under the score: behind → red '⚔️ הוא לפניך ב-4,200' with subtle pulse; ahead → gold '👑 אתה עדיין מוביל ב-1,100'. Add a CTA when behind: 'שחק עכשיו ועקוף אותו' that exits spectate and starts a contest game. New CSS class .spectator-delta-pill in screens.css using --color-danger / --color-accent tokens. Result: turns watching into a personal challenge — the strongest return/compulsion lever this screen can have.
- [x] ✅ **בוצע (סבב 3)** · **🟠 גבוה · מאמץ `M` · ★★★★ · התמכרות** — **Static board between ticks + no merge/score juice — watching feels frozen, not 'live'**
  - **הבעיה:** Every 1s tick rebuilds the entire grid via wrap.innerHTML (src/10-spectator.js:277-295) with zero transition — tiles teleport, the score number snaps, and there is no merge flash, no chain pop, no score count-up. A 1-second-resolution board with no motion reads as a still photo refreshing, which kills the 'someone is playing RIGHT NOW' tension that makes spectating compelling. The only animated element is the LIVE dot (screens.css:910-914) and the next-tier dot (screens.css:953-957).
  - 📍 **הוכחה:** `renderSpectatorView does a full innerHTML replace each tick (src/10-spectator.js:277); .spectator-grid .cell has no entrance/merge animation (screens.css:931-934); liveScoreText is set directly with no count-up (src/10-spectator.js:245,287).`
  - 🔧 **לעשות:** Diff snapshots between ticks: keep a per-cell tier map on spectatorSession; on tick, only update changed cells and add a one-shot class (.spec-cell-pop scale 0.7→1.12→1 on a new tile, .spec-cell-merge brightness flash on an upgrade) — mirror the dspecAppear/dspecMerge pattern already used elsewhere. Animate the header score with a short count-up (lerp old→new over ~400ms, tabular-nums already set). Respect prefers-reduced-motion. Result: the board visibly 'plays', restoring the voyeuristic live tension.
- [x] ✅ **בוצע (סבב 3)** · **🟡 בינוני · מאמץ `S` · ★★★ · סגירוּת** — **ESC / back-gesture does nothing during live watch — the global close path is silently dead**
  - **הבעיה:** The ESC/history-back handler (__bloomGetCloseableModals, src/04-ui-utils.js:917-936) targets a selector '#spectator-screen' that is NEVER created in the DOM — the live view is injected as .spectator-view INTO #grid-wrap (src/10-spectator.js:277). So pressing ESC or swiping back while watching closes nothing; the player is trapped to the on-screen buttons only. The picker modal (.info-modal) IS covered, but the full-screen watch view is not. The same phantom id is guarded against in 14-events.js:27 (`if (getElementById('spectator-screen')) return false`), so bonus-event suppression during spectate also never fires.
  - 📍 **הוכחה:** `grep shows '#spectator-screen' referenced only in src/04-ui-utils.js:935 and src/14-events.js:27, never created — the real view is `wrap.innerHTML = '<div class="spectator-view">…'` (src/10-spectator.js:277).`
  - 🔧 **לעשות:** Either (a) give the injected view a real hook: add id/class 'spectator-screen' to the .spectator-view wrapper in src/10-spectator.js:278 AND make __bloomDismissTopmostModal route it to stopSpectator('exit') instead of clicking a back button; or (b) in src/04-ui-utils.js add a branch: if spectatorSession is active, call stopSpectator('exit') and return true. Also update 14-events.js:27 to check `window.spectatorSession` instead of the phantom id so bonus events can't spawn over the spectator grid. Result: ESC/back-gesture exits the watch cleanly, matching the UX-gate Q4 'one obvious way to close'.
- [x] ✅ **בוצע (סבב 3)** · **🟡 בינוני · מאמץ `S` · ★★★ · נוחות** — **Picker auto-refreshes once and then goes stale — empty/list state freezes**
  - **הבעיה:** refreshSpectatorPicker (src/10-spectator.js:29-70) fetches the live-player list exactly ONCE on open. There is no polling interval, so a player who opens the picker on 'אין כרגע שחקנים פעילים' (src/10-spectator.js:43) must close and reopen to discover someone went live; conversely the +liveScore numbers shown are frozen at open-time and grow stale within seconds. There's also no loading skeleton beyond a plain 'טוען…' text and no manual refresh affordance.
  - 📍 **הוכחה:** `openSpectatorPicker calls refreshSpectatorPicker() once (src/10-spectator.js:26); no setInterval anywhere in the picker path; empty state is terminal text (src/10-spectator.js:43).`
  - 🔧 **לעשות:** In openSpectatorPicker, store a picker poll timer (setInterval(refreshSpectatorPicker, 4000)) and clear it in the close handler at src/10-spectator.js:24-25 and inside the row onclick before startSpectator (src/10-spectator.js:66). Add a small skeleton-row shimmer for the 'טוען…' state and a '🔄 רענן' affordance on the empty state. Guard against clobbering focus/scroll on re-render. Result: the picker stays live, +scores tick up, and a newly-active rival appears without a manual reopen.
- [ ] **🟡 בינוני · מאמץ `M` · ★★★ · יופי** — **Bare hex colors everywhere — bypasses design tokens, weakens dark-mode parity and consistency**
  - **הבעיה:** The entire spectator stylesheet uses hardcoded hex/rgba (#FAFAF6, #1B5E20, #C8472F, #6F6E68, rgba(255,107,157,…)) instead of the base.css :root tokens (--color-surface, --color-success, --color-danger, --color-accent, --radius-md, --shadow-*). This is why dark mode needs ~12 separate manual overrides in dark.css (lines 90,152-154,224-232,286,368) and why the green '+score' / live-tag red can drift from the rest of the app. The reconnect banner is even inline-styled in JS with raw hex (src/10-spectator.js:208).
  - 📍 **הוכחה:** `screens.css:855 background:#FAFAF6; :870 color:#1B5E20; :904 background:#C8472F; :959 color:#8B3E0A; reconnect banner inline cssText at src/10-spectator.js:208 uses rgba(0,0,0,0.78)/#FAC775.`
  - 🔧 **לעשות:** Refactor public/css/screens.css spectator block (847-974) to use tokens: surfaces → var(--color-surface)/var(--color-surface-2); positive score → var(--color-success); live-tag/behind → var(--color-danger); radii → var(--radius-md/--radius-lg); the reconnect/toast/header shadows → var(--shadow-md). Move the reconnect-banner styling out of JS inline cssText into a .spectator-reconnect class. Then delete the now-redundant dark.css overrides. Result: automatic dark-mode parity, app-wide visual consistency ('יפה'), and one less place colors can drift.
- [x] ✅ **בוצע (סבב 3)** · **⚪ נמוך · מאמץ `S` · ★★ · נוחות** — **Exit/switch buttons sit below the fold; no thumb-anchored controls**
  - **הבעיה:** .spectator-controls is rendered after the grid + tier-ladder in normal document flow with margin-top:14px (screens.css:935-939), and .spectator-view has no fixed/sticky footer. On a tall phone with the ~280px grid + header + 8-dot ladder, the exit/switch row can land near or below the comfortable thumb zone, and during a 'reconnecting' state the controls don't move up. The switch/exit buttons are also equal-weight (flex:1 each), so the primary action (exit) doesn't visually dominate.
  - 📍 **הוכחה:** `screens.css:935-939 .spectator-controls is flow-positioned with both buttons flex:1; .spectator-controls .btn.secondary just gets a grey background (screens.css:940), no size/weight differentiation; no position:sticky/fixed.`
  - 🔧 **לעשות:** Make .spectator-controls position:sticky; bottom: calc(8px + env(safe-area-inset-bottom)) within the view (or fixed within grid-wrap), give the primary exit button more visual weight (full accent fill via --color-accent, slightly larger) and keep 'החלפת שחקן' as a lighter secondary. Ensure ≥44px tap height. Result: one-thumb reach, clearer primary action, no scrolling to leave.

---

## 3. Dynamic Boards Picker + Season/Battle Pass — ציון `62/100` (C+)

> **הקשר:** home tile + picker modal (showDynamicBoardsPicker) and the dual-track Battle Pass modal (showSeasonPassModal / renderSeasonModalBody)
>
> **מימדים:** התמכרות **8** · יופי **6** · נוחות **5** · בהירות **5** · פוקוס **3** · סגירוּת **7** · רלוונטיות **7**

**פסק דין:** מעצמת-שימור שטובעת בהצלחה שלה — כל מנוף קיים וה-Battle-Pass dual-track הכי טוב בכיתה, אבל עד 8 באנרים פועמים קוברים את הלוחות מתחת לקיפול. **כדי להעלות:** כווץ את הבאנרים לרצועת-pills אחת מתקפלת מעל הלוחות, החלף את confirm() של ה-premium במודאל מותג, הגדל כפתורים, הוסף reduced-motion.

**✅ מה כבר טוב (אל תיגע):**
- Exceptionally rich retention stack layered onto one surface: per-board personal-best chip (🏆 שיא), global leader target chip (👑 name: score), 'be first' pioneer chip, FOMO countdown badges (critical/new/soon), daily-special XP+reward multiplier banner, live tournament banner, cross-board streak banner with milestone progress, and per-board earned-achievement icon stacks — every card is a self-contained dopamine loop (05c:2896-2939).
- Dual-track Battle Pass executes the loss-aversion pattern textbook-correctly: locked-but-earned premium tiers render as 🔒 +N💎 teases (renderSeasonModalBody 05c:1746-1751) and the buy banner sums exactly how many gems are 'waiting for you' in tiers you already crossed (05c:1699-1701) — the strongest monetization punch in the codebase.
- Closability is solid: .dyn-boards-overlay is registered in the global ESC handler allowlist (04-ui-utils.js:923) and .dyn-season-modal-overlay matches the generic [class*=modal-overlay] selector; both modals also have backdrop-tap-to-close and a sticky bottom 'חזרה לבית'/'חזור' button.
- Relevance discipline is good: banners only render when the server confirms the feature is live (daily special guarded by window._dailySpecial.enabled, tournament by getLiveTournament, premium column hidden when admin disables it at 05c:1739), and renderFomoBadge returns '' for non-urgent boards (05c:3012) keeping boring cards clean.
- Per-card urgency class + FOMO badge re-paint via refreshPickerTimers (05c:3017) updates only the badge hosts every 60s without losing scroll/focus — a genuinely careful live-countdown implementation.

**🛠️ משימות לביצוע (7):**

- [ ] **🟠 גבוה · מאמץ `L` · ★★★★★ · פוקוס** — **Picker header buries the actual boards under up to 8 stacked banners**
  - **הבעיה:** showDynamicBoardsPicker stacks Daily-Special + Tournament + Friends + Season + Quests + Achievements + Streak + Freeze banners ABOVE the board list (05c:2770-2792). The CSS author already documented that on a phone this consumed ~560px and squeezed the list to ~6px tall, and the 'fix' was to make the whole modal scroll as one unit (boards.css:925-931). That means the screen's actual purpose — picking a board — is pushed entirely below the fold on first open. There is no single dominant action; the eye is pulled in 8 directions, violating UX gate Q3.
  - 📍 **הוכחה:** `src/05c-dynamic-boards.js:2764-2798 (8 banner variables concatenated into .dyn-boards-head); public/css/boards.css:925-931 (comment admitting the list was squeezed to 6px, now whole-modal-scroll workaround)`
  - 🔧 **לעשות:** In showDynamicBoardsPicker (05c:2764), collapse the secondary headers into a single horizontal 'rewards' strip or a one-line accordion: keep only the single highest-priority banner expanded (pickFocusBoard logic already exists at 05c:68 — reuse its priority: daily-special > live-tournament > streak-danger > unclaimed-season), and render the rest as compact tappable pills in one wrap row (icon + count badge) that expand on tap. Goal: the first board card is visible without scrolling on a 750px-tall phone. Add a thin 1px divider + 'הלוחות' label directly above #dyn-boards-list so the player's eye lands on the list.
- [ ] **🟠 גבוה · מאמץ `M` · ★★★★ · יופי** — **Premium purchase uses a native browser confirm() — jarring and off-brand for the #1 monetization flow**
  - **הבעיה:** confirmAndBuyPremium fires window.confirm() with a multi-line \n-formatted English-mixed string (05c:1835-1839) for the single highest-revenue action in the game. A native OS dialog breaks the RTL Hebrew visual language, renders LTR with system fonts, has no brand styling, no animation, and on iOS shows the URL/title — the exact moment a player decides to spend 1500💎 looks like a cheap error popup instead of a premium upgrade moment.
  - 📍 **הוכחה:** `src/05c-dynamic-boards.js:1835-1839 (var msg = '...\n\n...'; if (!confirm(msg)) return;)`
  - 🔧 **לעשות:** Replace the confirm() in confirmAndBuyPremium (05c:1830) with a branded in-app confirmation overlay reusing the existing modal styling (gold .dyn-season head palette). Show: ✨ icon, price vs balance line, the total premium gems they unlock, the 'תקף לעונה הנוכחית בלבד' note, and two ≥44px buttons (🔓 שדרג · cancel). Animate in with the dynBoardsSlideUp cubic-bezier already used at boards.css:1829 so it feels like a celebration, not a warning.
- [ ] **🟡 בינוני · מאמץ `S` · ★★★ · נוחות** — **Both close buttons are 32px — below the 40px tap-target gate**
  - **הבעיה:** The picker close ✕ and the season-modal close ✕ are both 32×32px (boards.css:952 and :1842), under the UX-gate ≥40px requirement. On a phone, a 32px target near the top-left corner (RTL) is an easy mis-tap, especially since the picker close scrolls away with the header (only the bottom 'חזרה לבית' stays sticky), forcing a scroll-up to reach it.
  - 📍 **הוכחה:** `public/css/boards.css:950-957 (.dyn-boards-close width:32px height:32px); public/css/boards.css:1839-1848 (.dyn-season-modal-close width:32px height:32px)`
  - 🔧 **לעשות:** Bump both .dyn-boards-close and .dyn-season-modal-close to width/height 40px (keep the inner glyph font-size, just enlarge the hit area), and add a subtle :focus-visible outline using var(--color-accent) for keyboard users. Consider making the picker close button position:sticky within the head or duplicating a small ✕ in the sticky foot so it is always reachable without scrolling up.
- [ ] **🟡 בינוני · מאמץ `S` · ★★★ · יופי** — **No prefers-reduced-motion guard on the picker's many infinite animations**
  - **הבעיה:** The picker header simultaneously runs multiple infinite keyframe loops — dailySpecialBannerPulse 2.4s (boards.css:3640), dynFomoPulseCritical 1.4s (boards.css:1097), dynFomoPulseNew 2s (boards.css:1103), dynSeasonBuyStar on the premium buy banner — with zero prefers-reduced-motion coverage in boards.css (only .ds-xp-chip and gacha cards at boards.css:3029 and :5215 are guarded). For motion-sensitive users this header is a constant flicker, and multiple competing pulses also dilute the FOMO signal (everything pulsing = nothing stands out).
  - 📍 **הוכחה:** `public/css/boards.css:3640, :1097, :1103 (infinite animations); only :3029 and :5215 carry prefers-reduced-motion guards — none cover the dyn-boards picker banners`
  - 🔧 **לעשות:** Add a single @media (prefers-reduced-motion: reduce) block in boards.css setting animation: none on .dyn-daily-special-banner, .dyn-fomo-pill-critical, .dyn-fomo-pill-new, .dyn-season-buy-banner, .dyn-tournament-banner-pulse, and .dyn-season-headline-claim. Separately, even for motion-OK users, reduce simultaneous pulses: pulse ONLY the single highest-priority banner (tie into the focus fix above) and give the rest a static accent border so one signal dominates.
- [ ] **🟡 בינוני · מאמץ `S` · ★★★ · נוחות** — **Dual-track claim buttons are too small for the highest-value reward action**
  - **הבעיה:** In the dual-track tier rows, both free and premium claim CTAs share .dyn-season-track-cta with padding 6px 8px / font 12px (boards.css:3934-3947) and flex:1 inside a flex row (boards.css:3927). On a 20-tier list with two side-by-side buttons per row, each '🎁 +N💎' claim target is ~28px tall and quite narrow — small for the satisfying 'collect my reward' tap that should feel rewarding. The premium tease and free claim also look near-identical in size, weakening the visual hierarchy of 'this one is collectable NOW'.
  - 📍 **הוכחה:** `public/css/boards.css:3934-3947 (.dyn-season-track-cta padding:6px 8px; font-size:12px); public/css/boards.css:3927-3933 (.has-premium tracks flex row)`
  - 🔧 **לעשות:** In boards.css raise .dyn-season-track-cta min-height to 36px and padding to 9px 10px. Give the actively-claimable variant .dyn-season-track-cta-claim a stronger affordance (slightly larger font 13px + a subtle one-shot scale-up animation on render, prefers-reduced-motion-guarded) so the eye locks onto the row that can be claimed right now versus the locked/tease rows.
- [ ] **⚪ נמוך · מאמץ `S` · ★★ · בהירות** — **Empty-state and premium-buy footer contain confusing dead-end copy**
  - **הבעיה:** Two micro-copy issues hurt the <3s comprehension: (1) the premium buy banner foot reads 'תקף לעונה הנוכחית בלבד · 4.99 $ דרך IAP (בקרוב)' (05c:1713) — mixing a current gem price with a future-but-unavailable USD path is confusing at the decision moment and reads like a half-built feature. (2) The boards empty-state (05c:2856-2861) is a fine fallback but the whole rich header (8 banners) still renders above it, so a player with no boards still scrolls past tournament/season/quest banners to reach a 'no boards yet' message — header should compress when the list is empty.
  - 📍 **הוכחה:** `src/05c-dynamic-boards.js:1713 (IAP בקרוב footer); src/05c-dynamic-boards.js:2855-2861 (empty state renders below full 8-banner header)`
  - 🔧 **לעשות:** Drop the '4.99 $ דרך IAP (בקרוב)' clause from the buy-banner foot (05c:1713) until IAP is live — keep only 'תקף לעונה הנוכחית בלבד'. In showDynamicBoardsPicker, when boards.length === 0 (05c:2855), suppress or collapse the secondary reward banners (season/quests/achievements/freeze) so the empty-state message and a clear alternate CTA (daily challenge / friends contest) are the first thing seen.
- [ ] **⚪ נמוך · מאמץ `M` · ★★ · יופי** — **Picker + season modals use zero design tokens — all hardcoded hex**
  - **הבעיה:** The entire picker and season modal CSS is hardcoded hex (#FFF7E6, #BA7517, #FAC775, #FF6B9D, #5A2A00, radius values 18px/20px/12px, shadows) rather than the base.css :root tokens (--color-accent, --radius-md, --shadow-glow, --z-modal). Only one stray var() appears (--color-text-muted fallback at boards.css:3950). This makes theme/brand changes risky and inconsistent, and the z-index 9000 (boards.css:913) bypasses the --z-modal token convention, risking stacking conflicts with toasts/celebrations.
  - 📍 **הוכחה:** `public/css/boards.css:910-1014 (picker, all hex), :1820-1948 (season modal, all hex), :913 (z-index:9000 hardcoded), :3950 (the lone var() usage)`
  - 🔧 **לעשות:** Incrementally migrate the picker/season surfaces to tokens: swap the modal radius to var(--radius-lg), card radius to var(--radius-md), the overlay shadow to var(--shadow-md), z-index:9000 to var(--z-modal), and map the gold accents to var(--color-accent)/--color-accent-ink where the brand allows. Do it as a contained pass so dark-mode parity (already present at boards.css:2034+) keeps working.

---

## 4. Global Design System / Dark mode / Toasts / Theme / Responsiveness — ציון `62/100` (C+)

> **הקשר:** cross-cutting — the look-and-feel backbone (base.css tokens, dark.css, viral.css, src/04-ui-utils.js)
>
> **מימדים:** התמכרות **6** · יופי **5** · נוחות **6** · בהירות **7** · פוקוס **6** · סגירוּת **7** · רלוונטיות **8**

**פסק דין:** תשתית tokens+סגירוּת+נגישות מעוצבת-היטב — שכמעט לא מחווטת (~4% אימוץ tokens, היררכיית z-index מתועלמת, toast לבן קשיח, 366 אנימציות מול 9 הגנות reduced-motion). זה עמוד-השדרה של 'יפה ונוח' והוא שאיפתי בלבד. **כדי להעלות:** campaign אימוץ-tokens הדרגתי, אכוף את ה-z-index, תקן את ה-toast, והוסף בלוק reduced-motion גלובלי ל-viral.css. **תיקון כאן מרים את כל 20 הדפים האחרים.**

**✅ מה כבר טוב (אל תיגע):**
- Token vocabulary is genuinely well-designed: base.css:17-87 defines a clean accent/neutral/semantic palette, an 8-level z-index hierarchy with documented layer logic, micro-interaction tokens (--anim-fast/base/slow with admin-tunable --anim-mult), and 5 reusable keyframes — the FOUNDATION is solid even if adoption is low.
- Global ESC + browser-back modal handler (04-ui-utils.js:917-1016) is a strong closability win: a single keydown+popstate listener finds the topmost overlay, prefers the modal's own close button (preserving cleanup/refund logic), with a thoughtful EXCLUDE list so celebrations/FTUE/in-game FX are never yanked mid-frame.
- Safe localStorage wrappers (04-ui-utils.js:7-33) correctly defend against Safari Private Mode / iOS quota throws and are exposed on window so admin/bot reuse the same defense — prevents a corrupt key from white-screening boot.
- Keyboard :focus-visible rings (base.css:107-117) and a global prefers-reduced-motion guard on the 5 shared micro-interaction classes (base.css:100-102) show real accessibility intent.
- viral.css has solid dark-mode parity (39 html[data-theme] overrides) and the streak calendar (04-ui-utils.js:686-756) is a genuinely strong loss-aversion surface with a prospective next-milestone line and milestone reward previews.

**🛠️ משימות לביצוע (6):**

- [ ] **🟠 גבוה · מאמץ `M` · ★★★★ · יופי** — **z-index hierarchy tokens are ignored — overlays still hardcode 9998/9999/10000/10005, causing collisions**
  - **הבעיה:** base.css:65-74 defines a beautiful 8-level z-index system (--z-modal:2000 ... --z-toast:3000 ... --z-critical:4000) precisely to end the 'escalating z-index war' that left close buttons unclickable. Yet only 3 var(--z-) references exist against 200 z-index declarations. The toast hardcodes z-index:10005 (04-ui-utils.js:433) — IDENTICAL to the push-permission prompt's 10005 (16-push.js:91), so a toast firing while the push prompt is open can render on top of (or under, unpredictably) a system-blocking prompt. Modals hardcode 10000 (02-shop.js:2366), banners 9998/9999. The token system exists but nobody uses it, so the original stuck-modal bug class is still live.
  - 📍 **הוכחה:** `base.css:65-74 tokens; toast at 04-ui-utils.js:433 'z-index:10005'; push prompt 16-push.js:91 'z-index:10005' (collision); modal 02-shop.js:2366 'z-index:10000'; base.css hardcodes 9998 (1197,1641), 9999 (1333,1377...), 10002 (1061). Only 2 var(--z-) in base.css.`
  - 🔧 **לעשות:** In 04-ui-utils.js showToast, change the inline 'z-index:10005' to use the token: 'z-index:var(--z-toast,3000)'. Change the push prompt (16-push.js:91) to 'z-index:var(--z-critical,4000)' so critical system prompts always sit above toasts. Migrate the modal overlays (02-shop.js:2366 and siblings) to 'var(--z-modal,2000)' / 'var(--z-modal-stack,2100)'. This eliminates the toast/push collision and makes stacking deterministic.
- [ ] **🟠 גבוה · מאמץ `L` · ★★★★ · יופי** — **Design tokens are defined but barely adopted — the system is aspirational, not enforced**
  - **הבעיה:** The whole point of base.css :root tokens is ONE source of truth so the app stops feeling like stitched-together screens — but adoption is in the low single digits. Across all CSS: 3,738 hardcoded hex values vs only 172 var(--color) references (~4.4%), 26 var(--radius), and just 3 var(--z-) references against 200 z-index declarations. Even base.css itself has 259 hardcoded hex and only 12 var(--color) uses. This means a brand re-theme requires editing thousands of literals, dark-mode gaps creep in per-surface, and radius/shadow/spacing drift screen-to-screen — exactly the 'feels like different apps' problem tokens were meant to solve.
  - 📍 **הוכחה:** `base.css:17-87 defines tokens; grep shows 3738 hardcoded hex vs 172 var(--color) / 26 var(--radius) / 3 var(--z-) across public/css/*.css. base.css internal: 259 hex vs 12 var(--color).`
  - 🔧 **לעשות:** Run an incremental token-migration sweep, highest-traffic surfaces first: in base.css, dark.css, home-v2.css, screens.css, replace literal accent golds (#FAC775/#BA7517/#412402), neutrals (#1C1A18/#F2EFE9/#6F6E68), and semantic greens/oranges with var(--color-accent / -accent-soft / -accent-ink / -text / -text-muted / -success / -danger). Replace border-radius literals 8/12/18px with var(--radius-sm/md/lg) and box-shadow literals with var(--shadow-sm/md). This makes the existing dark.css overrides automatic for migrated rules and turns the token block into the real source of truth.
- [x] ✅ **בוצע (סבב 1)** · **🟡 בינוני · מאמץ `S` · ★★★ · נוחות** — **Toast 'info' variant is hardcoded white — breaks in dark mode and ignores the token palette**
  - **הבעיה:** showToast's palette object (04-ui-utils.js:415-420) hardcodes the default 'info' toast as bg:#FFF / fg:#1C1A18. In dark mode (the home meta theme-color goes to #1A1816) a pure-white toast slab is a jarring bright flash against a warm-dark UI — the opposite of 'comfortable/beautiful'. The toast is the single most reused confirmation surface in the app (join contest, save name, ad watch, errors), so every confirmation in dark mode looks off-brand. It also bypasses --color-surface / --color-text entirely, so it never inherits the established dark neutrals.
  - 📍 **הוכחה:** `04-ui-utils.js:415-420 palette: info { bg:'#FFF', fg:'#1C1A18' ... }; dark theme bg is #1A1816 (dark.css:10). No html[data-theme] awareness in the inline style built at 04-ui-utils.js:430-436.`
  - 🔧 **לעשות:** In showToast (04-ui-utils.js:412), swap the info palette to token-driven values: bg:'var(--color-surface)', fg:'var(--color-text)', border:'var(--color-border)'. Success/error/warning can keep their saturated brand colors (they read fine on both themes) but also pull from --color-success/--color-danger/--color-warning. Result: the default toast adapts to light/dark automatically and matches the rest of the UI.
- [ ] **🟡 בינוני · מאמץ `S` · ★★★ · נוחות** — **Streak calendar close button is 32px (under the 40px tap target) and not wired into the global ESC/back handler**
  - **הבעיה:** The streak calendar (04-ui-utils.js:686) is a strong loss-aversion surface, but two ergonomics gaps: (1) its close button .streak-cal-close is 32x32px (home-v2.css:2197-2198) — below the 40px tap-target minimum the project's own UX gate Q4 demands, hard to hit with a thumb. (2) Its overlay class is 'streak-cal-overlay' which does NOT contain the substring 'modal-overlay' and is NOT in the curated extras list (04-ui-utils.js:922-936), so the global ESC + browser-back handler can't close it. It survives via its own backdrop-tap + close handler, so it's not stuck — but it's inconsistent with the unified closability system that every other modal uses, and keyboard users can't ESC out of it.
  - 📍 **הוכחה:** `04-ui-utils.js:743-755 builds streak-cal-overlay + streak-cal-close; home-v2.css:2197-2198 width/height 32px; class 'streak-cal-overlay' fails the 'modal-overlay' substring match in __bloomGetCloseableModals (04-ui-utils.js:919) and is absent from the extras list (lines 922-936).`
  - 🔧 **לעשות:** In home-v2.css:2190 bump .streak-cal-close to width/height:40px (font-size:24px) and keep it top-start for RTL. In 04-ui-utils.js __bloomGetCloseableModals, add '.streak-cal-overlay' to the curated extras querySelectorAll list (line 922-936) so ESC + browser-back close it through the unified path. Confirm the close button carries class .modal-close OR add [data-close-modal] so __bloomDismissTopmostModal's selector at line 979 finds it.
- [x] ✅ **בוצע (סבב 1)** · **🟡 בינוני · מאמץ `M` · ★★★ · יופי** — **366 keyframes / 240 infinite animations but only 9 reduced-motion guards — viral.css (the celebration backbone) has ZERO**
  - **הבעיה:** The home and celebration surfaces are animation-dense (the codebase itself flags '156 infinite animations on one page = visual chaos'). Across all 13 CSS files there are 366 @keyframes and 240 'infinite' loops, but only 9 prefers-reduced-motion blocks total — and 10 of 13 files (including viral.css with 13 keyframes / 4 infinite loops of streak-hero / addiction-badge / FTUE animation, plus dark.css, social.css, discovery.css, bottom-nav.css, home.css, tiles-aurora.css) have NONE. Users who set reduced-motion (vestibular sensitivity) still get the full pulsing/glowing barrage, which is both an accessibility miss and a 'comfortable' miss. base.css only guards its 5 shared utility classes, not the hundreds of bespoke animations.
  - 📍 **הוכחה:** `grep: 366 @keyframes, 240 'infinite', 9 prefers-reduced-motion across public/css/*.css. Per-file reduced-motion: base.css 3, boards.css 2, screens.css 2, home-v2.css 1, home-v3.css 1, ALL others incl viral.css = 0. base.css:100-102 only guards .ui-* utility classes.`
  - 🔧 **לעשות:** Add a global reduced-motion fallback at the END of base.css: @media (prefers-reduced-motion: reduce){ *, *::before, *::after { animation-duration:0.01ms!important; animation-iteration-count:1!important; transition-duration:0.01ms!important; } } with targeted opt-outs for genuinely load-bearing transforms. Then add scoped @media blocks in viral.css for the streak-hero / addiction-badge / FTUE keyframes so those high-motion surfaces freeze gracefully. This makes the whole app respect the OS setting in one stroke.
- [ ] **⚪ נמוך · מאמץ `M` · ★★ · התמכרות** — **Theme switcher has no visible affordance on most surfaces and toast/transient banners don't theme-track**
  - **הבעיה:** Theme is a strong comfort/retention lever (a player who finds the dark mode delightful stays longer), but cycleTheme (04-ui-utils.js:198) is only reachable via the mute popover's theme row — there's no discoverable theme toggle on home or in-game, so most players never find it. The 3-state auto/light/dark cycle is also unusual UX (most expect a simple toggle) and the only feedback is a tiny icon swap in a buried menu (syncThemeRow:205). Combined with the un-themed white toast, the dark-mode experience feels half-finished rather than a polished selling point.
  - 📍 **הוכחה:** `04-ui-utils.js:198-214 cycleTheme/syncThemeRow only wired into the mute popover; no theme entry point on home-v2 or in-game header. applyTheme (191-197) correctly swaps data-theme + meta theme-color but nothing surfaces it prominently.`
  - 🔧 **לעשות:** Surface a small theme toggle pill in the home-v2 top bar (or the existing pid/settings row) calling window cycleTheme, with a clear ☀️/🌙/🖥️ icon + label so dark mode is discoverable in <3s. After the toast fix above lands, dark mode becomes a coherent end-to-end experience worth promoting. Optional: collapse the 3-state cycle to a 2-state light/dark toggle with a separate 'auto' checkbox to reduce confusion.

---

## 5. Weekly Recap (BLOOM Wrapped) — ציון `63/100` (C+)

> **הקשר:** Auto-fires Sunday 12:00 IL on home mount — viral share-image surface (src/40-weekly-recap.js, CSS in public/css/home-v2.css:3712-3927)
>
> **מימדים:** התמכרות **6** · יופי **6** · נוחות **5** · בהירות **8** · פוקוס **8** · סגירוּת **4** · רלוונטיות **7**

**פסק דין:** Wrapped מגודר-היטב עם תמונת-שיתוף נושאת-referral, אבל נחתך מ-ESC/back חסר, auto-fire חוטף-כוונה, וסטטיסטיקות-נפח בלבד. **כדי להעלות:** חבר ESC/back, הפוך את ה-auto-fire לבאנר עדין (לא חטיפה), והוסף מסגור week-over-week ('+47% מהשבוע שעבר') כדי שיהיה brag-worthy.

**✅ מה כבר טוב (אל תיגע):**
- Clean single-action card: the recap modal has exactly ONE primary CTA (📸 צור תמונה לשיתוף, line 114) — no competing buttons, strong focus and clarity in <3s.
- Smart auto-fire gating: Sunday-afternoon-only + per-week localStorage dedup + 5-game minimum (lines 34-85) means it never spams and never shows a depressing empty recap to inactive players.
- Share image carries the ?ref=BLOOM-XXXX referral via window.__bloomBuildShareUrl (lines 256-258) so every Wrapped share counts toward referrals + fires a push when an invitee joins — closes the K-factor loop.
- Polished spring entrance (wrCardPop cubic-bezier overshoot, line 3739) + celebration sound/buzz on auto-fire (lines 126-129) make the moment feel like a reward, not a notification.
- Canvas image is built ONLY on button click (line 122), not eagerly — saves CPU/memory on the common 'just glance and close' path.

**🛠️ משימות לביצוע (6):**

- [x] ✅ **בוצע (סבב 1)** · **🟠 גבוה · מאמץ `S` · ★★★★ · סגירוּת** — **ESC / browser-back do NOT close either Wrapped modal**
  - **הבעיה:** The recap overlay class is `wr-overlay` (40-weekly-recap.js:95) and the share-image overlay is `wr-share-overlay` (line 251). The global close handler __bloomGetCloseableModals only matches `[class*="modal-overlay"]` plus a curated extras list (04-ui-utils.js:919-936) — neither wr class matches either path, so ESC and the mobile back-gesture both fail to dismiss these full-screen overlays. Worse, the EXCLUDE table references `wrapped-share-overlay` (04-ui-utils.js:946) which is NOT the real class name (`wr-share-overlay`), so even that dead exclusion is wrong. Closing relies solely on the 32px ✕ and backdrop tap.
  - 📍 **הוכחה:** `src/40-weekly-recap.js:95 (className 'wr-overlay'), :251 ('wr-share-overlay'); src/04-ui-utils.js:919-936 (allowlist), :946 (wrong exclude name)`
  - 🔧 **לעשות:** In src/04-ui-utils.js __bloomGetCloseableModals(), add `.wr-overlay, .wr-share-overlay` to the curated `extras` selector string (line ~922-936) so ESC + history-back route through the unified dismiss. Remove the stale `'wrapped-share-overlay'` key from EXCLUDE (line 946). Verify the topmost-modal logic closes the share overlay before the recap card when both are open. Result: ESC and Android/iOS back-swipe close Wrapped like every other modal.
- [ ] **🟡 בינוני · מאמץ `L` · ★★★★ · התמכרות** — **Grade & stats are volume-only — no growth/personalization (weak vs Spotify Wrapped)**
  - **הבעיה:** The activity grade is computed purely from games-played count (server.js:8297-8301: 100→A+, 50→A, 25→B, 10→C, else D), and the 6 stat cards are flat current-week numbers (games, best score, top tier, trophies, friends, level — lines 106-111). There is no week-over-week comparison ('+38% more than last week'), no superlative/identity hook ('הלוח האהוב עליך', 'הרצף הכי ארוך', 'שעות משחק'), and no surprising/personal data point. Spotify Wrapped's retention power comes from identity ('you're a top-1% listener') and growth framing — both absent. This caps the share-worthiness and the 'I want next week's' pull.
  - 📍 **הוכחה:** `server.js:8296-8314 (grade=games-only, flat stats); src/40-weekly-recap.js:105-112 (6 flat stat cards)`
  - 🔧 **לעשות:** Extend GET /api/weekly-recap (server.js ~8237) to also query last-week's totals and compute deltas, plus a superlative (favorite board by play count, longest streak, total minutes). In 40-weekly-recap.js add a hero comparison line under the grade ('שיחקת 38% יותר מהשבוע שעבר 📈' green / red) and swap one weak stat card for a personal superlative ('🎯 הלוח האהוב: ולנטיין'). Mirror these onto the canvas image. Result: an identity + growth hook that makes the recap brag-worthy and creates 'beat my own number' anticipation.
- [ ] **🟡 בינוני · מאמץ `M` · ★★★ · נוחות** — **Auto-fire interrupts player intent instead of delighting**
  - **הבעיה:** maybeAutoShow is called 2200ms after the home mounts (05a-home-v2.js:542) and immediately throws up a full-screen z-10001 overlay with sound + buzz (40-weekly-recap.js:126-129). A returning player who opens the app on Sunday to tap Play gets their first interaction hijacked — they may already have a thumb moving toward the Play button and instead dismiss a modal. There is no 'requestIdleCallback / wait for first idle' or 'only after they land on home and stop scrolling' guard, and no softer entry (e.g. a dismissable banner that expands on tap) for players who don't want it right now.
  - 📍 **הוכחה:** `src/05a-home-v2.js:541-542 (2200ms setTimeout auto-show); src/40-weekly-recap.js:70-85, 126-129`
  - 🔧 **לעשות:** Soften the auto-fire: in 40-weekly-recap.js maybeAutoShow, instead of immediately calling showRecapModal, first mount a non-blocking gold 'Wrapped מוכן 🌟 — לחץ לצפייה' slide-in banner at the top of home (reuse the friend-notify-banner pattern in home-v2.css). Open the full modal only on tap; auto-dismiss the banner after ~12s (still marked seen). This converts an interrupt into an invitation and protects the Play-first intent. Keep the celebration sound for the modal-open, not the banner.
- [ ] **⚪ נמוך · מאמץ `S` · ★★ · יופי** — **Entrance + infinite brand pulse ignore prefers-reduced-motion**
  - **הבעיה:** wrCardPop (spring scale/translate, home-v2.css:3739) and the infinite wrBrandPulse on '🌟 Wrapped' (line 3773) run unconditionally. The only prefers-reduced-motion block in home-v2.css covers friend-notify-banner (5792-5795) — it does not disable any wr animation. Users who set reduce-motion still get a bouncing card and a forever-breathing title, which the project's own UX/visualPolish standard flags as a defect.
  - 📍 **הוכחה:** `public/css/home-v2.css:3739 (wrCardPop), :3773 (wrBrandPulse infinite), :5792-5795 (reduced-motion only covers friend-notify)`
  - 🔧 **לעשות:** Add to the prefers-reduced-motion block in home-v2.css (~5792): `.wr-card{animation:none}` `.wr-brand{animation:none}` `.wr-overlay,.wr-share-overlay{animation:none}`. Keep opacity:1 final state so the card still appears, just without the spring/pulse. Result: respectful motion for sensitive users, no behavior change for everyone else.
- [ ] **⚪ נמוך · מאמץ `S` · ★★ · נוחות** — **Share-image build has no loading state; close ✕ below 40px tap target**
  - **הבעיה:** Clicking 'צור תמונה לשיתוף' synchronously paints a 720×1280 canvas then calls toBlob async (40-weekly-recap.js:149-242) with zero visual feedback — on a slow phone the button looks frozen for several hundred ms before the share modal appears, inviting a double-tap. Separately, both close buttons are sub-target: .wr-close is 32×32px (home-v2.css:3752) and .wr-share-close is 30×30px (line 3888), under the 40px comfort/accessibility minimum.
  - 📍 **הוכחה:** `src/40-weekly-recap.js:122-124 (onclick→buildAndShareImage, no spinner), :239-242 (toBlob async); public/css/home-v2.css:3752 (32px), :3888 (30px)`
  - 🔧 **לעשות:** In 40-weekly-recap.js onclick handler (line 122), set the button label to 'יוצר תמונה… ⏳' + disable it before calling buildAndShareImage, and re-enable inside showShareImageModal. Bump .wr-close and .wr-share-close to width/height:40px (keep the icon centered) in home-v2.css. Result: clear progress feedback (no double-tap) and comfortable, reachable close targets.
- [ ] **⚪ נמוך · מאמץ `M` · ★★ · יופי** — **Canvas decoration uses Math.random() — can overlap stats/text and is non-deterministic**
  - **הבעיה:** The scattered emoji decoration on the share image is placed via Math.random() across the full 720×1180 area (40-weekly-recap.js:167-169). At 0.12 alpha it's subtle, but random placement can land an emoji directly on the grade number or a stat value, and every generation looks different — undermining a polished, on-brand shareable. A best-in-class Wrapped image is pixel-deterministic and never collides with content.
  - 📍 **הוכחה:** `src/40-weekly-recap.js:164-169 (ctx.globalAlpha=0.12, 14× Math.random() positions over full canvas)`
  - 🔧 **לעשות:** In buildAndShareImage (40-weekly-recap.js ~167), replace random scatter with a fixed decorative layout confined to the top (y<360) and bottom (y>1100) margins only — a deterministic ring or corner cluster of petals — so emoji never overlap the grade tile (380-580) or the stat grid (660-1050). Optionally add a small QR/URL chip in a bottom corner to boost scan-through. Result: consistent, collision-free, more professional share asset.

---

## 6. Game-over / Over-screen — ציון `66/100` (B-)

> **הקשר:** post-game — highest-emotion surface (rendered inside #grid-wrap .overlay by render({over:true}) in src/12-tour-info.js)
>
> **מימדים:** התמכרות **8** · יופי **7** · נוחות **6** · בהירות **6** · פוקוס **4** · סגירוּת **5** · רלוונטיות **8**

**פסק דין:** מסך-סיום ממוקסם-שימור עם כל מנוף F2P ו-choreography טוב — אבל קיר-גלילה של 25+ אלמנטים שקובר את כותרת-הניצחון, עם countdown קפוא (ticker לא-מוגדר) וכותרת-חגיגה מוקטנת ל-13px אפור. **כדי להעלות:** הגדר את `startNextRewardCountdown`, צמצם ל-3-4 בלוקים דומיננטיים, הגדל את הכותרת והניקוד, והוסף כפתור 'בית' מתויג.

**✅ מה כבר טוב (אל תיגע):**
- Dense, well-stacked retention arsenal: rank pill with gold/silver/bronze podium variants (12-tour-info.js:666-687), best-score delta (698-708), board personal-best banner (715-737), gap-to-next-TOP-N tier (740-754), rival overtake line (615-630), streak FOMO escalating by length (897-911), replay-share K-factor button (887-894), and a next-reward return-time hook (920-930) — almost every known F2P over-screen lever is present.
- Real dopamine choreography on the hero number: count-up ease-out-cubic animation on the score (12-tour-info.js:1053-1080), and on isNewBest a confetti + soundMilestone(7) + buzz pop lands ~250ms in (1089-1099), with win-streak milestone celebration a beat later (1104-1115). Correctly skipped for restored/already-played/bot/skin-trial.
- Strong design-token adoption + dark-mode parity: over-again-btn uses --radius-lg / --shadow-md / --shadow-glow / ctaGlow breathing (screens.css:997-1018) and has a full dark override; over-next-reward, over-streak, over-funnel, lb-rank-pill all ship dark variants (dark.css:167-180, screens.css:1317-1322).
- Good relevance gating: continue-ad only when score>5000 and not restored/daily-already-played/challenge (12-tour-info.js:642-643); replay-share only over threshold; funnel only when daily alreadyPlayed; ad-claim collapses to a '✓ received' line once claimed per gameId (658-660).

**🛠️ משימות לביצוע (6):**

- [x] ✅ **בוצע (סבב 1)** · **🟠 גבוה · מאמץ `S` · ★★★★ · התמכרות** — **Next-reward countdown is frozen — startNextRewardCountdown() is never defined**
  - **הבעיה:** The AD.6 'next daily rewards in HH:MM:SS' return-hook (the strongest 'come back at a specific time' lever) renders its value once at game-over and then never ticks. The render code calls startNextRewardCountdown() at game-over, but that function is not defined anywhere in src/ or the built app.js — the call is swallowed by a try/catch, so #onr-countdown displays a static timestamp that never moves. A frozen clock reads as broken and kills the urgency.
  - 📍 **הוכחה:** `src/12-tour-info.js:1338 `try { startNextRewardCountdown(); } catch (e) {}` — but grep across all src/*.js and public/app.js finds only this single occurrence of the identifier (no `function startNextRewardCountdown`). The #onr-countdown span is set once at src/12-tour-info.js:926-927 via formatCountdown(msUntilNextIsraelMidnight()).`
  - 🔧 **לעשות:** Define startNextRewardCountdown() (e.g. in src/06-contests.js next to equipOverlay, or in 12-tour-info.js) that: finds #onr-countdown, sets a setInterval(1000) re-writing el.textContent = formatCountdown(msUntilNextIsraelMidnight()), and self-clears when the element is gone (clearInterval if !document.getElementById('onr-countdown')). Mirror the existing startCountdown() ticker for the daily timer. Result: the gold 🎁 pill counts down live to Israel midnight, restoring the return-time urgency.
- [ ] **🟠 גבוה · מאמץ `L` · ★★★★ · פוקוס** — **Over-screen is a 25+ element scroll wall — no single dominant moment after the score**
  - **הבעיה:** The over template stacks ~25 stacked banners/CTAs in one column: restored banner, title, score, sub, rank pill, claim-name, best-delta, board-best, board-rank, daily-special, streak banner, ach banners (multiple), quest banners (multiple), rank-tier, rival, continue, win-streak meter, again-btn, replay-share, watch-ad, streak-FOMO, countdown/next-reward, funnel, leaderboard, full 8-row tier table, game-stats summary, balance line, share label/card/actions, spectate. Many pulse independently (rival pulses 2s, winstreak pulses, next-reward gradient, replay-share pulses 1.8s). The eye has no anchor — the highest-emotion signal (new best / crown) competes with a dozen secondary banners, violating UX gate Q3. This is exactly the over-polish-everywhere flatness the brief warns about.
  - 📍 **הוכחה:** `src/12-tour-info.js:773-1041 builds the entire wrap.innerHTML as one flat concatenation; pulsing animations at screens.css:987 (rivalPulse), boards.css:7306 (replayShareBtnPulse 1.8s infinite), screens.css ctaGlow on again-btn, plus over-winstreak/over-streak-hot/over-daily-special all animate simultaneously.`
  - 🔧 **לעשות:** In src/12-tour-info.js render({over}), introduce a priority hierarchy: keep ONLY the single highest-emotion banner above the primary 'again' CTA (new-best > crown > streak-milestone > rank podium), and collapse the rest (ach/quest/rival/board-best/rank-tier/stats summary/tier table) into a single tap-to-expand '📋 עוד פרטים' accordion below the share actions. Cap simultaneous infinite animations to one (the chosen hero banner) — give the rest static/entrance-only styling. Result: one clear celebration + one clear CTA, with depth available but not shouting.
- [ ] **🟡 בינוני · מאמץ `S` · ★★★ · סגירוּת** — **No labeled exit-to-home inside the over content — only the buried ⋯ menu**
  - **הבעיה:** The over-screen renders inside #grid-wrap and offers only forward CTAs (play again / mode buttons / funnel). A player who is done and wants to leave to home has no visible ≥40px labeled 'home/exit' control in the over content; the only path is the small ⋯ popover in the top game chrome (added in BN.1 specifically because veterans were stranded) or browser-back. This fails UX gate Q4 (one obvious way to back out) for the 'I'm finished' intent and adds friction for casual exits.
  - 📍 **הוכחה:** `src/12-tour-info.js:773-1041 over template contains 'again', replay, watch-ad, funnel, share, spectate buttons but no home button; exit-to-home only via src/13-boot.js:290+ ⋯ top-menu-popover ('home / achievements / info / reset'). The over .overlay scrolls but has no header bar.`
  - 🔧 **לעשות:** Add a low-emphasis secondary '🏠 חזור הבית' link below the share-actions row in the over template (src/12-tour-info.js, after line 1039), wired to showHome() / __bloomStartMode. Style it as a quiet text-button (≥44px tap height, --color-text-muted) so it doesn't compete with the gold 'again' CTA. Optionally also honor ESC/back on the over-screen by routing to home. Result: a clear, no-hunt exit for finished players without weakening the primary replay pull.
- [x] ✅ **בוצע (סבב 7)** · **🟡 בינוני · מאמץ `M` · ★★★ · יופי** — **Celebration headline is visually demoted — 13px grey uppercase vs the 42px score**
  - **הבעיה:** The emotional headline (e.g. '👑🎉 שיא חדש + כתר!', '🔥 מטורף!') is rendered in .over-title which is styled as a tiny 13px, weight-500, grey (#6F6E68), letter-spaced UPPERCASE label — a treatment meant for a quiet eyebrow, not the hero emotional line. The Hebrew+emoji combined headlines (12-tour-info.js:585-596) deserve to be the loudest text on screen, but they read smaller and fainter than the score and even some banners. This blunts the peak-emotion moment ('יפה' suffers).
  - 📍 **הוכחה:** `src/12-tour-info.js:776 `'<div class="over-title">' + title + '</div>'` with title set at 585-596; styled at base.css:1279 `.over-title { font-size: 13px; font-weight: 500; color: #6F6E68; letter-spacing: 0.08em; text-transform: uppercase; }` and dark.css:47 keeps it grey #8E8A82.`
  - 🔧 **לעשות:** Split the headline from the eyebrow. Keep a small grey .over-title eyebrow for 'סיום משחק' but render the celebratory title (new-best/crown/big-score variants) in a new .over-headline class: font-size ~22-24px, font-weight 800, color var(--color-text) (gradient gold for crown/new-best), no uppercase (uppercase breaks Hebrew anyway and only affects the latin/emoji), with an entrance pop (scale 0.9→1, 0.4s) and prefers-reduced-motion guard. Result: the win headline becomes the dominant text, properly framing the count-up score below it.
- [x] ✅ **בוצע (סבב 7)** · **⚪ נמוך · מאמץ `S` · ★★ · יופי** — **Hero score caps at 42px — the count-up climax underwhelms on a phone**
  - **הבעיה:** The score is the single number the count-up animation builds to, yet .over-score is only 42px / weight-700. On a ~380px-wide phone this is a modest size for the emotional centerpiece, especially when 6-7 digit scores appear. The animation work (TA.4 ease-out count-up) is good but the typographic payoff is small, so the climb feels less satisfying than best-in-class (where the final number bursts large).
  - 📍 **הוכחה:** `public/css/base.css:1280 `.over-score { font-size: 42px; font-weight: 700; ... }`; count-up logic at src/12-tour-info.js:1060-1078 animates this exact element.`
  - 🔧 **לעשות:** Bump .over-score to clamp(44px, 13vw, 60px), font-weight 800, tighter line-height, and on the final tick of the count-up add a one-shot scale-bump (1→1.08→1, 220ms cubic-bezier overshoot) with a prefers-reduced-motion guard. For new-best/crown, tint with the gold accent gradient. Result: the number lands with a visible punch that rewards the climb.
- [ ] **⚪ נמוך · מאמץ `S` · ★★ · יופי** — **Continue-ad button uses a hardcoded green #2E8B6F with inline styles, off the token system**
  - **הבעיה:** The 'watch ad & continue' and pay-to-continue buttons are styled entirely with inline hex colors (#2E8B6F green, #BA7517 gold) rather than the design tokens used by the rest of the over-screen, and there is no dark-mode adjustment — on a dark surface the muted green/gold borders sit slightly off the established palette. This is a small consistency gap ('יפה' parity) on one of the higher-intent surfaces of the screen.
  - 📍 **הוכחה:** `src/12-tour-info.js:648-649 build #continue-ad / #continue-pay with inline `style="background:#2E8B6F;color:#FFF..."` and `border:1px solid #BA7517`; watch-ad-btn similarly inline at 660. No .continue-ad CSS class exists in screens.css (the over-again-btn nearby properly uses --shadow-md/--shadow-glow tokens).`
  - 🔧 **לעשות:** Extract the continue/continue-pay/watch-ad buttons into CSS classes (.over-continue-ad / .over-continue-pay / .over-watch-ad) in screens.css using --color-success / --color-accent / --radius-md tokens, and add html[data-theme=dark] overrides. Remove the inline hex from src/12-tour-info.js. Result: the second-chance and ad surfaces match the token-driven look and read correctly in dark mode.

---

## 7. Duel modal + Live Race (60s) + Spectator widget — ציון `66/100` (B-)

> **הקשר:** duels — friend / random / live (bottom-nav קהילה tab → ⚔️ duel entry)
>
> **מימדים:** התמכרות **8** · יופי **6** · נוחות **6** · בהירות **6** · פוקוס **5** · סגירוּת **5** · רלוונטיות **8**

**פסק דין:** juice של מרוץ-חי ברמה הכי גבוהה ולוח-צפייה באמת חי — אבל ה-overlay של התוצאה שובר את חוזה-הסגירה (אין ESC/backdrop/✕), forfeit של 24px, וחלון 4-CTA בלי פעולה דומיננטית. **כדי להעלות:** הוסף ESC/backdrop/✕ לתוצאה, הגדל את ה-forfeit ל-40px+, הפוך 'מרוץ חי' לפעולה דומיננטית יחידה, הוסף tokens+reduced-motion.

**✅ מה כבר טוב (אל תיגע):**
- Live Race HUD is genuinely best-in-class for retention juice: ahead/behind color tints (lrh-ahead gold / lrh-behind pink, home-v2.css:5057-5063), per-score-bump animation on growth (paintLiveRaceHUD, 02-shop.js:1388-1403), an urgent red pulse + faster-pulsing progress bar in the last 10s (lrh-urgent, home-v2.css:4978/5083), and a 3-2-1-GO countdown with escalating sound+buzz (showLiveRaceCountdown, 02-shop.js:1181). This is the strongest dopamine surface in the game.
- The spectator widget (injectDuelSpectatorWidget/pollDuelLiveState, 02-shop.js:1818-1995) renders the opponent's REAL board live with three distinct cell animations (appear/merge/clear) + a top countdown that turns urgent-red in the final 10s — the highest-emotion 'will they pass me?' moment is fully animated instead of a static spinner.
- Wager UX is mature and non-blocking: preset chips (חינם/50/100/250/500), a live balance line, and inline error text instead of a blocking alert (_wagerAffordable, 02-shop.js:11-20) — never traps the player.
- Friends picker inside the duel modal (02-shop.js:110-221) with avatar + online/played-today status + one-tap suffix injection + search is far better than forcing the player to type a 4-char code; rows are built via DOM API (XSS-safe).
- Robust never-stuck result flow: waiting → polling (pollDuelUntilSettled) → graceful 5-min 'unresolved' exit with a home CTA + admin issue report (02-shop.js:1669-1781), plus cross-session win/loss/tie/declined/expired banners (showDuelNotificationBanner) with score-vs-score and per-kind haptics.
- One-tap rematch on settled human duels with a loud gradient hero CTA that demotes 'play again' to secondary (showDuelResultOverlay, 02-shop.js:2092-2099) — keeps post-match momentum.

**🛠️ משימות לביצוע (7):**

- [x] ✅ **בוצע (סבב 1)** · **🟠 גבוה · מאמץ `S` · ★★★★ · סגירוּת** — **Live Race result overlay has NO ESC / backdrop / ✕ close — only one button**
  - **הבעיה:** showLiveRaceResult builds .live-race-result-overlay (z-index 10020) with a single 'המשך' button whose only close path is an inline onclick='this.closest(...).remove()'. There is no ✕, the backdrop is not clickable (no overlay-level click handler), and the overlay class is NOT in __bloomGetCloseableModals so ESC and the browser-back gesture do nothing. A player who taps outside or presses back is stuck staring at the result with the body still in live-race-active state.
  - 📍 **הוכחה:** `src/02-shop.js:1454-1493 (overlay built, only lrr-btn closes); src/04-ui-utils.js:917-936 (closeable selector lists info-modal/board-lb/etc but NOT live-race-result-overlay); public/css/home-v2.css:5127 (.live-race-result-overlay has no backdrop handler)`
  - 🔧 **לעשות:** In src/02-shop.js showLiveRaceResult, add an overlay-level backdrop click handler (ov.onclick = function(e){ if(e.target===ov) ov.remove(); }) and a 40px ✕ close button top-left of .lrr-card, AND add '.live-race-result-overlay' to the curated extras selector in src/04-ui-utils.js __bloomGetCloseableModals so ESC + history-back dismiss it. Result: three independent ways to close, satisfying UX gate Q4.
- [ ] **🟠 גבוה · מאמץ `S` · ★★★ · נוחות** — **Forfeit ✕ in the live HUD is only 24px — below tap-target minimum on the most time-pressured screen**
  - **הבעיה:** The live-race forfeit button (.lrh-forfeit) is a 24×24px circle in the top-left of the HUD with no expanded hit area, while the parallel async duel-hud-exit is 32px + a 6px invisible buffer. During a frantic 60-second race the player is tapping the grid rapidly; a 24px target for 'quit the race' is both hard to hit on purpose and easy to mis-tap (it does fire a confirm, but a mis-tap mid-flow is jarring). It sits well under the 40px comfort threshold.
  - 📍 **הוכחה:** `public/css/home-v2.css:4984-4989 (.lrh-forfeit width/height:24px, no ::before hit buffer); contrast src/screens.css:1554-1582 where .duel-hud-exit is 32px + inset:-6px hit area`
  - 🔧 **לעשות:** In public/css/home-v2.css, bump .lrh-forfeit to 30px and add a .lrh-forfeit::before { content:''; position:absolute; inset:-8px } invisible hit-area expander (mirror the .duel-hud-exit pattern in screens.css). Keep it visually small so it doesn't compete with the centered timer, but make the touch target ≥40px effective. Result: comfortable, mis-tap-resistant forfeit.
- [ ] **🟡 בינוני · מאמץ `M` · ★★★★ · פוקוס** — **Duel modal is a 4-CTA wall with no single dominant action — focus failure**
  - **הבעיה:** showDuelModal stacks four equally-weighted full-width buttons (⚔️ friend, 🎲 random, ⚡ live, 🎁 gift) plus the 'send challenge' send, the friends picker, difficulty pills, a wager row, and 'my duels' list. Nothing is the dominant action; the eye has no anchor and a new player must parse 'בלי שעון' vs '60 שניות' across three differently-colored buttons to understand the modes. This buries the most addictive lever (the live race) in a column of peers and fails UX gate Q3 (one dominant action).
  - 📍 **הוכחה:** `src/02-shop.js:83-92 (four sequential full-width .btn buttons, each same width, only color differentiates); the friend send button at :83 and random/live at :86/:88 all visually equal weight`
  - 🔧 **לעשות:** Restructure the modal body in src/02-shop.js showDuelModal into a clear hierarchy: a single hero '⚡ מרוץ חי · 60 שניות' card at top (the highest-emotion mode), then a compact 2-up row of secondary modes (🎲 אקראי / ⚔️ חבר), and collapse 🎁 gift + the BLOOM-code typing path behind a 'אפשרויות נוספות' expander. Reserve the loud gradient for ONE primary. Result: the eye lands on the live race first, friction to the dopamine peak drops.
- [ ] **🟡 בינוני · מאמץ `S` · ★★★ · נוחות** — **No prefers-reduced-motion guard on any live-race / spectator animation**
  - **הבעיה:** The live race fires several infinite/continuous animations — lrhUrgentPulse (HUD scale-pulse), lrhBarPulse (progress bar opacity), lrhScoreBump, rmFlashEmoji (infinite emoji bob on the result card), and the spectator's dspecPulse/dspecCountdownPulse. The only prefers-reduced-motion block in home-v2.css (line 5792) covers ONLY friend-notify banners. Motion-sensitive players get a pulsing, scaling, bobbing screen during the highest-intensity 60-second moment with no relief — a real comfort/accessibility gap the project explicitly tracks elsewhere.
  - 📍 **הוכחה:** `public/css/home-v2.css:5792-5795 (reduced-motion block scoped to .friend-notify-banner only); infinite animations at :4979 (lrhUrgentPulse), :5086 (lrhBarPulse), :5157 (rmFlashEmoji on .lrr-emoji), spectator dspecPulse/dspecCountdownPulse inline at src/02-shop.js:1863/1874`
  - 🔧 **לעשות:** Extend the @media (prefers-reduced-motion: reduce) block in public/css/home-v2.css to set animation:none on .live-race-hud.lrh-urgent, .lrh-timer-bar-fill, .lrh-score-bump, .lrr-emoji, and add a reduced-motion override inside injectDuelSpectatorWidget's inline <style> for .dspec-cell-appear/.dspec-cell-merge/.dspec-countdown-urgent. Keep the color/state changes (ahead/behind, urgent red) but kill the scale/opacity pulses. Result: respects reduced-motion, comfortable for sensitive players.
- [ ] **🟡 בינוני · מאמץ `L` · ★★★ · יופי** — **Zero design-token usage — entire duel modal + result overlays are hardcoded hex inline styles**
  - **הבעיה:** The duel modal innerHTML (showDuelModal) contains ~41 hardcoded hex colors and inline styles with no reference to the base.css :root tokens (--color-accent, --radius-md, --shadow-glow, etc.). The duel result overlay (showDuelResultOverlay) is also fully inline-hex. This means the surface can't follow theme changes, has inconsistent radii/shadows vs the tokenized rest of the app, and is the single biggest 'יפה'/consistency liability on this screen. It also has NO dark-mode override in dark.css, so the light .info-card duel modal stays bright-cream on a dark device.
  - 📍 **הוכחה:** `src/02-shop.js:40-99 (41 inline #hex values, 0 var(--...) tokens); dark.css grep for duel/live-race returns nothing — no dark overrides exist`
  - 🔧 **לעשות:** Move the duel modal and result-overlay presentation out of inline strings into tokenized CSS classes in public/css/screens.css using --color-accent/--color-surface/--radius-md/--shadow-md, and add an html[data-theme=dark] override block for the duel .info-card so it reads correctly on dark devices. Keep markup but swap style='#hex' for class names. Result: theme-consistent, dark-mode-correct, easier to restyle.
- [ ] **⚪ נמוך · מאמץ `S` · ★★ · בהירות** — **Optimistic wager deduction can flash a balance the server never agreed to**
  - **הבעיה:** startRandomMatchmaking immediately decrements local playerBalance by the wager and repaints before the first server poll confirms (the live-race path guards with a balance check first, but the random path does NOT). If the server later rejects (insufficient_funds), it refunds via stopRandomMatchmaking('cancelled') + a toast — but for a moment the player sees a balance drop that may be wrong, and the two matchmaking paths behave inconsistently. Minor confusion, not a money bug (server escrow is authoritative).
  - 📍 **הוכחה:** `src/02-shop.js:941-944 (random path deducts with no pre-check) vs :1081-1089 (live path guards 'if playerBalance < wager' before deducting)`
  - 🔧 **לעשות:** In src/02-shop.js startRandomMatchmaking, mirror the live-race guard: before the optimistic deduction, check 'if (_randomMatchWager > 0 && playerBalance < _randomMatchWager) { showToast('💎 אין מספיק יהלומים'); return; }'. Result: consistent behavior across both matchmaking paths, no misleading balance flash.
- [ ] **⚪ נמוך · מאמץ `M` · ★★ · סגירוּת** — **Matchmaking overlays trap the player — no ESC/backdrop, only the cancel button**
  - **הבעיה:** showRandomMatchOverlay and showLiveRaceMatchOverlay create full-screen searching overlays whose only exit is the 'ביטול' button; they are not in the ESC-closeable selector and have no backdrop click. While the cancel button IS visible (so not a hard dead-end), ESC and back-gesture both do nothing, breaking the unified close contract the rest of the app honors, and a player who reflexively presses back stays stuck in the queue.
  - 📍 **הוכחה:** `src/02-shop.js:1024-1056 (showRandomMatchOverlay — only rm-cancel-btn closes) and :1149-1178 (showLiveRaceMatchOverlay — only lrm-cancel-btn); neither class appears in src/04-ui-utils.js:922-936 extras selector`
  - 🔧 **לעשות:** Either add '.random-match-overlay, .live-race-match-overlay' to the curated extras in src/04-ui-utils.js __bloomGetCloseableModals AND route their close through stopRandomMatchmaking('cancelled')/stopLiveRaceMatchmaking('cancelled') (so the wager refund still fires), or wire a backdrop click + ESC keydown inside the two show* functions that call the proper cancel. Result: ESC/back also cancels the search and refunds the stake.

---

## 8. Progression cluster (Trophy Road + Trophy Chests + Album + Lifetime + Leagues) — ציון `66/100` (B-)

> **הקשר:** 🏆 דרגות tab — long-arc meta progression. Home tiles for Trophy Road, Weekly Leagues, Lifetime/Prestige, Album route to the "progress" tab; Trophy Chests routes to "rewards". Each tile opens a full-screen overlay modal with its own claim choreography.
>
> **מימדים:** התמכרות **7** · יופי **6** · נוחות **5** · בהירות **8** · פוקוס **5** · סגירוּת **8** · רלוונטיות **8**

**פסק דין:** אשכול-מטא מלוטש עם hook-שימור חזק (גביעים יורדים) ו-choreography משביע — אבל מדלל פוקוס (4 אריחים בטאב) ומשאיר את הסולם-החברתי של Trophy Road על השולחן. **כדי להעלות:** בחר אריח-גיבור אחד דומיננטי, הוסף 'דירוג מולך' (שחקנים בקרבתך) ל-Trophy Road, החיה את ה-league countdown, החלף confirm().

**✅ מה כבר טוב (אל תיגע):**
- Trophy Road nails the #1 retention lever — loss aversion. Trophies go DOWN on weak games (−8🏆, 34:229) with new-player protection (under 50🏆 no loss), and the modal explicitly warns 'משחק קצר ≤100 = −8 🏆 (זהירות!)' (34:229). The arena-promotion overlay (34:343) with radial gradient + soundMilestone(7) + escalating buzz is a genuine dopamine peak.
- Trophy Chests is a textbook must-return loop: timed unlock (38:184), one-chest-at-a-time scarcity choice, live countdown ticker (38:325), and a rarity-themed full-screen celebration with confetti scaled per rarity (40/24/12 particles, 38:302) + sound + buzz (38:318). The earn→banner→tap-to-find flow (38:407) closes the 'won a chest but where?' gap well.
- Claim choreography is consistent and satisfying across all five systems — every claim does balance bump via __bloomBumpBal, soundMilestone(5-7), and a tactile buzz pattern. League/Lifetime promotions get full-screen celebrations with auto-dismiss.
- Strong relevance gating: level gates L10 (trophy/lifetime), L15 (album), L20 (league); chest tile only mounts when chests exist (38:61); album hidden when totalCells===0 (25:73). New players aren't drowned in advanced meta.
- Closability is solid — every modal has a × button, backdrop-tap close, and ESC support via the unified __bloomGetCloseableModals handler (aria-label='סגור' matches the fallback selector at 04-ui-utils.js:979). Celebrations correctly dismiss on tap/ESC/timeout.

**🛠️ משימות לביצוע (7):**

- [ ] **🟠 גבוה · מאמץ `L` · ★★★★★ · התמכרות** — **Trophy Road has no social/ladder comparison — half the Clash Royale hook is missing**
  - **הבעיה:** Clash Royale's Trophy Road is addictive because trophies are the RANKED LADDER — you constantly see 'players near you' and fear being overtaken. BLOOM's trophy modal shows only your own trophies, your own arena progress, and your own milestone gems (34:145-232). There is zero 'who else is in your arena', no rank, no rival-near-you pressure. The loss-aversion is real but purely self-referential, which caps the compulsion.
  - 📍 **הוכחה:** `renderTrophyBody (34:145) renders header + next-arena + 3 personal stats + milestones + arena ladder + tips — none reference other players. No /api/trophies/leaderboard call anywhere in 34-trophy-road.js.`
  - 🔧 **לעשות:** Add a 'מי באזור שלך' section to renderTrophyBody (34-trophy-road.js) backed by a new /api/trophies/nearby endpoint returning the 2 players just above and 2 below the device in trophy count (same arena). Show name + trophy count + a gold '⚔️ עוד N🏆 לעקוף את X' pill for the nearest-above player (mirror the dyn-boards next-target pill pattern). This converts a solo number into competitive pressure.
- [ ] **🟠 גבוה · מאמץ `M` · ★★★★ · פוקוס** — **Four meta-progression tiles stack in one tab — focus diluted**
  - **הבעיה:** The דרגות/progress tab simultaneously hosts trophy-home-tile, league-home-tile, lifetime-home-tile, and album-home-tile (confirmed routed to 'progress' in src/46-bottom-nav.js:65-69). All four are full-width tiles with their own progress bars, claim badges, and (for trophy) an embedded 8-node arena strip. When several have unclaimed rewards, multiple gold 🎁 badges and 'has-claim/has-reward/can-prestige' pulse classes fire at once, so no single highest-emotion signal dominates — exactly the overload the project's Priority Calmer was built to prevent.
  - 📍 **הוכחה:** `src/46-bottom-nav.js:65-69 routes all four to 'progress'; trophy tile embeds renderTrophyStrip (34:84) inside the tile body; each tile adds its own pulse class (34:45 has-claim, 30:105 has-reward, 26:72 can-prestige).`
  - 🔧 **לעשות:** In the progress tab, apply the existing Priority Calmer pattern (src/80-polish.js): rank these four by urgency (trophy milestone-claimable > league reward unclaimed > prestige-ready > album-claimable), give ONLY the top one the .polish-spotlight halo and add .polish-calmed (desaturate + pause animation + dim) to the other three. Result: the eye lands on the single most rewarding action.
- [ ] **🟡 בינוני · מאמץ `M` · ★★★★ · יופי** — **Lifetime prestige uses a native confirm() — breaks the polished aesthetic and RTL**
  - **הבעיה:** The single most ceremonial action in the longest meta-arc (Prestige = restart from L1 for a permanent ⭐) is gated by a raw browser confirm() dialog (26:184). It renders in the OS chrome, ignores the app's gold/purple theme, is left-aligned LTR on most browsers (mangling the Hebrew bullet list), and feels like a 1998 web prompt right before a confetti celebration. It's the jarring exception in an otherwise custom-styled cluster.
  - 📍 **הוכחה:** `src/26-lifetime.js:178-184 confirmAndPrestige builds a multi-line string and calls if (!confirm(msg)) return;`
  - 🔧 **לעשות:** Replace the confirm() in confirmAndPrestige (26-lifetime.js) with a custom themed confirmation overlay (reuse the lifetime-modal-card visual language: purple-gold gradient, ⭐ icon, the 3 bullet consequences, a primary 'בצע פרסטיג' button and a secondary 'ביטול'). Use design tokens (--radius-lg, --shadow-glow) and direction:rtl so the bullets align right. This makes the highest-stakes decision feel premium.
- [ ] **🟡 בינוני · מאמץ `S` · ★★★ · התמכרות** — **League weekly-reset countdown is static and goes stale**
  - **הבעיה:** The league tile and modal show '⏳ השבוע מתאפס בעוד 2י 5ש' computed once via msUntilWeeklyReset() at render time (30:88, 30:154), but there is NO setInterval ticking it. The deadline — the single strongest FOMO lever in a weekly league — freezes the moment the tile mounts and silently decays. A player who leaves home open sees a wrong, stale countdown, weakening the 'play before reset' urgency.
  - 📍 **הוכחה:** `grep for setInterval in src/30-leagues.js returns nothing; msUntilWeeklyReset (30:51) is only called inside tileInner (30:88) and renderLeagueBody (30:154), both one-shot renders.`
  - 🔧 **לעשות:** In 30-leagues.js add a startLeagueResetTicker() that updates the .league-tile-reset and .league-current-reset text every 60s while the tile/modal is mounted (mirror the chest startBannerTicker at 38:325), and tear it down on home unmount. In the final hours switch to a red urgent style + 1s cadence so 'נשארו 3 שעות' pulses.
- [ ] **🟡 בינוני · מאמץ `S` · ★★★ · נוחות** — **Modal close buttons are 32-36px — below the comfortable tap target**
  - **הבעיה:** Every progression modal's close button is undersized: chests/album/lifetime/league are 32×32px (home-v2.css:3134, boards.css:6777/7109/8225) and trophy is 36×36px (home-v2.css:1739), all below the 44px iOS / 40px recommended minimum. The hit target is a small '×' glyph in a translucent circle pinned to the top-left corner. On a phone held one-handed this is a fiddly, mis-tappable target — friction on the most-used control of every modal.
  - 📍 **הוכחה:** `home-v2.css:3134 (.chests-modal-close 32×32), boards.css:6777/7109/8225 (album/lifetime/league 32×32), home-v2.css:1739 (.trophy-modal-close 36×36).`
  - 🔧 **לעשות:** Bump all five close buttons to min 44×44px (width/height:44px) and increase the × font-size to ~26px while keeping the visual circle ~36px via an inner element or padding (so it doesn't look bulky but the tappable area is generous). Backdrop-tap and ESC already exist as backups, but the explicit close should be effortless. Centralize into one shared .meta-modal-close class to avoid five divergent definitions.
- [ ] **🟡 בינוני · מאמץ `S` · ★★★ · יופי** — **No prefers-reduced-motion coverage for any progression animation**
  - **הבעיה:** The cluster is animation-heavy — trophy-strip current-node pulse, arena-promotion scale-in, chest emoji shake/bounce, chest-open confetti, league/lifetime promotion celebrations, the can-prestige tile pulse — yet the only prefers-reduced-motion rules in the whole stylesheet target .ds-xp-chip and .gacha-card-value-* (boards.css:3029, 5215). The owner's 'comfortable' goal and basic accessibility require these to calm down for motion-sensitive users; right now confetti and pulsing fire at full intensity regardless of the OS setting.
  - 📍 **הוכחה:** `home-v2.css prefers-reduced-motion block (5792) only covers .friend-notify-banner; boards.css blocks (3029, 5215) only cover ds-xp-chip and gacha-card-value; no trophy/chest/album/lifetime/league animation is gated.`
  - 🔧 **לעשות:** Add a @media (prefers-reduced-motion: reduce) block covering this cluster: set animation:none on .trophy-strip-node.tr-current, .trophy-promo-card, .chest-card-emoji-shake/-bounce, .chest-conf, .league-promo-card, .lifetime-celeb-stars, and the .has-claim/.has-reward/.can-prestige tile pulses; replace celebration confetti with a static reveal. Also gate the JS confetti loop in showChestOpenCelebration (38:302) behind a reduced-motion check.
- [ ] **⚪ נמוך · מאמץ `M` · ★★★ · התמכרות** — **Album & Lifetime are pure unbounded grind with no deadline or near-term hook**
  - **הבעיה:** Album completion and Lifetime XP both visualize progress well but offer no time pressure, no 'next thing happens at X', no comeback hook. Lifetime is explicitly a months-long grind to L100 (26:3) with the only payoff a one-time prestige at the very end; the modal lists 8 passive XP sources (26:135-143) but gives the player no immediate reason to act TODAY. Album similarly only rewards on full board/tier completion (25:193,214) with no 'you're 1 cell away' urgency surfaced. These are retention floors, not drivers.
  - 📍 **הוכחה:** `26-lifetime.js renders sources + a prestige card that is only actionable at L100 (26:146); 25-album.js claim buttons appear only on canClaim (25:193/214), with no 'almost there' nudge for boards at 7/8.`
  - 🔧 **לעשות:** In album tileInner (25:84) and renderAlbumBody, surface the closest-to-complete board as a '🔥 עוד תא אחד ללוח X — שחק עכשיו!' CTA that deep-links into that board (the strongest completionist nudge). For lifetime, add a 'הדרגה הבאה בעוד N XP — משחק אחד ≈ +10' line to tileInner (26:39) so each level feels reachable in one session, converting a distant grind into a per-session goal.

---

## 9. Onboarding / FTUE / first-run routing / push prompt — ציון `66/100` (C+)

> **הקשר:** First session — the make-or-break first 30 seconds (3-step scripted demo tutorial, boot routing, soft push pre-prompt, name/country prompts deferred to leaderboard submit)
>
> **מימדים:** התמכרות **5** · יופי **7** · נוחות **6** · בהירות **7** · פוקוס **8** · סגירוּת **8** · רלוונטיות **8**

**פסק דין:** חזק טכנית ומגודר נכון, אבל 30 השניות הראשונות נפתחות עם מטלה במקום תשוקה ומסתיימות בזריקה פסיבית לבית. **כדי להעלות:** הוסף רגע value-prop/welcome לפני הדמו (תצוגת Crown/Diamond + הבטחה), סיים עם CTA 'בוא נשחק' מפורש, קצר את זמני-ההמתנה הכפויים, ושקול לתת לשחקן בחירה אמיתית בלוח האמיתי.

**✅ מה כבר טוב (אל תיגע):**
- FTUE is correctly gated to true first-timers only — boot.js:402 requires both ftueShouldRun() AND !hasHistory, and ftueShouldRun() skips bot/spectator/watch URL contexts (15-ftue.js:29-36). Veterans never see it (relevance is strong).
- Fail-soft engineering is genuinely good: buildOverlay() detects a missing grid and tears down + hands off to showHome() so a broken tutorial never strands a first-timer (15-ftue.js:139-148), and renderFtueGrid guards Array.isArray + try/catch (15-ftue.js:189-214).
- Push permission uses the correct soft pre-prompt pattern fired ONLY on high-intent social moments (duel sent, gift, first achievement, streak danger) — never cold at boot. Respects browser denied/granted state and a 24h cooldown (16-push.js:133-170). This is best-practice and protects grant rate.
- Name/country prompts are deferred entirely out of onboarding — the player reaches the board with zero forms (defaultPlayerName placeholder), and prompts only appear at leaderboard-submit, which is the right friction-minimizing tradeoff for D1.
- FTUE visuals reuse design tokens (--color-accent, --radius-lg, --shadow-md, --shadow-glow) with full dark-theme parity and RTL direction set on the overlay; step-dots, arrow bounce, WOW pop all have proper overshoot easing (viral.css:381-573).

**🛠️ משימות לביצוע (7):**

- [x] ✅ **בוצע (סבב 5)** · **🟠 גבוה · מאמץ `M` · ★★★★★ · התמכרות** — **No value-prop / welcome moment before the tutorial — straight into a chore**
  - **הבעיה:** The very first thing a brand-new player sees is an instruction bubble 'הקש על העמודה כדי להפיל את האבן' on an empty grid (15-ftue.js:57). There is no hook: no 'this is the addictive merge game', no aspirational Crown/Diamond preview, no emotional promise of what they're about to feel. The first 30s — explicitly the make-or-break window — open with homework, not desire. Top F2P (Royal Match, Gardenscapes) open with a juicy near-win or a goal image. BLOOM opens cold.
  - 📍 **הוכחה:** `15-ftue.js:52-83 FTUE_STEPS — step 0 is bare 'tap the column'; buildOverlay() header is only a static '🌸 ברוכים הבאים ל-BLOOM' (15-ftue.js:121) with no value prop, no animated goal, no reward tease.`
  - 🔧 **לעשות:** In src/15-ftue.js buildOverlay(), add a 1.5-2s opening beat BEFORE renderStep(0): a centered hero card showing the 8-tier ladder culminating in 👑 Crown with text like 'מזגו אבנים → הגיעו לכתר → תפסו את הראש בטבלה'. Animate the Crown with a --shadow-glow pulse. Make it tap-to-continue. Goal: the player feels desire (status + progression) within 2 seconds, before the mechanic lesson.
- [x] ✅ **בוצע (סבב 5)** · **🟠 גבוה · מאמץ `M` · ★★★★★ · התמכרות** — **FTUE ends with NO commitment CTA — silently dumps player on home**
  - **הבעיה:** After step 3's chain celebration, advanceAfterCheer() auto-fires finishFTUE(true) → showHome() after 1300ms with zero player action (15-ftue.js:394-403, 406-427). The code comment at 15-ftue.js:8 explicitly promises a 'מוכן? בוא נשחק' handoff that does not exist. The single highest-intent moment in the player's life — they just learned the loop and felt the chain dopamine — is wasted on a passive transition to a 25-tile home wall instead of a big 'בוא נשחק עכשיו!' button that drops them into a real game. This is the textbook first-session conversion lever, and it's missing.
  - 📍 **הוכחה:** `15-ftue.js:394-403 advanceAfterCheer auto-advances with setTimeout; finishFTUE(true) at line 399 calls onDone (=showHome from boot.js:410) with no intermediate CTA. The promised handoff in the file's own header comment (line 8) is unimplemented.`
  - 🔧 **לעשות:** In src/15-ftue.js, after the 3rd step's cheer, replace the auto-finishFTUE with a 4th 'graduation' beat: a full-width gold CTA button '🎮 בוא נשחק עכשיו' (≥56px, --radius-lg, --shadow-glow, breathing pulse) plus a smaller 'דלג לבית'. The CTA should call onDone but ALSO set a flag so boot/home immediately starts a real practice game (init('practice',{fresh:true})) instead of landing on the tile-wall home. Channel the just-earned dopamine straight into first real play.
- [ ] **🟡 בינוני · מאמץ `L` · ★★★★ · התמכרות** — **Scripted demo means the player never makes a real choice or feels real agency**
  - **הבעיה:** Every step accepts exactly ONE column (15-ftue.js:251-256: any other column just nudges the arrow and does nothing). The board, the next piece, and the outcome are 100% predetermined. The player is clicking a 'next' button disguised as a board — they never experience the real engine's randomness, the variable-reward core that makes the game addictive. The demo-vs-real-engine tradeoff (cheap + safe) was chosen, but it costs the most important thing: a real near-miss or a self-discovered chain in the first session, which is what converts. The muscle-memory claim (15-ftue.js:18-19) is weak when there's only one legal move per beat.
  - 📍 **הוכחה:** `15-ftue.js:248-260 onCellTap rejects col !== step.col with no consequence; FTUE_STEPS pre-states (lines 54,64,76) are fixed; outcomes (after.mergedTier, chain) are hardcoded, not engine-computed.`
  - 🔧 **לעשות:** Lowest-risk upgrade: after the 3 scripted beats, hand into ONE real seeded engine drop on the actual board (a deterministic seed guaranteed to allow an easy merge) with a coach-mark, before showHome. If full engine integration is too risky, at minimum let step 3 accept a chain in 2 different columns so the player makes a genuine choice once. Goal: at least one moment of real agency + real engine feedback in session one.
- [ ] **🟡 בינוני · מאמץ `S` · ★★★ · יופי** — **Push pre-prompt modal is hand-rolled with inline styles — bypasses design tokens + reduced-motion**
  - **הבעיה:** showPushPrePrompt() builds the entire modal with hardcoded hex colors (#FAC775, #BA7517, #1C1A18, #6F6E68) and inline cssText (16-push.js:90-113) instead of the design-token system every other surface uses. It has NO dark-theme variant (white card on a dark app), NO prefers-reduced-motion guard on its fadeIn/opacity animations, and the close ✕ is only available via backdrop tap / the 'אחר כך' text link (no dedicated ≥40px close glyph). On a dark-mode device this flashes a glaring white card; the visual inconsistency reads as 'a different app', violating the 'יפה' goal.
  - 📍 **הוכחה:** `16-push.js:90-113 inline cssText with literal hexes; no html[data-theme="dark"] override exists for #push-pre-prompt; animation:fadeIn 0.25s at line 93 with no reduced-motion fallback.`
  - 🔧 **לעשות:** Move the push pre-prompt markup to a CSS class in viral.css (e.g. .push-pre-prompt-card) using --color-surface/--color-accent/--radius-lg/--shadow-glow, add a html[data-theme="dark"] override, wrap entrance animations in @media (prefers-reduced-motion: reduce){animation:none}, and add a visible ≥40px ✕ close button to match the rest of the modal system. Result: theme-consistent, accessible push prompt.
- [x] ✅ **בוצע (סבב 5)** · **🟡 בינוני · מאמץ `S` · ★★★ · נוחות** — **Each FTUE beat has a long forced wait (~1.7-2.6s of locked dead-time per step)**
  - **הבעיה:** Between the player's tap and being able to act again, the step locks (15-ftue.js:257) through a chain of setTimeouts: drop 380ms + merge 220ms + animateMergeAt 260ms + (chain) animateChainHop 280ms + celebrate WOW 1700ms display + advanceAfterCheer 1300ms before the next step renders. On the chain step that's ~3s of staring at a banner before the next beat. Impatient new players (the highest-churn cohort) feel held hostage. There's no way to tap-to-skip the celebration and move on.
  - 📍 **הוכחה:** `15-ftue.js:317 (380ms), :316 (220ms), :341 (260ms), :363 (280ms), :383 (1700ms wow), :403 (1300ms advance) — all fixed, none skippable; ftueState.locked stays true throughout.`
  - 🔧 **לעשות:** In src/15-ftue.js, make the celebration tap-to-advance: while a cheer is showing, a tap anywhere on the overlay should immediately clear pending timeouts and call renderStep(next)/finishFTUE. Also shorten advanceAfterCheer from 1300ms to ~700ms. Keeps the dopamine beat for those who watch it, but never traps the impatient. Goal: player-controlled pacing, faster path to home.
- [ ] **⚪ נמוך · מאמץ `S` · ★★ · סגירוּת** — **FTUE overlay isn't wired into the global ESC/back close handler**
  - **הבעיה:** The only escape from the FTUE is the small 'דלג על המדריך' text link at the very bottom (15-ftue.js:131, viral.css:562 — 12px font, transparent). There is no ESC-key handler, no browser-back handler, and the skip target is a tiny low-contrast text button far below the board — below thumb-reach on a tall phone and easy to miss. Per the project's own UX gate Q4, every surface needs ONE obvious close; a 12px transparent text link at the bottom of a full-screen takeover is borderline.
  - 📍 **הוכחה:** `15-ftue.js:149-153 wires only #ftue-skip.onclick; no keydown/popstate listener exists in 15-ftue.js; viral.css:562-573 .ftue-skip is 12px transparent. The FTUE overlay is also not added to __bloomGetCloseableModals() per the project's TC.1 convention.`
  - 🔧 **לעשות:** In src/15-ftue.js startFTUE, add a document keydown listener for 'Escape' that calls finishFTUE(false), removed in finishFTUE. Promote the skip affordance: make 'דלג על המדריך' a visible ≥40px pill (subtle border, --color-text-muted) placed top-corner within thumb reach, not 12px transparent text at the bottom. Ensures the make-or-break screen is never a trap.
- [ ] **⚪ נמוך · מאמץ `M` · ★★ · נוחות** — **Country picker is a 55-button flag wall that can fire right after first game — heavy for a casual moment**
  - **הבעיה:** On first leaderboard submit, promptForName chains promptForCountry (11-game.js:1700-1705), which renders a scrolling 4-column grid of ~55 flag buttons (11-game.js:1584-1601). Immediately after a brand-new player's first game-over — a high-emotion moment — they're hit with a dense country grid. While it's skippable and searchable, two stacked modals (name then flags) of forms at the player's first peak is friction that can cost the share/leaderboard conversion.
  - 📍 **הוכחה:** `11-game.js:1584-1601 builds COUNTRY_LIST.map into ~55 buttons in a max-height:340px scroller; chained automatically from promptForName via maybeChainCountry (11-game.js:1700-1705) on the first name-pick.`
  - 🔧 **לעשות:** Soften the chain: after promptForName, do NOT auto-open the full flag grid. Instead default the flag to a geo/locale guess (navigator.language → country) and surface a tiny '🇮🇱 שנה דגל' chip in the leaderboard modal for the rare player who wants to change it (the affordance already exists per CLAUDE.md). Removes a second forced modal at the player's first emotional peak.

---

## 10. Player Profile (/player/BLOOM-XXXX) + Welcome (/welcome) + Privacy (/privacy) — ציון `66/100` (C+)

> **הקשר:** external/server-rendered + legal
>
> **מימדים:** התמכרות **5** · יופי **7** · נוחות **7** · בהירות **8** · פוקוס **8** · סגירוּת **6** · רלוונטיות **7**

**פסק דין:** זוג משטחים server-rendered מלוטשים עם SEO חזק וכרטיס-פרופיל נקי — אבל הפרופיל נכשל כנשק ויראלי (OG גנרי, בלי אתגר/FOMO, מבוי-סתום, ארנק-💎 דלוף). **כדי להעלות:** צור OG/תמונה דינמית per-player (שם+שיא+רמה), הוסף 'תוכל לעבור 248,500?' + דירוג + כפתור-אתגר, הסר את חשיפת ה-💎, וחבר חזרה ל-/welcome.

**✅ מה כבר טוב (אל תיגע):**
- The /welcome landing is genuinely well-crafted: dark-gold brand hero with animated floating tier icons (server.js:269-273), live social-proof stat pills gated to only render when totalPlayers>0 (server.js:311-315), a clean 6-feature 'why is it addictive' grid, and a WhatsApp share CTA — strong SEO (JSON-LD VideoGame, canonical, OG, keywords) plus emotional framing in Hebrew.
- The /player profile card is polished and self-contained: popIn overshoot animation (cubic-bezier 0.34,1.56,0.64,1), auto light/dark theme via prefers-color-scheme (server.js:15423), gradient avatar from name initial, animated XP bar, a 3+4 stat-card grid, and a prominent dark/gold 'play also' CTA carrying ?ref= so every shared profile is an attributed K-factor funnel.
- Display name is XSS-sanitized via regex strip of <>"'& (server.js:15364) and the 404 fallback for an unknown code is a graceful branded page with a play link rather than a raw error.
- Privacy page is clean, honest, RTL-correct, legally complete (Israeli law + GDPR, right-to-erasure with the exact localStorage key, 90-day PII auto-purge disclosure) and has a clear 'back to game' button.

**🛠️ משימות לביצוע (7):**

- [x] ✅ **בוצע (סבב 4)** · **🟠 גבוה · מאמץ `M` · ★★★★★ · התמכרות** — **Profile has zero competitive/FOMO hook — it brags but never invites a challenge**
  - **הבעיה:** The profile is a static résumé (best, games, credits, days, contests, earned, referrals). It never says 'can you beat 248,500?' as an action, never shows global RANK, never surfaces today's daily challenge, and never offers a duel. The shareText (server.js:15366) ends with 'תצליח לנצח?' but the page body itself has no challenge framing — the strongest viral merge-game pattern (Royal Match / Wordle 'beat my score') is left on the table. A viewer cannot directly race this player.
  - 📍 **הוכחה:** `server.js:15432-15442 stat grid is purely retrospective; server.js:15445 CTA is bland 'שחק גם ב-BLOOM' with no score reference; no rank, no daily-challenge link, no duel CTA.`
  - 🔧 **לעשות:** In the profile render (server.js:15425-15445), (1) make the best-score stat the visual hero (bigger, with a 'הכי גבוה' crown) and add a one-line challenge banner under it: 'תעבור את {best}?' as the headline above the play button; (2) change the primary CTA copy from 'שחק גם ב-BLOOM' to 'נסה לעבור את {name} 🔥' so the click is framed as a race; (3) if feasible, fetch the player's global daily rank and show a '#N בעולם' pill in the level-badge row. This converts a résumé into an addiction invitation.
- [ ] **🟠 גבוה · מאמץ `L` · ★★★★★ · התמכרות** — **Profile OG/Twitter card is generic — a shared profile shows the same static image for everyone, killing the viral hook**
  - **הבעיה:** The whole point of a shareable profile is that a friend sees THIS player's brag (level, best score, name) in the link preview and wants to beat them. But og:image is the static site-wide social-share.png (server.js:15376) and twitter:card is 'summary' (small thumbnail, server.js:15377), not 'summary_large_image'. So a WhatsApp/Twitter unfurl of /player/BLOOM-XXXX shows a generic BLOOM banner with no name, no level, no score — the personalization that would make a non-player click is invisible in the preview where it matters most.
  - 📍 **הוכחה:** `server.js:15376 og:image=...social-share.png (static); server.js:15377 twitter:card=summary; the rich per-player data (name, lvl.title, stats.best) is only in og:title/description text, not in the image.`
  - 🔧 **לעשות:** In the /player/:code route (server.js:15368-15379), switch twitter:card to 'summary_large_image' and generate a per-player OG image. Cheapest path: add an /api/og/player/:code endpoint that server-renders a 1200x630 PNG (via node-canvas or an SVG-to-PNG) showing avatar+name+level title+best score on the dark-gold brand background, then point og:image and twitter:image at it with a cache header. The preview should read '{name} · רמה 12 · שיא 248,500' so a non-player sees a real human's brag, not a logo.
- [x] ✅ **בוצע (סבב 4)** · **🟠 גבוה · מאמץ `S` · ★★★★ · נוחות** — **Profile page has NO way back into the game shell or to your own profile — it is a viral dead-end for the viewer**
  - **הבעיה:** The profile renders ONLY a 'play also' CTA to /?ref= and two share buttons (server.js:15444-15449). There is no link to /welcome (the conversion-optimized landing), no nav, no 'see the leaderboard', and no close/back chrome at all. A curious non-player who lands here gets one binary choice; an existing player who taps their own profile from home has no obvious 'back' (relies on browser back only). For the highest-emotion identity surface, the funnel is a single dead-end with no secondary path (e.g. 'see today's daily challenge' or 'view leaderboard').
  - 📍 **הוכחה:** `server.js:15444-15450 — btns block contains only btn-play (href=/?ref=) + two share buttons; no back link, no /welcome link, no leaderboard link anywhere on the page.`
  - 🔧 **לעשות:** In the profile btns block (server.js:15444), add a thin secondary row of two text links under the share buttons: 'איך משחקים?' → /welcome and 'לוח המובילים' → / (or a future /leaderboard deep-link). Keep btn-play dominant. This gives the viewer a soft secondary path and gives an existing player an explicit exit, turning the dead-end into a 2-branch funnel.
- [x] ✅ **בוצע (סבב 4)** · **🟡 בינוני · מאמץ `S` · ★★★ · רלוונטיות** — **Credits/💎 prominently displayed on a PUBLIC profile — wrong signal, leaks economy, weak brag**
  - **הבעיה:** The public profile shows balance 'קרדיטים' and total_earned '💎 הרוויח' as two of seven stats (server.js:15435, 15440). On a viral landing surface aimed at non-players, a gem balance is meaningless noise (a stranger has no context for '💎') and it publicly exposes the player's wallet. It dilutes the two stats that actually create envy/competition — best score and level. UX gate Q3 (focus) and Q5 (relevance) both fail here.
  - 📍 **הוכחה:** `server.js:15435 stat-val ${player.balance|0} 💎 lbl קרדיטים; server.js:15440 stat-val ${total_earned} lbl 💎 הרוויח — two of seven stat cells are economy figures on a public share page.`
  - 🔧 **לעשות:** In the profile stat grids (server.js:15432-15442), drop the raw balance/credits cell entirely and replace total_earned with a more brag-worthy public stat (e.g. highest tier reached as an emoji 👑, or current streak). Reserve gem balance for the in-app private profile only. Re-weight the grid so 🏆 שיא and רמה dominate. This sharpens the envy signal and stops leaking the wallet.
- [ ] **🟡 בינוני · מאמץ `M` · ★★★ · יופי** — **Welcome and Profile are fully detached from the app's design-token system — divergent palettes, no shared CSS, double maintenance**
  - **הבעיה:** Both pages hardcode their own color values and re-declare a private :root in /welcome (server.js:264) instead of consuming base.css design tokens (--color-accent, --radius-md, --shadow-glow, etc. per CLAUDE.md §UX). The profile card uses #BA7517/#FAC775/#252320 literals (server.js:15390-15406), privacy.html uses yet another set (#BA7517/#F7F5F0, privacy.html:11-18). Result: three different gold shades and radii across surfaces, and any brand refresh requires editing 3 inline style blocks. Visual consistency ('יפה' directive) is only skin-deep.
  - 📍 **הוכחה:** `server.js:264 /welcome redeclares --gold:#FAC775 etc inline; server.js:15385-15406 profile uses literal hex with no tokens; privacy.html:11-18 third independent palette. None reference base.css tokens.`
  - 🔧 **לעשות:** Extract a tiny shared server-rendered CSS partial (or a /assets/landing.css served statically) defining the brand tokens once, and have /welcome, /player, and privacy.html link it. At minimum unify the gold (--gold:#FAC775 / --gold-dark:#BA7517), border-radius (16px cards / 14px buttons), and shadow values across all three so the gold, corner radius, and glow match base.css exactly. Cuts maintenance to one place and guarantees brand consistency across the viral surfaces.
- [x] ✅ **בוצע (סבב 4)** · **⚪ נמוך · מאמץ `S` · ★★ · יופי** — **No prefers-reduced-motion guard on the infinite animations in /welcome and the profile popIn**
  - **הבעיה:** The welcome hero runs perpetual heroGlow (server.js:268) and float (server.js:272) infinite keyframe animations, and the profile card animates popIn on every load (server.js:15388), with no @media (prefers-reduced-motion: reduce) override anywhere. The in-app CSS is careful about this (CLAUDE.md repeatedly notes reduced-motion guards), so these server-rendered surfaces are inconsistent and can cause discomfort for motion-sensitive users on the first-impression page.
  - 📍 **הוכחה:** `server.js:267-272 heroGlow + float are 'infinite' with no reduced-motion guard; server.js:15388 popIn unconditional; no @media prefers-reduced-motion in either inline <style>.`
  - 🔧 **לעשות:** Add a single @media (prefers-reduced-motion: reduce){ *{animation:none!important} } block to the inline <style> of both /welcome (after server.js:295) and /player (after server.js:15421), plus privacy has none needed. Keeps the static layout intact while respecting the OS accessibility setting on the most-viewed first-impression pages.
- [ ] **⚪ נמוך · מאמץ `S` · ★★ · יופי** — **Privacy page is visually thin and slightly off-brand vs the dark-gold app identity**
  - **הבעיה:** privacy.html (privacy.html:10-19) is a plain light document with a single gold heading and a serif-ish system stack — it doesn't carry the BLOOM brand mark, has no logo, and uses a lighter palette than the rest of the funnel. It is functional and legally complete but feels like a different product, slightly undermining trust at the exact moment a careful user is reading the privacy policy.
  - 📍 **הוכחה:** `privacy.html:11-19 minimal inline CSS, no brand header/logo, light #F7F5F0 background diverging from the dark-gold profile/welcome identity.`
  - 🔧 **לעשות:** Add a small BLOOM brand header to privacy.html (the 🌸 mark + 'BLOOM' wordmark in --gold, linking to /), and align its accent gold + back-button radius to the shared tokens from the consolidation task. Optionally darken to match the app theme. Low effort, raises trust and brand cohesion on the legal surface.

---

## 11. Discovery / Feature Map — ציון `68/100` (B-)

> **הקשר:** 🏠 home top tile — feature FOMO / progressive-unlock map (tile + next-unlock banner + 28-feature modal)
>
> **מימדים:** התמכרות **7** · יופי **6** · נוחות **6** · בהירות **8** · פוקוס **5** · סגירוּת **9** · רלוונטיות **8**

**פסק דין:** מפת-FOMO באמת שימושית עם hook-באנר חזק וסגירוּת חסינה — נבלמת מ-styling בלי-tokens, חוסר reduced-motion, ✕ <44px, ושני משטחים פועמים מעל ה-PLAY. **כדי להעלות:** הרגע אחד מהשניים (או מזג ל-pill), עבור ל-tokens, הוסף reduced-motion, הגדל ✕, וחזק את ה-locked-teaser עם scarcity/תזכורת.

**✅ מה כבר טוב (אל תיגע):**
- Strong progressive-unlock FOMO core: progress bar + '9/28 פתוחים · רמה N/20' (src/47-discovery.js:299, renderTile) gives a glanceable completion gap that pulls returning players — a real collection-completionist hook.
- The urgent next-unlock state is well-designed loss-aversion: when gamesToNext<=1 the banner flips to red gradient + '🔥 משחק אחד עד הפתיחה!' with a pulsing halo (renderNextUnlock src/47-discovery.js:368 + .discovery-next-unlock-urgent discovery.css:101) — a concrete, time-bound 'play one more' lever.
- Locked-teaser bullets are genuine FOMO content (toggleLockedTeaser src/47-discovery.js:536): three benefit lines + 'עוד N משחקים לפתיחה' hint per locked feature — surfaces exactly 'what you're missing'.
- Search also matches the locked-feature teaser text (renderBody src/47-discovery.js:446), so a player searching a benefit like 'יהלומים' finds the locked feature offering it — smart discoverability.
- Closability is excellent: modal carries both .discovery-modal-overlay and .modal-overlay so the global ESC + backdrop handler catches it (__bloomGetCloseableModals 04-ui-utils.js:919), plus explicit backdrop click (src/47-discovery.js:389) and an aria-labeled ✕. Three independent ways to close.
- Accurate copy: getPlayerLevel = games+1 (04-ui-utils.js:582-585), so gamesToNext = nextLevel-level genuinely equals the number of games to play — 'עוד N משחקים' never lies.
- Thorough dark-theme parity (discovery.css:361-419) covering tile, banner, urgent state, modal, rows, teaser — no near-invisible text on dark surface.
- XSS-safe by construction — pure createElement + textContent, zero innerHTML for variable content (file header comment + verified in buildRow).

**🛠️ משימות לביצוע (6):**

- [ ] **🟠 גבוה · מאמץ `M` · ★★★★ · פוקוס** — **Two pulsing surfaces stack ABOVE the PLAY CTA and fight focus**
  - **הבעיה:** Both the Discovery tile (.discovery-tile-pulse, infinite glow for the entire L1–19 range — i.e. most of a player's lifetime, src/47-discovery.js:319) AND the next-unlock banner (urgent red-pulsing halo, discovery.css:101) mount one after another at the home, both appended via home.appendChild (maybeMountTile src/47-discovery.js:279, maybeMountNextUnlock:341). Two animated meta-navigation surfaces competing for attention before the primary 'שחק עכשיו' action contradicts the Priority Calmer model (only the highest-emotion tile should pulse). A constantly-pulsing 'feature map' tile is meta-noise, not a higher-emotion signal than streak-danger or trophy-claim.
  - 📍 **הוכחה:** `src/47-discovery.js:319 (`if (s.level < 20) tile.classList.add('discovery-tile-pulse')`) + discovery.css:33-39 (2.4s infinite) + discovery.css:101-109 (banner urgent infinite pulse)`
  - 🔧 **לעשות:** In src/47-discovery.js renderTile(), only add .discovery-tile-pulse when there is a genuinely fresh unlock the player hasn't seen (e.g. summarize().gamesToNext<=1 OR a new feature crossed since last open via a localStorage 'seen' snapshot) — not for the whole L1–19 span. Register both #discovery-tile and #discovery-next-unlock with the Priority Calmer so when a higher-priority tile (streak-danger/trophy-claim) is pulsing, Discovery gets .polish-calmed. Keep the urgent banner pulse but suppress the tile's steady-state glow so only ONE of the two animates at a time.
- [ ] **🟡 בינוני · מאמץ `M` · ★★★★ · התמכרות** — **Locked teaser is the strongest hook but has weak follow-through — no scarcity, no reminder, dead-ends on a static hint**
  - **הבעיה:** toggleLockedTeaser (src/47-discovery.js:536) shows three benefit bullets + a 'עוד N משחקים לפתיחה' hint and then stops. There's no countdown, no 'closest unlock' pinning, and no way to act — the player reads the FOMO then closes. The single highest-converting moment (a locked feature the player wants) ends on inert text. The modal also doesn't surface a 'next to unlock' anchor at the top, so the locked content is buried inside category accordions rather than leading with the imminent reward.
  - 📍 **הוכחה:** `src/47-discovery.js:536-555 (teaser ends at static hint, no CTA/anchor) — only the home banner carries the urgency; inside the modal the imminent unlock has no special placement`
  - 🔧 **לעשות:** In showDiscoveryModal, render a pinned 'הבא בתור' (next-to-unlock) card at the top of the modal body using summarize().nextFeatures with the gamesToNext count + the urgent pulse styling, so the imminent reward leads. In toggleLockedTeaser, when gap<=2 add a small CTA row ('שחק עכשיו ←' that closes the modal and starts a game via the existing play entry point) to convert the FOMO into an immediate action instead of a dead-end.
- [ ] **🟡 בינוני · מאמץ `S` · ★★★ · יופי** — **No prefers-reduced-motion guard on three infinite animations**
  - **הבעיה:** discovery.css defines discoveryTilePulse (infinite), discoveryNextUrgent (infinite), discoveryCardPop and discoveryTeaserDown with zero prefers-reduced-motion handling. The project's own a11y convention (base.css has three @media (prefers-reduced-motion: reduce) blocks) is violated here — motion-sensitive users get a perpetually pulsing top-of-home plus a popping/sliding modal.
  - 📍 **הוכחה:** `grep found 0 prefers-reduced-motion in discovery.css vs base.css:100,915,1637 which all gate animations; offending keyframes discovery.css:36,106,168,333`
  - 🔧 **לעשות:** Append a `@media (prefers-reduced-motion: reduce)` block at the end of public/css/discovery.css that sets `animation: none` on .discovery-tile-pulse, .discovery-next-unlock-urgent, .discovery-modal-card and .discovery-row-teaser (keep a static box-shadow for the urgent state so it stays visually distinct). Matches the existing convention used across the codebase.
- [ ] **🟡 בינוני · מאמץ `M` · ★★★ · יופי** — **Ignores base.css design tokens — hardcoded hex, raw z-index:10000, no token radius/shadow**
  - **הבעיה:** Every color, radius, shadow and z-index is hardcoded instead of using the design-token system in base.css :root. The modal uses z-index:10000 (discovery.css:143) instead of var(--z-modal)=2000, breaking the harmonized stacking convention the ESC handler relies on. Accent gold #BA7517/#FAC775 are literally --color-accent/--color-accent-soft but written as raw hex, so a future palette/theme change won't propagate here. Radii are 14px/16px/10px ad-hoc vs --radius-md=12px.
  - 📍 **הוכחה:** `discovery.css:143 (`z-index: 10000`) vs base.css:70 (`--z-modal: 2000`); discovery.css:20,157,239 hardcode #BA7517/#FAC775 which equal base.css:19-20 --color-accent/--color-accent-soft; radii 14/16/10px vs base.css:41 --radius-md:12px`
  - 🔧 **לעשות:** In public/css/discovery.css replace z-index:10000 with var(--z-modal); swap hardcoded accent hex (#BA7517, #FAC775, #FFE194) for var(--color-accent), var(--color-accent-soft) and a glow var; align border-radii to var(--radius-md)/--radius-lg; use var(--shadow-md)/--shadow-glow for box-shadows. This restores theme consistency and correct modal stacking without visual regression.
- [ ] **🟡 בינוני · מאמץ `S` · ★★ · נוחות** — **Modal close ✕ is 32×32px — below the 44px tap-target standard**
  - **הבעיה:** The modal close button is 32×32px (discovery.css:183-184). The project's UX gate Q4 calls for a visible ≥40px close; iOS/Material recommend 44px. On a phone the 32px circle in the top-left corner is a small thumb target, and being top-LEFT in an RTL layout it's the far corner for a right-handed thumb.
  - 📍 **הוכחה:** `discovery.css:180-194 (`.discovery-modal-close { width:32px; height:32px; top:8px; left:8px }`)`
  - 🔧 **לעשות:** In public/css/discovery.css bump .discovery-modal-close to 40px (or 44px) width/height, increase font-size to ~18px, and keep the comfortable 8px inset. ESC + backdrop tap already cover it, but the visible affordance should meet the tap-target minimum for comfort.
- [ ] **⚪ נמוך · מאמץ `S` · ★★ · נוחות** — **'→ פתח' can silently no-op into a confusing toast right after landing on home**
  - **הבעיה:** openFeature (src/47-discovery.js:571) tries tileSelector then openGlobal; if the target tile hasn't mounted yet (home-v2 defers maybeShow* via setTimeouts up to ~3.2s, per CLAUDE.md), it falls through to a toast 'הפיצ׳ר עוד נטען — נסה שוב עוד רגע'. A player who opens Discovery in the first seconds and taps an unlocked feature gets a non-action + dismisses the modal-or-not ambiguously, which reads as broken.
  - 📍 **הוכחה:** `src/47-discovery.js:571-594 (no retry; immediate fallback toast on miss)`
  - 🔧 **לעשות:** In openFeature, when neither tileSelector nor openGlobal resolves, do NOT remove the overlay; instead retry once after ~400ms (single setTimeout) before showing the toast, and keep the modal open so the player can tap again. Alternatively gate the modal's open buttons behind a 'ready' check so freshly-deferred tiles resolve before the player can tap.

---

## 12. Bottom navigation (5 tabs) — ציון `69/100` (B-)

> **הקשר:** Persistent bottom-nav across home: 🏠 משחק / 🎁 פרסים / 👥 קהילה / 🏆 דרגות / 🛍 חנות. MutationObserver routes home tiles into pre-built tab screens.
>
> **מימדים:** התמכרות **5** · יופי **7** · נוחות **7** · בהירות **8** · פוקוס **8** · סגירוּת **9** · רלוונטיות **8**

**פסק דין:** ניווט 5-טאבים נקי, בנוי-היטב ובטוח-iOS שמצטיין בבהירות/סגירה/safe-area — אבל מתייחס ל-badges כמונה 'אריח חדש' במקום סימן דביק של 'פרס מחכה'. **כדי להעלות:** שנה את ה-badge לספור פרסים-ממתינים בפועל (אל תאפס במבט), אפשר badge בטאב הבית, והוסף reduced-motion. זה ה-ROI הגבוה ביותר על מנוף בודד באודיט.

**✅ מה כבר טוב (אל תיגע):**
- Solid, conventional 5-tab pattern with clear Hebrew labels + icons — understood in <3s, RTL-correct (inset-inline-start for badge), and the active tab is unambiguous (gold accent + scaled icon + top indicator bar).
- iOS safe-area handled correctly: anchors layout to env(safe-area-inset-bottom) rather than bottom:0+padding (bottom-nav.css:20), with translate3d/will-change compositor hardening (43-47) to stop the documented iOS scroll-drift bug. Home content gets matching bottom padding (251-253) so the bar never covers tiles.
- Closability/escape is excellent for a nav: switching is one tap, active tab persists in localStorage (bloom_active_tab), and CSS defense-in-depth hides the nav during gameplay via body:not([data-active-tab]) (274-276) so it can never overlap the grid.
- Thoughtful empty states: friendly placeholder cards (buildPlaceholderCard) instead of blank tabs, auto-removed when a real tile arrives (moveTileToTab) — good relevance. Tap targets are 60px min-height, comfortably above the 44px WCAG floor.

**🛠️ משימות לביצוע (7):**

- [ ] **🟠 גבוה · מאמץ `L` · ★★★★★ · התמכרות** — **Badges signal 'new tile mounted', NOT 'reward waiting to be claimed' — the #1 addiction miss**
  - **הבעיה:** The badge system counts how many tiles arrived in a tab since the player last opened it (updateTabBadge → countRealTiles vs readSeenCount, src/46-bottom-nav.js:238-256), and zeroes the badge the instant the player opens the tab (goToTab → snapshotSeenCount + setBadge(id,0), 504-507). So a spin-ready wheel, an unclaimed trophy milestone, a full BP-tier-to-claim, or a crying pet all produce the SAME generic count badge as a passive tile that merely mounted — and once you glance at the tab the badge clears even though the reward is still unclaimed. This throws away the strongest return-driver a bottom-nav has: a persistent red 'claim me' dot that won't go away until you act.
  - 📍 **הוכחה:** `src/46-bottom-nav.js:238-256 (badge = realTiles - lastSeen), :503-507 (goToTab snapshots seen + clears badge on mere view)`
  - 🔧 **לעשות:** In 46-bottom-nav.js add an actionable-state layer on top of the tile-count badge. Expose a window.__bloomTabActionBadge(tabId, count) that feature modules (spin, trophy, login-cal, BP, pet, daily-deal) call when they have a claimable/urgent state. Maintain two badge sources per tab: (a) new-content count (current behavior, clears on view) and (b) actionable count (persists until the underlying action is done, NOT cleared by goToTab). Render the actionable badge with priority — if actionable>0 show that number in red; else fall back to the new-content dot. Update setBadge to read max(actionable, dot). Wire spin/trophy/login-cal/BP/pet to call the new API in their maybeShow* + on state change. Result: the 🎁 and 🏆 tabs carry a sticky red 'N' that only disappears once the player actually claims, driving the daily return loop.
- [ ] **🟡 בינוני · מאמץ `M` · ★★★★ · התמכרות** — **Home tab (🏠 משחק) can never show a badge — high-urgency hooks live there silently**
  - **הבעיה:** Badge refresh is hardcoded to the four non-home tabs (refreshAllBadges iterates ['rewards','social','progress','shop'], src/46-bottom-nav.js:277-280; maybeShowFirstTimeTabsTip:413 same list). But the home tab hosts Battle Pass (#home-v2-season-pass), Lives (#lives-home-widget), Weekly host and Jackpot — surfaces that frequently have a 'claim BP tier' or 'lives full' urgency. Because the player usually sits on the home tab, an urgent home-tab hook gets zero nav-level escalation, and a player on (say) the shop tab gets no nudge that a BP tier is claimable back home.
  - 📍 **הוכחה:** `src/46-bottom-nav.js:276-280 (refreshAllBadges excludes 'home'), :44-51 (BP/lives/jackpot mapped to 'home')`
  - 🔧 **לעשות:** Allow the home tab to carry an actionable badge too. In refreshAllBadges include 'home' in the loop but skip the 'new content' delta for it (home tiles aren't migrated, so tile-count delta is meaningless) — instead drive the home badge purely from the new __bloomTabActionBadge actionable API (BP-tier-claimable, lives-full). Guard setBadge so the home badge never shows while _activeTab==='home' (the player is already there) but DOES show when they're on another tab. Result: leave home → claim a BP tier appears → a red dot on 🏠 pulls you back.
- [ ] **🟡 בינוני · מאמץ `S` · ★★★ · יופי** — **Three infinite animations with no prefers-reduced-motion guard**
  - **הבעיה:** bottom-nav.css has zero prefers-reduced-motion handling (grep: 0 matches), yet it defines three perpetually-looping animations: bnBadgeDotPulse (1.8s infinite, :141/148-151), the tabs-tip bnTipPulse (1.6s infinite, :300/307-310) and bnTipBounce (1.2s infinite, :314/316-319). The rest of the codebase consistently guards loops (base.css:100, :915, :1637 etc.), so this is an inconsistency that will annoy motion-sensitive users and breaks the project's own 'יפה'/accessibility bar. The bouncing 👇 tip is especially aggressive.
  - 📍 **הוכחה:** `public/css/bottom-nav.css (no prefers-reduced-motion; infinite loops at :141, :300, :314); contrast base.css:100/915/1637 which all guard their loops`
  - 🔧 **לעשות:** Append to bottom-nav.css: @media (prefers-reduced-motion: reduce) { .bn-tab-badge-dot, .bn-tabs-tip, .bn-tabs-tip-icon, .bloom-bottom-nav { animation: none; } } and also neutralize the bnSlideUp entrance + bnBadgePop. Keep the static gold/red styling so the badge dot is still visible (just not pulsing). Matches the per-component reduced-motion pattern already used across base.css.
- [ ] **⚪ נמוך · מאמץ `M` · ★★★ · התמכרות** — **Tabs are static labels with no live-state preview — they don't tease what's inside**
  - **הבעיה:** Each tab is a fixed icon+label (TABS array, src/46-bottom-nav.js:23-29). Beyond the count badge, there's no micro-preview of urgency or value — e.g. the 🎁 tab doesn't hint 'free spin ready', the 🏆 tab doesn't hint 'milestone +250💎'. Best-in-class F2P navs use the nav itself as a FOMO surface (Coin Master / Clash Royale show timers and 'collect' states on the bar). Right now the nav is purely structural, leaving dopamine on the table.
  - 📍 **הוכחה:** `src/46-bottom-nav.js:23-29 (static labels), :104-127 (buildNav renders icon+label+badge only)`
  - 🔧 **לעשות:** Add an optional one-line micro-status under the most urgent tab's label, swappable via the actionable-state API. When 🎁 has a free spin available, briefly flip its label to a gold 'חינם!' chip; when 🏆 has a claimable milestone show the icon with a tiny gold ring. Implement as an optional .bn-tab-flag span set by __bloomTabActionBadge with a reason string; default off (focus-preserving — only the single highest-priority tab flags at a time, never all five). Reuse the existing badge-pop animation. Result: the nav itself becomes a daily-return pull without adding modal noise.
- [ ] **⚪ נמוך · מאמץ `M` · ★★★ · נוחות** — **Badge resets on glance even with rewards unclaimed; no audio/haptic on switch for first-discovery**
  - **הבעיה:** goToTab plays soundDrop on every switch (src/46-bottom-nav.js:509) which is fine, but combined with the badge-clears-on-view behavior (504-507) the system actively discourages the player from re-checking: open the tab once, badge gone, no reason to return even though the reward is still sitting there. Friction is low but the loop is hollow — the nav teaches 'nothing persists', which undercuts the daily ritual.
  - 📍 **הוכחה:** `src/46-bottom-nav.js:503-510 (snapshot seen + clear badge + soundDrop on every goToTab)`
  - 🔧 **לעשות:** Tie badge clearing to ACTION rather than VIEW. After implementing __bloomTabActionBadge, do NOT clear the actionable badge in goToTab — only clear the new-content delta there. Let the actionable badge be cleared by the feature module when the player actually claims (spin spun, milestone claimed). Keep soundDrop on switch. Result: opening 🎁 and seeing the spin still sitting there keeps the red badge until you spin, reinforcing the 'come collect' habit.
- [ ] **⚪ נמוך · מאמץ `S` · ★★ · יופי** — **Active-tab indicator is subtle; inactive tabs are low-contrast gray — weak glanceability**
  - **הבעיה:** The active state relies on color swap to --color-accent (#BA7517) + a 1.18x icon scale + a thin 3px/36px top bar (bottom-nav.css:90-107). Labels stay font-weight 700 on BOTH active and inactive tabs, so the only weight signal is color. Inactive tabs are var(--color-text-muted, #6F6E68) on a near-white blurred bar — readable but flat. On a busy home background the 3px top bar can be easy to miss, and there's no background-fill or pill behind the active icon, so the bar reads less 'selected' than modern app navs (which usually fill or pill the active item).
  - 📍 **הוכחה:** `public/css/bottom-nav.css:82-107 (label weight 700 both states; active = color + 3px bar + scale only)`
  - 🔧 **לעשות:** Strengthen the active affordance in bottom-nav.css: add a soft pill/halo behind the active icon — e.g. .bn-tab-active .bn-tab-icon { filter: drop-shadow(0 2px 6px var(--color-accent-glow)); } plus a subtle radial background chip behind the icon via a ::after using --color-accent-glow at ~12% alpha and --radius-md. Optionally bump active label to font-weight 800 and inactive down to 600 for a clearer hierarchy. Keep the gold top bar. Result: the selected tab reads instantly 'lit' even over a colorful home.
- [ ] **⚪ נמוך · מאמץ `S` · ★★ · נוחות** — **Persistent will-change: transform keeps a compositor layer alive forever (minor perf/comfort)**
  - **הבעיה:** The nav sets will-change: transform permanently (bottom-nav.css:45) as part of the iOS hardening. will-change is intended to be applied just before an animation and removed after; leaving it on indefinitely pins an extra GPU layer for the lifetime of the home, costing memory on low-end Android. The translate3d/backface-visibility hardening already creates the layer, so will-change is redundant here.
  - 📍 **הוכחה:** `public/css/bottom-nav.css:43-47 (translate3d + will-change:transform + backface-visibility all permanent)`
  - 🔧 **לעשות:** In bottom-nav.css remove the permanent 'will-change: transform' from .bloom-bottom-nav (lines ~45). The translate3d(0,0,0) + backface-visibility:hidden already promote the element to its own layer for the iOS fix, so the compositing benefit is retained without the standing memory cost. Verify on iOS that the scroll-drift fix still holds after removal.

---

## 13. Contests (menu / create / join / smart leaderboard / in-game HUD) — ציון `69/100` (B)

> **הקשר:** friends contests — create/join/leaderboard flow + in-game contest HUD
>
> **מימדים:** התמכרות **8** · יופי **6** · נוחות **6** · בהירות **7** · פוקוס **5** · סגירוּת **9** · רלוונטיות **7**

**פסק דין:** HUD-תחרות חי מצוין ו-smart-leaderboard עם hooks חזקים (next-target pill, פעימת-דירוג, צפייה) — נבלם מ-7-חלקי טופס-יצירה, סחף-tokens, ו-safe-area חסר. **כדי להעלות:** כווץ את הטופס לשדה-שם + 'צור' (כל השאר ברירת-מחדל מתחת ל-'אפשרויות מתקדמות'), הוסף safe-area, ועבור ל-tokens.

**✅ מה כבר טוב (אל תיגע):**
- Best-in-class in-game contest HUD: 3-column (target / me / chaser) with absolute scores, projected live rank, and a rank-improvement dopamine pulse (contestHudRankUp keyframe, 06-contests.js:1183, screens.css:1688). This is the strongest retention surface on the screen — ambient 'where do I stand' pressure every drop.
- Persistent 'next target' pill on MY leaderboard row — 'עוד N כדי לעקוף את X' (06-contests.js:682) — surfaces the single most-addictive contest moment without waiting for a poll. Pulsing gold gradient with dark-mode parity (screens.css:771, dark.css:103).
- Smart leaderboard scales correctly: flat under 12 players, top-5 + my-window + expand-all + search above (renderContestSmartBoard, 06-contests.js:698), with podium medals 🥇🥈🥉 (06-contests.js:657). Search keeps focus/caret across the 20s silent refresh (06-contests.js:855) — real polish.
- Closability is excellent: #contest-screen is registered in the global ESC/back-gesture handler (__bloomGetCloseableModals, 04-ui-utils.js:935), every screen has a ≥40px back button, and the HUD→leaderboard→resume loop returns to the paused game in one tap (06-contests.js:1334, 1388).
- Spectate-to-watch wired straight off live rows (data-spectate-target, 06-contests.js:648) — turns the leaderboard into a live social surface, a strong friends-group hook.

**🛠️ משימות לביצוע (6):**

- [ ] **🟠 גבוה · מאמץ `M` · ★★★★ · פוקוס** — **Create-contest form is a 7-section wall — heavy friction for the core 'invite family' use case**
  - **הבעיה:** showCreateContestForm renders SEVEN stacked decision blocks before the submit button: name, host display name, duration (3 pills), board type (2 pills), difficulty (5 pills), score mode (2 pills), wager (number input). For the prime use case ('משפחת כהן · פסח' — a casual family contest), 5 of these are advanced knobs the host doesn't care about. Every extra decision is a drop-off point, and the host is the viral seed — losing them kills the whole invite tree. The screen scrolls well past one viewport on a phone.
  - 📍 **הוכחה:** `src/06-contests.js:196-243 — name input (205), host (207), duration row (209), board-type row (215), difficulty row (221, 5 pills), score-mode row (230), wager input (237), submit (241). All seven are always visible, no progressive disclosure.`
  - 🔧 **לעשות:** In showCreateContestForm (src/06-contests.js:196), collapse difficulty + score-mode + wager into a single collapsed '⚙️ הגדרות מתקדמות' accordion that defaults closed (sensible defaults already exist: default/cumulative/0-wager). Keep only name + host + duration above the fold so the happy path is name → duration → 'צור והעתק קוד'. The accordion expands on tap with a chevron rotation. Result: 3-decision happy path, advanced users still reach everything in one tap.
- [ ] **🟡 בינוני · מאמץ `S` · ★★★ · נוחות** — **Contest screens ignore safe-area insets — back button and bottom CTA clip under notch/home-indicator**
  - **הבעיה:** The .contest-screen container uses a fixed padding of 30px 24px with no env(safe-area-inset-*) and the back button is absolutely positioned at top:14px right:14px. On notched iPhones the back button can sit under the status-bar notch, and the destructive bottom action ('נתק ממכשיר זה') plus the play CTA can be partially hidden behind the home indicator. The contest HUD correctly uses max(8px, env(safe-area-inset-top)) (screens.css:1613), so the inconsistency is visible the moment the HUD overlaps the screen.
  - 📍 **הוכחה:** `public/css/screens.css:2-7 (.contest-screen padding:30px 24px; overflow-y:auto, no safe-area) and :16-18 (.contest-back-btn top:14px right:14px). Compare to the HUD at :1613 which does respect env(safe-area-inset-top).`
  - 🔧 **לעשות:** In public/css/screens.css, change .contest-screen padding to `calc(30px + env(safe-area-inset-top)) 24px calc(30px + env(safe-area-inset-bottom))` and move .contest-back-btn top to `max(14px, env(safe-area-inset-top))`. This stops the back button and the leave/play CTAs from clipping on notched phones.
- [ ] **🟡 בינוני · מאמץ `M` · ★★★ · יופי** — **Contest forms use hardcoded hex instead of design tokens — visual drift from the polished over-screen siblings**
  - **הבעיה:** The entire contest form/card/board surface is built on literal hex values (#1C1A18, #FAFAF6, #FAC775, #6F6E68) and ad-hoc radii (input 9px, pill 8px, submit-btn 10px, card 12px, link-card 12px, HUD 14px). The over-screen banners in the SAME file already migrated to tokens (--color-accent, --radius-md, --shadow-glow at screens.css:1001-1028), so the contest area looks slightly off-system — different gold, different corner softness, different shadow language. The owner explicitly now wants 'יפה' (beautiful/consistent).
  - 📍 **הוכחה:** `public/css/screens.css:83-132 (.contest-input radius:9px #FAFAF6, .contest-duration-pill radius:8px, .contest-submit-btn radius:10px #1C1A18) vs the token-based block at :1001-1028 (var(--radius-lg), var(--shadow-glow), var(--color-accent)).`
  - 🔧 **לעשות:** In public/css/screens.css, migrate the contest form/card/pill/button rules to design tokens: backgrounds → var(--color-surface)/var(--color-surface-2), borders → var(--color-border), accent fills → var(--color-accent), and unify radii to var(--radius-md) for inputs/pills/buttons and var(--radius-lg) for cards. Replace the :focus outline #FAC775 with var(--color-accent). Keep dark.css overrides working (they already key off these classes). Result: contest screens match the rest of the app's corner/shadow/gold language.
- [ ] **🟡 בינוני · מאמץ `M` · ★★★ · נוחות** — **Join flow has no friend-name preview before commit and uses a separate preview screen — extra friction + uncertainty**
  - **הבעיה:** showJoinContestForm is a bare 6-char code input that, on submit, navigates to a whole separate showContestPreview screen. The player types a code blind — there is no inline confirmation of WHO/WHAT they're joining (host name, member count) until after a full screen transition. For a friends contest the reassurance 'this is סבא משה's contest, 8 playing' is exactly the social-proof hook that should appear the moment the code resolves, reducing the 'did I type it right?' anxiety and the chance of bouncing.
  - 📍 **הוכחה:** `src/06-contests.js:456-478 (showJoinContestForm — code input only, on submit → showContestPreview at :476). No debounced lookup, no inline host/member preview.`
  - 🔧 **לעשות:** In showJoinContestForm (src/06-contests.js:456), add a debounced (~300ms) inline lookup once the code reaches 6 chars: fetch contest meta and render a small confirmation card under the input ('🎮 <name> · מארח <host> · N שחקנים · ⚔️ הצטרף') with the join button enabled only after a valid resolve. Keep showContestPreview as the fallback for failures. Result: the friend's name appears inline as social proof, and a mistyped code is caught before a screen transition.
- [ ] **⚪ נמוך · מאמץ `S` · ★★★ · התמכרות** — **Contest menu has no first-time hook / social proof for a player with zero contests**
  - **הבעיה:** When a player has never joined a contest, showContestMenu shows only the bare 'create' and 'join' cards (the 'התחרויות שלי' continue card is gated on activeContestCode). There is no aspirational copy, no example, no 'compete with family for bragging rights' framing, and no live social proof — so the highest-emotion reason to start a contest (status competition with people you know) is invisible at the exact decision moment. New players get a flat chooser instead of a pull.
  - 📍 **הוכחה:** `src/06-contests.js:33-100 — continueCard only built when activeContestCode is truthy (:40); otherwise the screen is just title + sub + two generic action cards (:52-76), no value-prop or social-proof element.`
  - 🔧 **לעשות:** In showContestMenu (src/06-contests.js:33), when there is no active contest, inject a small aspirational hero above the cards: a one-line value prop ('🏆 מי במשפחה הכי טוב ב-BLOOM?') plus, if cheap to fetch, a live-players or 'X contests running now' social-proof pill. Keep it lightweight and torn down with the screen. Result: the empty menu becomes a pull toward creating/joining instead of a neutral chooser.
- [ ] **⚪ נמוך · מאמץ `S` · ★★ · יופי** — **Destructive 'leave contest' uses a raw native confirm() — off-brand and jarring**
  - **הבעיה:** The 'נתק ממכשיר זה' action fires a native browser confirm() dialog. It's unstyled, breaks the RTL/branded aesthetic, and on iOS shows the URL/host in the dialog chrome — exactly the kind of 'this feels like a webpage, not a game' break the owner wants gone. It's also visually the same weight as a benign action despite being destructive.
  - 📍 **הוכחה:** `src/06-contests.js:1470 — `if (!confirm('לנתק את המכשיר מהתחרות? ...')) return;``
  - 🔧 **לעשות:** Replace the native confirm() at src/06-contests.js:1470 with a small branded modal-overlay confirm (reuse the existing toast/overlay pattern or a lightweight .modal-overlay) styled with --color-danger for the confirm button and a clear cancel. Register it in __bloomGetCloseableModals if it's full-screen. Result: the destructive action gets a consistent, on-brand confirmation instead of browser chrome.

---

## 14. Pet / Mascot (Tamagotchi) — ציון `69/100` (B-)

> **הקשר:** Home tile widget + pet modal — the emotional anchor (guilt-trip retention mechanic)
>
> **מימדים:** התמכרות **6** · יופי **7** · נוחות **7** · בהירות **8** · פוקוס **8** · סגירוּת **6** · רלוונטיות **8**

**פסק דין:** Tamagotchi מקסים ומ-juiced עם guilt-trip חזק ו-dark-mode נקי — אבל האשמה לא ניתנת-לפעולה (אין 'האכל אותי עכשיו' עם שעון/הימור), הלולאה היומית דקה, ו-naming לא נסגר ב-ESC. **כדי להעלות:** הוסף CTA ישיר מהאריח האדום + loss-aversion עם שעון, רמז את שלב-האבולוציה הבא, הוסף שכבת-רצף/חברתית, וחבר naming ל-ESC.

**✅ מה כבר טוב (אל תיגע):**
- Genuinely strong guilt-trip rendering: sad/crying mood swaps the home widget to a red gradient, pulses the border (petWidgetNeedsAttn 1.8s), greyscales the pet emoji (grayscale(0.4) at boards.css:5830), and the modal crying message shakes (petCryingShake at boards.css:6043) — the Tamagotchi emotional pressure is real and on-brand.
- Excellent action juice and post-fix dopamine: pet/feed swap the button to a '✓ קיבלת! +20💎' confirmation (22-pet.js:319-322), fire a big +N💎 floater from modal center (showPetRewardFloater), heart/sparkle radial bursts from the pet emoji, soundMilestone + escalating buzz, and a balance bump via __bloomBumpBal — clear cause→reward loop.
- Robust error/edge handling rewrite: disabled buttons still fire friendly toasts instead of silently absorbing the tap (22-pet.js:247-269), ⏳ loading state restores original HTML on failure, and the re-show timer is cancelled on close (_pendingReshowTimer) — fixes the old 'modal pops up every second' bug.
- Good relevance gating + dark-mode parity: widget unlocks at L8 (22-pet.js:42), action dots (💗/🍽) appear only when an action is actually available, and full html[data-theme=dark] overrides exist for every pet surface (boards.css:6134-6171).
- Beautiful, consistent green Tamagotchi palette with overshoot modal pop (spModalPop cubic-bezier 0.34,1.56), bobbing pet (petWidgetBob), and a level-up slide toast — visually cohesive and pleasant.

**🛠️ משימות לביצוע (6):**

- [x] ✅ **בוצע (סבב 6)** · **🟠 גבוה · מאמץ `M` · ★★★★★ · התמכרות** — **Guilt is emotional but not ACTIONABLE — no 'feed me now' CTA or time-specific loss-aversion**
  - **הבעיה:** The crying/sad state is the single strongest retention lever the pet has, but it only pulses red and shows a vague message ('😢 הפרח שלך עצוב... שיחקת לאחרונה?' at 22-pet.js:205). The home widget that pulses red has no direct action — tapping it opens the modal, and even the modal's message is passive text. There is no concrete countdown ('הפרח לא אכל 14 שעות', 'יבכה בעוד 3 שעות'), no one-tap 'הרגע אותו' button on the widget, and no real consequence to ignoring it (the pet never actually loses level/stage). Guilt without a clock or a stake is weak — Coin Master/Tamagotchi tie the guilt to a visible degrading timer.
  - 📍 **הוכחה:** `src/22-pet.js:201-207 (static mood messages, no time data) + boards.css:5810-5818 (red pulse with no embedded CTA) — mood is derived purely server-side from hours-since-visit but the client never surfaces the hours or a 'next decay' time.`
  - 🔧 **לעשות:** In src/22-pet.js renderWidgetInner(): when mood is sad/crying, add a second meta line with the concrete signal the server should return (e.g. data.hoursSinceVisit) — 'לא ביקרת 14 שעות · הוא בוכה' — and add an inline one-tap '💗 הרגע אותו' button directly on the widget that calls doPetAction without opening the modal first. Have the server return a 'willCryAt'/'moodDecaysInMs' field and render a live countdown in the modal message ('יהפוך לעצוב בעוד 2:14') to create a return-by-a-time hook.
- [x] ✅ **בוצע (סבב 6)** · **🟡 בינוני · מאמץ `M` · ★★★★ · התמכרות** — **Modal lacks evolution-anticipation — the next-stage emoji is never teased to pull progression**
  - **הבעיה:** The 4 evolution stages (🌱→🌿→🌸→🌺) are the pet's long-arc retention hook, but the modal only shows the CURRENT stage emoji + the XP bar to the next LEVEL — it never previews the next STAGE or how close it is. A player at level 5 (🌱) about to hit 🌿 at level 6 has no visual 'one more level to evolve!' lure. Genshin/Tamagotchi-style collections always silhouette the next form to drive the chase.
  - 📍 **הוכחה:** `src/22-pet.js:196-199 — modal renders current stageLabel + level X/maxLevel + xpToNext, but no nextStage preview. The stage thresholds (6/11/16) are hardcoded in grantPetXpForGame (22-pet.js:521-524) yet never surfaced as a teaser.`
  - 🔧 **לעשות:** In src/22-pet.js showPetModal(), compute the next stage from the level thresholds (6/11/16) and when within ~2 levels of an evolution, add a gold teaser row under the XP bar: '🌿 עוד 1 דרגה והוא יתפתח!' with the next-stage emoji shown at reduced opacity/greyscale. Add a one-time evolution celebration (confetti + showPetLevelUpToast variant 'התפתח ל-🌿 שתיל!') when crossing a stage boundary in doFeedAction/grantPetXpForGame.
- [ ] **🟡 בינוני · מאמץ `L` · ★★★★ · התמכרות** — **Only two thin daily actions and no social/streak layer — the loop caps out fast**
  - **הבעיה:** Per day the pet offers exactly: 1 free pet (+20💎) and up to feedsPerDay feeds. There's no petting streak ('יום 5 ברצף ליטוף · בונוס!'), no friend comparison ('הפרח של דניאל בדרגה 12'), and no reason to return more than once. The pet is a pleasant side-tile but doesn't compound — after the first visit each day there's nothing left to chase until the XP bar slowly fills from games.
  - 📍 **הוכחה:** `src/22-pet.js:211-220 — only pet + feed actions; no streak field consumed, no social field, totalPetCount/totalFedCount shown only as flat lifetime stats (22-pet.js:222-224).`
  - 🔧 **לעשות:** Add a petting-streak to the server state and surface it in the modal stats row as '🔥 רצף ליטוף N ימים' with an escalating bonus on milestones (mirror the existing dyn-streak pattern). Optionally add a 'הפרח של החברים' compare strip in the modal (reuse the friends fetch) to add social pressure. This turns the pet from a once-a-day stop into a streak the player fears breaking.
- [x] ✅ **בוצע (סבב 6)** · **🟡 בינוני · מאמץ `S` · ★★★ · סגירוּת** — **Naming overlay has no visible close ✕ and is not in the ESC/back-gesture closer**
  - **הבעיה:** The first-run naming overlay (.pet-name-overlay) is the very first pet surface a new L8 player meets, yet it offers only a low-contrast text 'דלג' link (boards.css:5931, 12px, #047857) and a backdrop tap to escape. There is no ≥40px corner ✕. Critically, .pet-name-overlay does NOT match the global ESC handler's selector ([class*="modal-overlay"]) and is not in the curated extras list (src/04-ui-utils.js:922-936) — so pressing ESC or swiping back will NOT dismiss it, violating the UX gate's 'one obvious way to close'.
  - 📍 **הוכחה:** `src/22-pet.js:116-128 (overlay markup has no ✕ button, only #pet-name-skip text) + src/04-ui-utils.js:922-936 (extras list omits .pet-name-overlay).`
  - 🔧 **לעשות:** Add a 36-40px circular ✕ button (reuse the .pet-modal-close pattern, aria-label="סגור") to the top-left of .pet-name-card in src/22-pet.js promptForPetName(), wired to close(). Then add '.pet-name-overlay' to the extras querySelectorAll in src/04-ui-utils.js __bloomGetCloseableModals() so ESC + browser-back dismiss it through the unified path.
- [x] ✅ **בוצע (סבב 6)** · **🟡 בינוני · מאמץ `S` · ★★★ · יופי** — **Zero prefers-reduced-motion coverage — continuous bob/pulse/shake ignore OS accessibility setting**
  - **הבעיה:** Every pet animation runs forever regardless of the user's reduced-motion preference: petWidgetBob (3s infinite on widget + 100px modal emoji), petWidgetNeedsAttn (red box-shadow pulse), petDotsBounce, and petCryingShake (0.6s infinite translateX). None are wrapped in @media (prefers-reduced-motion: reduce). The codebase already establishes this pattern (boards.css:5215, base.css:100) and the owner explicitly wants the game 'comfortable' (נוח) — perpetual motion is fatiguing and an accessibility regression.
  - 📍 **הוכחה:** `boards.css:5823 (petWidgetBob infinite), :5813 (petWidgetNeedsAttn infinite), :5860 (petDotsBounce infinite), :6043 (petCryingShake infinite) — grep for prefers-reduced-motion in boards.css returns only :3029 and :5215, neither covering pet.`
  - 🔧 **לעשות:** Add a @media (prefers-reduced-motion: reduce) block in boards.css near the pet rules that sets animation:none on .pet-widget-emoji, .pet-home-widget.pet-widget-needs-attention, .pet-widget-dots, .pet-modal-emoji, .pet-modal-msg-crying, and .pet-heart-burst/.pet-sparkle-burst. Keep the red gradient/border for crying (static color still conveys guilt) but stop the perpetual movement.
- [x] ✅ **בוצע (סבב 6)** · **⚪ נמוך · מאמץ `S` · ★★ · נוחות** — **Modal close button is 32px — under the 40px tap-target minimum**
  - **הבעיה:** The pet modal ✕ (.pet-modal-close) is 32×32px (boards.css:5974-5975), below the recommended ≥40px thumb target. On a phone, in the top-left corner (RTL), it is both small and slightly awkward to reach. The rest of the modal's actions are well-sized, making this the one friction point in an otherwise comfortable surface.
  - 📍 **הוכחה:** `public/css/boards.css:5970-5984 — width:32px; height:32px.`
  - 🔧 **לעשות:** In boards.css .pet-modal-close bump width/height to 40px (or 38px with a larger invisible hit-area via padding), increase font-size to 20px, and raise background contrast slightly (rgba(0,0,0,0.22)) so it reads clearly on the light-green card. Apply the same to the new naming-overlay ✕.

---

## 15. Home screen (Power-Hero default) — ציון `70/100` (B)

> **הקשר:** 🏠 משחק tab / landing — most-viewed surface. Files: src/05a-home-v2.js, src/31-home-variants.js, src/80-polish.js, public/css/home-v2.css
>
> **מימדים:** התמכרות **7** · יופי **7** · נוחות **6** · בהירות **8** · פוקוס **6** · סגירוּת **9** · רלוונטיות **7**

**פסק דין:** נחיתה ממוקדת ובנויה היטב, עם משמעת CTA-יחיד מצוינת והיגיינת מגע/RTL/dark-mode טובה — אבל משאירה כסף-התמכרות על השולחן. **כדי להעלות:** מלא את כותרת-המשנה של ה-CTA ב-hook אישי (רצף/שיא/פרס יומי), הפוך את 3 חרירי ה-balance המתים (💎/❤️/⭐) לכפתורים בלחיצה-אחת, הרגע את ריבוי האנימציות במקביל והוסף reduced-motion.

**✅ מה כבר טוב (אל תיגע):**
- Strong single-CTA discipline: the giant 'שחק עכשיו' button dominates (power-hero CSS home-v2.css:5220 max-width 460 / 26px padding / 22px font + powerHeroCtaBreath), exactly the Royal-Match pattern that lifts session-start rate.
- Genuinely good tap-target hygiene — balance slots forced to 44px min-height (home-v2.css:50), hero nav arrows 44×44 (home-v2.css:5336), hero dots use a 28px hit-box around a 7px visible dot (home-v2.css:5293).
- Rotating hero card is well-engineered: pauses 12s after any user interaction (31-home-variants.js:596-599), RTL-aware swipe/arrow/keyboard nav, dot indicators, and self-cleaning interval when the card leaves the DOM (31:598).
- Good dark-mode parity and design-token usage throughout the balance bar (--color-surface / --color-border / --shadow-md) with explicit dark overrides for all 4 slot tints (home-v2.css:77-80) — fixes the old invisible-text bug.
- Priority Calmer is a smart focus lever: only the single highest-priority hot tile keeps its pulse (80-polish.js:88-100), the rest get desaturated/dimmed — the eye is pulled to ONE claim.
- Re-entry guards everywhere (hero-extras id check 31:341, carousel guard 31:202) and aggressive teardown of leaked in-game banners on home mount (05a:60-75) keep the landing clean.

**🛠️ משימות לביצוע (7):**

- [x] ✅ **בוצע (סבב 1)** · **🟠 גבוה · מאמץ `S` · ★★★★ · התמכרות** — **The dominant CTA has an empty subtitle — biggest unused hook on the most-tapped element**
  - **הבעיה:** The giant primary 'שחק עכשיו' button renders a #home-v2-cta-sub span that is created empty (src/05a-home-v2.js:146) and is NEVER populated anywhere in the codebase (grep confirms zero setters in src/). The single most-viewed, most-tapped surface in the game carries no streak/return-time/reward micro-copy. Players get no loss-aversion or 'come back at X' pull at the exact moment of highest intent. The CSS even styles .home-v2-cta-sub:not(:empty) (home-v2.css:456) — the slot was designed for content that never arrived.
  - 📍 **הוכחה:** `src/05a-home-v2.js:146 (empty span), no setter in src/; public/css/home-v2.css:456 + :5234 style an :not(:empty) state that never fires`
  - 🔧 **לעשות:** In src/05a-home-v2.js, add updateHomeCtaSub() called from showHomeV2()'s render pass (near renderHeroBannerV2). Populate #home-v2-cta-sub with the strongest of: daily-challenge not played → '🎁 האתגר היומי מחכה'; streak.count>=3 and !todayPlayed → '🔥 שמור על רצף ' + count + ' ימים'; else 'נצח את השיא: ' + best.toLocaleString(). Use the existing --color-accent-soft styling already in place. Result: the dominant button now carries a personalized addiction hook.
- [x] ✅ **בוצע (סבב 1)** · **🟠 גבוה · מאמץ `S` · ★★★★ · נוחות** — **3 of 4 balance-bar slots are dead — gems/lives/level do nothing on tap**
  - **הבעיה:** The balance bar is the most-viewed widget (top of every home view) but only the streak slot is interactive (it's a <button> opening the streak calendar, 05a:115+488). The 💎 gems slot (05a:107), ❤️ lives slot (05a:111) and ⭐ level slot (05a:119) are plain <div>s with no handler. Tapping 💎 — the most monetization-relevant element on screen — is a dead-end: no route to the gem bank or shop. Tapping ❤️ doesn't open the lives-refill modal. This is both a friction/comfort miss and a lost monetization/return funnel.
  - 📍 **הוכחה:** `src/05a-home-v2.js:107 (gems <div>), :111 (lives <div>), :119 (level <div>) vs :115 (streak <button> wired at :485-489)`
  - 🔧 **לעשות:** In src/05a-home-v2.js showHomeV2(): convert #home-v2-bal-gems-slot and #home-v2-bal-lives-slot to <button type=button>. Wire gems → window.__bloomGemBank?.openModal() (fallback showShop), lives → window.__bloomLives?.openModal(). Leave level as a <button> opening the discovery/level modal (__bloomDiscovery) or the level-progress sheet. Add :active{transform:scale(0.96)} matching the streak slot (home-v2.css:90). Result: every glanceable stat becomes a 1-tap entry to its system.
- [ ] **🟡 בינוני · מאמץ `S` · ★★★ · יופי** — **Several home animations ignore prefers-reduced-motion (comfort + a11y gap)**
  - **הבעיה:** The home runs multiple infinite keyframe loops with no reduced-motion fallback: powerHeroCtaBreath (home-v2.css:5239, scale+glow pulse on the giant CTA), ctaV2Glow (:451), polishSpotlightHalo (:2058), balFloatUp/balBumpScale (:118/93), and the JS-driven 7s hero auto-rotation (31:597). The only prefers-reduced-motion block in home-v2.css (:5792) covers just the friend-notify banner. Users who set reduce-motion (motion sensitivity, vestibular issues) still get a perpetually breathing button + halo + auto-swapping hero — the opposite of 'נוח'.
  - 📍 **הוכחה:** `public/css/home-v2.css:5792 (only friend-notify covered); :5239 powerHeroCtaBreath, :451 ctaV2Glow, :2058 polishSpotlightHalo all infinite with no guard`
  - 🔧 **לעשות:** Append to the @media (prefers-reduced-motion: reduce) block in public/css/home-v2.css: set animation:none on .home-v2-cta, body.power-hero .home-v2-cta, .polish-spotlight::before, .home-v2-bal-float. In src/31-home-variants.js renderRotatingHeroCard, gate the auto-rotate setInterval behind window.matchMedia('(prefers-reduced-motion: reduce)').matches === false (keep manual arrows/dots working). Result: respectful, calmer home for motion-sensitive players.
- [ ] **🟡 בינוני · מאמץ `M` · ★★★ · פוקוס** — **Three simultaneous motion sources compete for the eye — focus is diluted**
  - **הבעיה:** On a typical home, THREE things move at once: (1) the giant CTA breathes/glows perpetually (powerHeroCtaBreath, home-v2.css:5227), (2) the rotating hero card swaps content every 7s and its CTA pulses, and (3) the Priority Calmer spotlights one tile with an animated gold halo (polishSpotlightHalo). The Priority Calmer's whole thesis is 'draw the eye to ONE thing' (80-polish.js header), but it doesn't calm the always-breathing CTA or the rotating hero above it, so there are effectively 2-3 co-equal attention magnets stacked vertically. That undercuts the single-dominant-action promise.
  - 📍 **הוכחה:** `src/80-polish.js:70-101 (spotlight one tile) runs alongside home-v2.css:5227 (CTA breathe) and src/31-home-variants.js:597 (hero auto-rotate) with no coordination`
  - 🔧 **לעשות:** Establish a motion hierarchy. Simplest: when a hot signal exists in the rotating hero (collectHotSignals returns >0 with cls hero-reward/hero-urgent), reduce the CTA breath to a one-shot pop (drop powerHeroCtaBreath, keep only ctaV2Pop) so the reward signal wins; when there is NO hot signal, let the CTA breathe. Add a body class (e.g. body.has-hot-hero) toggled in renderRotatingHeroCard and gate the CTA breath off under it in CSS. Result: at most ONE perpetual motion source at a time.
- [ ] **🟡 בינוני · מאמץ `M` · ★★ · רלוונטיות** — **Power-Hero categorized drawer is dead code in production (bottom-nav supersedes it)**
  - **הבעיה:** applyHeroVariant() short-circuits and RETURNS before building the categorized drawer whenever document.body has data-active-tab set (31-home-variants.js:350-361). The bottom-nav module sets data-active-tab on every mount (src/46-bottom-nav.js:351), so in shipped builds the drawer — including the whole DRAWER_CATEGORIES taxonomy + renderCategorizedDrawer (31:431-465) — NEVER renders. ~120 lines of categorization logic are unreachable. Not a player-facing bug, but it means the 'navigable menu vs flat wall' benefit described in the comments is actually delivered by bottom-nav tabs, not this code; the dead path is maintenance debt and a misleading source of truth for future edits.
  - 📍 **הוכחה:** `src/31-home-variants.js:350-361 (early return when bottomNavActive); src/46-bottom-nav.js:351 (sets data-active-tab on mount)`
  - 🔧 **לעשות:** Decide the owner of secondary-tile routing. Recommended: keep bottom-nav as the router and DELETE the now-unreachable drawer branch (collapser/toggle/renderCategorizedDrawer + DRAWER_CATEGORIES) from src/31-home-variants.js, keeping only collectHotSignals + renderRotatingHeroCard. Add a one-line comment that bottom-nav owns tile placement. Result: single source of truth, smaller bundle, no confusion for the next editor.
- [ ] **⚪ נמוך · מאמץ `S` · ★★★ · התמכרות** — **Empty CTA-sub + always-on glow makes the button feel generic vs. a live reward surface**
  - **הבעיה:** Because the CTA sub is empty (see finding 1) and the only motion is a generic gold glow, the dominant button reads as a static 'play' affordance rather than a live, rewarding surface. Best-in-class F2P (Royal Match 'Play', Coin Master spin) couple the primary CTA with a tangible micro-reward or count (energy/coins/streak). BLOOM already computes streak, daily-played, best, and tomorrow-reward in renderHeroBannerV2 (05a:985-1003) — none of it reaches the button.
  - 📍 **הוכחה:** `src/05a-home-v2.js:985-1003 computes tomorrowPreview/streak but only feeds the hero card; CTA stays bare`
  - 🔧 **לעשות:** Reuse the already-computed values from renderHeroBannerV2 (streak.count, todayPlayed, tomorrow reward) to drive both the CTA sub AND an optional tiny reward chip on the CTA (e.g. '+25💎 על המשחק הראשון היום' when daily not played). Keep it to one line. Result: the giant button itself becomes a dopamine surface, not just a navigation affordance.
- [ ] **⚪ נמוך · מאמץ `S` · ★★ · יופי** — **Micro-tutorial balloon uses innerHTML with a hardcoded high z-index and no token**
  - **הבעיה:** The first-visit micro-tutorial balloon (80-polish.js:163-203) is built with innerHTML, positioned with a hardcoded z-index:10500 (home-v2.css:2068) instead of the --z-modal/--z-toast tokens defined in base.css, and its gradient (#FFD93D→#F5AD2E) is an off-token color not matching the --color-accent palette used elsewhere on home. Minor inconsistency with the design-token system the rest of the home respects, and the fixed z-index can collide unpredictably with modals.
  - 📍 **הוכחה:** `src/80-polish.js:166 (innerHTML balloon), public/css/home-v2.css:2068 (z-index:10500 literal), :2076 (#FFD93D gradient off the accent palette)`
  - 🔧 **לעשות:** In public/css/home-v2.css, swap .polish-micro-tooltip z-index:10500 for var(--z-toast) (or a value just under --z-modal) and retint .polish-tooltip-body to the --color-accent / --color-accent-soft gradient so the balloon matches the home palette. Optionally rebuild the balloon via createElement+textContent in 80-polish.js to match the project's no-innerHTML-for-variable-content convention. Result: consistent stacking + brand-consistent tooltip.

---

## 16. Friends/Social system (unified hub + search + requests + notify + challenges + device sync) — ציון `70/100` (B)

> **הקשר:** social — friends hub modal (3 tabs) + online-friends banner + real-time friend pop-ups + friend challenges + cross-device sync
>
> **מימדים:** התמכרות **8** · יופי **6** · נוחות **6** · בהירות **8** · פוקוס **7** · סגירוּת **6** · רלוונטיות **7**

**פסק דין:** לולאה חברתית באמת ממכרת (פופ-אפ +200💎 בזמן-אמת, רשימה online-first, הזמנות K-factor) — נבלמת מ-✕ קטנים, חוסר-עקביות 4-פלטות, Friend Challenges קבורים, ו-poll של 45s. **כדי להעלות:** הגדל סגירות, עבור ל-tokens, הוסף badge-unread על כניסת-הבית, וקצר את ה-poll / הוסף נקודת-כניסה גלויה ל-Friend Challenges.

**✅ מה כבר טוב (אל תיגע):**
- Real-time friend pop-ups (51-friend-notify.js) are a genuinely strong social-validation dopamine loop: accept fires confetti via window.__bloomConfetti(28) + soundMilestone(6) + 'קיבלתם +200💎 כל אחד' and offers a one-tap ⚔️ אתגר rematch (respond() L221-246) — exactly the Clash-Royale 'accept friend = reward toast' emotion.
- FN.2 unification is real: showFriendsModal (05c L2299) renders search + requests INLINE via __bloomFriendSearch.renderSearchInto / renderRequestsInto (L2362-2371) inside one modal — no second stacked modal, and the banner/notify/deep-link all delegate to it on the correct tab (49 L34, 51 L257, 50 L66).
- Online-first sorting + an 'alive' header ('🟢 N פעילים עכשיו · אתגר אותם!', 05c L2442) plus per-friend ⚔️/🎯 one-tap actions makes the list immediately actionable rather than a static address book.
- K-factor invite is well-built: my-code + WhatsApp/native/copy share (05c L2324-2328) and the +200💎-for-both signup bonus is surfaced everywhere (banner empty-state, hub sub-line, accept toast).
- Security/robustness: all user-supplied names rendered via textContent or escaped (49 buildResultRow L230, 51 esc() L37, 05c escapeHtml); friend-notify banner has a prefers-reduced-motion guard (home-v2.css L5792); dark-mode parity is complete across social.css L499-571.
- dyn-friends-modal-overlay IS registered in the ESC + popstate close handler (04-ui-utils.js L924), and friend-search/device-sync/fc overlays all match the generic [class*='modal-overlay'] selector — so keyboard/back-gesture close works for the whole family.

**🛠️ משימות לביצוע (7):**

- [ ] **🟠 גבוה · מאמץ `M` · ★★★★ · התמכרות** — **Friend Challenges (39) have almost no discoverability — strong K-factor lever is buried**
  - **הבעיה:** Friend Challenges are the recursive viral lever (A challenges B → both get gems → revenge challenge), but the ONLY entry points are the 🎯 button per friend row inside the hub (05c L2497-2508) and an inbox click. There is no home tile, no badge for an incoming challenge, no count of active challenges, and openListModal is reachable only from inbox. A player with 3 friends never learns the feature exists, and an incoming 'beat 67K' challenge — a pure FOMO/competition hook — has no ambient surface. The activeIncomingTargets() helper (39 L298) exists to show a '🎯 לעבור: 67K' in-game pill but I see no caller wiring it into the mode-bar.
  - 📍 **הוכחה:** `src/39-friend-challenges.js:298-305 getActiveIncomingTargets exposed but no in-game mount visible; entry points limited to 05c:2497 friend-row 🎯 and inbox`
  - 🔧 **לעשות:** Add a 📨 incoming-challenge surface: (1) a home/community tile that mounts only when fetchChallenges() returns pending role==='challenged' items, showing '🎯 דניאל אתגר אותך: עבור 67K' with a 'שחק עכשיו' CTA; (2) wire activeIncomingTargets() into the in-game mode-bar as a live '🎯 לעבור: 67K מ-@דניאל' pill on daily/practice init so the target is visible mid-game (loss-aversion). Gate it the same as other social surfaces but make the incoming challenge impossible to miss.
- [ ] **🟡 בינוני · מאמץ `M` · ★★★★ · התמכרות** — **45s notify poll is too slow to feel 'real-time' and there is no badge on the home entry to pull the player in**
  - **הבעיה:** The pop-up poller runs every 45s (51 L19 POLL_MS) and only on visible tabs. A friend request that lands 5s after the player opens the app can take up to 45s to surface — the high-emotion moment is the moment the request arrives, and a 45s lag dulls it. There is also no persistent unread badge on a home surface; the only ambient signal is the slim banner (50) which is L5-gated and shows counts, not a red unread pill. Players who dismiss a pop-up have no breadcrumb back except remembering the friends hub exists.
  - 📍 **הוכחה:** `src/51-friend-notify.js:19 'var POLL_MS = 45000'; src/50-friends-banner.js:20 level gate L5+; no unread badge wired to a bottom-nav / home tab`
  - 🔧 **לעשות:** Drop POLL_MS to 20000 and additionally poll immediately on home-mount + after sending a request. Surface the unread incoming count (d.unreadIncoming, already returned) as a red pill on the bottom-nav קהילה tab or the friends-banner icon so a dismissed pop-up leaves a persistent 'you have N requests' hook. This converts a transient pop-up into a standing return-driver.
- [ ] **🟡 בינוני · מאמץ `S` · ★★★ · סגירוּת** — **All social close buttons are 30-32px — below the 40px tap-target / UX-gate threshold**
  - **הבעיה:** Every close ✕ in the social family is undersized: dyn-friends-modal-close is 32×32 (boards.css L2356), friend-search-close 32×32 (social.css L228-229), device-sync-close 32×32 (social.css L54-55), and fc-modal-close is only 30×30 (home-v2.css L3358). The UX gate (CLAUDE.md §10b Q4) demands a visible close ≥40px. On a phone these are hard to hit with a thumb, especially top-LEFT-anchored in an RTL layout where the thumb naturally rests bottom-right.
  - 📍 **הוכחה:** `boards.css:2356 'width: 32px; height: 32px'; home-v2.css:3358 'width: 30px; height: 30px'; social.css:54 'width: 32px'`
  - 🔧 **לעשות:** Bump all four close buttons to 40×40 (or 44×44 for iOS HIG). In boards.css .dyn-friends-modal-close, social.css .friend-search-close + .device-sync-close, and home-v2.css .fc-modal-close set width/height to 40px and add a min 44px hit area via padding or a transparent ::after expander. Keep the visual circle ~32px but expand the clickable region. Backdrop-tap + ESC already work, so this is the visible-affordance fix.
- [ ] **🟡 בינוני · מאמץ `M` · ★★★ · יופי** — **Zero design-token usage — palette/radius/shadow are hardcoded hex everywhere, and the four sub-surfaces clash**
  - **הבעיה:** None of the social CSS uses the base.css :root tokens (--color-accent, --radius-md, --shadow-glow, --z-modal). Every value is a raw literal, and the four surfaces use four unrelated gradients: green search head (#4FBD8B→#2E8B6F, social.css L222), pink hub head (#FF6B9D→#FFD86B, boards.css L2350), purple sync head (#7A5FE0→#FAC775, social.css L46), red challenge card (#FFFCF6→#FFEEEE, home-v2.css L3339). They are all 'the friends area' to the player but read as four different apps. Border-radius also drifts (16/20/22px) and z-index is hand-picked (10000/10038/9998) instead of token --z-modal.
  - 📍 **הוכחה:** `boards.css:2350 pink hub vs social.css:222 green search vs social.css:46 purple sync vs home-v2.css:3339 red challenge; radii 16px(social.css:28)/20px(boards.css:2340)/22px(home-v2.css:3340)`
  - 🔧 **לעשות:** Introduce a shared social palette: pick ONE family accent (the pink-gold hub gradient is the FN.2 canonical) and apply it to friend-search-head + the fc-modal-title accent so search/requests/challenges feel like one room. Migrate radii to var(--radius-lg), shadows to var(--shadow-md), and overlay z-index to var(--z-modal) from base.css :root. Keep device-sync purple (it's a deliberately distinct destructive flow) but align its radius/shadow tokens.
- [ ] **🟡 בינוני · מאמץ `M` · ★★★ · נוחות** — **'Accept from search' does a redundant 2-request round-trip and can silently say 'already handled'**
  - **הבעיה:** When a search result shows '🤝 אשר' (the other side already requested you), tapping it calls respondToIncomingFromDevice (49 L317) which first GETs the whole /api/friends/requests list, finds the matching request id by otherDevice, THEN POSTs request-respond. That's two sequential network hops for one tap, and if the list lookup misses (timing/pagination) the player gets a dead-end 'הבקשה כבר טופלה' toast (L332) with no friendship created — a confusing failure for what looked like a one-tap accept.
  - 📍 **הוכחה:** `src/49-friend-search.js:317-337 (respondToIncomingFromDevice fetches list then resolves id) → L330-336 fallback toast 'הבקשה כבר טופלה'`
  - 🔧 **לעשות:** Have the server /api/users/search return the pending requestId on rows where requestReceived is true, so buildResultRow can call respondToRequest(r.requestId,'accept',btn) directly (single POST). Remove the list-lookup hop in respondToIncomingFromDevice. Add an optimistic button state (spinner) and on the rare miss, re-run the search instead of dead-ending with a toast.
- [ ] **⚪ נמוך · מאמץ `M` · ★★★ · נוחות** — **Hub friends list shows no skeleton and no 'next-session' competitive hook (just status pills)**
  - **הבעיה:** The friends list loads with a bare '⏳ טוען חברים...' (05c L2340) — no skeleton rows, so on a slow connection the pane flashes empty then pops. More importantly for addiction: each friend row shows only a status pill (🟢 פעיל / ✓ שיחק היום / ⏰ לא פעיל, 05c L2451-2455) and ⚔️/🎯 buttons. There's no per-friend score / trophy / streak comparison — the single strongest 'return tomorrow to beat my friend' hook is absent. The player can't see 'דניאל passed your best today' inside the hub.
  - 📍 **הוכחה:** `src/05c-dynamic-boards.js:2340 plain loading text; L2446-2479 friend rows carry only name+code+status, no comparative number`
  - 🔧 **לעשות:** In renderFriendsList (05c L2421) add a shimmer skeleton (reuse .home-skeleton-grid pattern) while fetchFriends resolves. Then enrich each friend row with one comparative number returned by /api/friends/list — e.g. today's best score or trophy delta — rendered as a '👑 47K · +3K מעליך' or '↓ אתה מוביל' chip. This turns the address book into a daily rivalry board, the core friends retention lever.
- [ ] **⚪ נמוך · מאמץ `S` · ★★ · סגירוּת** — **Unified-hub close button isn't .modal-close — relies on element.remove() fallback, losing pane-load/focus cleanup**
  - **הבעיה:** The ESC handler (04-ui-utils.js L969-996) tries .modal-close / .info-close / [id$=modal-close] / [aria-label='סגור'] before falling back to element.remove(). The hub's button is class dyn-friends-modal-close (not .modal-close) but DOES have aria-label='סגור' (05c L2310) so ESC works via that path — fine. However the standalone fallback path in 49 (showModal) and the search input's setTimeout focus (49 L188) mean a remove()-only teardown skips the closeFriendsModal cleanup. Minor, but the inconsistency (some closers wired to a named fn, some to remove()) is a latent bug surface if cleanup logic is added later.
  - 📍 **הוכחה:** `src/05c-dynamic-boards.js:2310 class 'dyn-friends-modal-close' with aria-label 'סגור'; 04-ui-utils.js:996 fallback element.remove()`
  - 🔧 **לעשות:** Add the .modal-close class to dyn-friends-modal-close, friend-search-close, device-sync-close and fc-modal-close so the ESC/back handler always hits the intended close path (and future cleanup runs). Purely additive — keep existing aria-label. Ensures consistent teardown across the whole social family.

---

## 17. Admin dashboard (owner control center) — ציון `71/100` (B)

> **הקשר:** Owner-facing single-file RTL admin, 8 tabs (dashboard/game/economy/players/contests/monitor/issues/debug)
>
> **מימדים:** התמכרות **8** · יופי **7** · נוחות **5** · בהירות **7** · פוקוס **6** · סגירוּת **8** · רלוונטיות **8**

**פסק דין:** קונסולת-בעלים רבת-עוצמה עם שליטה אמיתית — ה-engagement overview וה-toggles נוגעים ב'אדמין שולט בכל מנוף' — אבל נבלמת מקיר-config בלי-חיפוש (200+ מפתחות), jump-cards שמובילים למבוי-סתום, תווית '42 מפתחות' מיושנת, ויעדי-מגע <44px. **כדי להעלות:** הוסף חיפוש/סינון/קיבוץ-קטגוריות לטבלת ה-config, גרום ל-jump-cards לפתוח+לגלול לקבוצה הנכונה, עדכן את התווית, והגדל יעדי-מגע.

**✅ מה כבר טוב (אל תיגע):**
- Engagement Overview (loadEngagementOverview, admin/index.html:4891) is a genuine 'mission control': 22 addiction systems rendered as live status cards (Trophy/Spin/Guild War/Rivalry from stat endpoints + 18 more via /api/config enabled-flags) with click-to-jump that switches tab + smooth-scrolls (4997-5016). This is exactly the 'admin controls every addiction lever' prime directive.
- Config editor cells (renderConfigValueCell, admin/index.html:9088) are best-in-class for a no-build admin: boolean keys render as ✓פעיל/✗כבוי toggle pairs with strong colored on-states + box-shadow rings (CSS 230-231), numeric keys get one-tap preset chips (204 keys mapped in PRESETS_PER_KEY), and free-form keys get input+save. All auto-save via one shared persistConfigValue (9118) with toast feedback.
- Rich per-key tooltips (TIPS_PER_KEY, admin/index.html:3281) with concrete examples, economic math, and warnings — 109 data-tip surfaces total. The owner genuinely understands what each knob does before touching it.
- Full closability: global ESC + backdrop-click handler covers all ~10 inline editor modals (admin/index.html:9391-9419), tab state persists to localStorage (9348), and lazy-load per tab (loadTab, 4867) keeps it responsive.
- Dark-mode parity is handled for the core config UI via html[data-theme=dark] overrides (242-247) AND the page respects @media prefers-color-scheme:dark.

**🛠️ משימות לביצוע (7):**

- [ ] **🟠 גבוה · מאמץ `M` · ★★★★★ · נוחות** — **No search/filter on the 200+ key config table — finding one knob means scrolling a wall**
  - **הבעיה:** The 'כל ההגדרות' table (btn-load-all-config, admin/index.html:9166) renders EVERY game_config row (204+ preset-mapped keys per PRESETS_PER_KEY, far beyond the legacy count) as one flat table with zero search, filter, or category grouping. To change e.g. 'bot_duel_player_win_rate_pct' the owner must visually scan hundreds of monospace LTR keys inside a collapsed <details>. This is the single biggest friction point in the whole owner workflow.
  - 📍 **הוכחה:** `admin/index.html:9176-9192 builds one <table> by iterating all rows with no filter UI; the only gate is r.key.startsWith('_'). No <input> for search exists in the config-table-host region (line 966-968 is just the load button).`
  - 🔧 **לעשות:** In btn-load-all-config.onclick (admin/index.html:9166), before building the table inject a sticky search input (placeholder 'חיפוש מפתח… (לדוגמה: bot, spin, reward)') plus 4-6 category filter chips derived from key prefixes (event_/bot_/spin_/season_/dyn_/gacha_ etc.). Wire an input listener that hides/shows <tr> rows by case-insensitive match on the key text + the tooltip text. Style the search box with --radius-md and the chips like .cfg-preset-btn. Result: owner types 'win_rate' and instantly sees the 2-3 relevant keys instead of scrolling 200.
- [ ] **🟠 גבוה · מאמץ `M` · ★★★★ · בהירות** — **Engagement jump-cards dump you at a collapsed, unloaded config section**
  - **הבעיה:** 18 of the 22 engagement cards (Gacha, Daily Deals, Battle Pass, League, Daily Tournament, Bot Fleet, etc.) have jump:'config-section' (admin/index.html:4962-4973). Clicking 'גאצ׳ה סקינים' switches to the 🎮 tab and scrolls to the config section header — but the giant settings table is inside a collapsed <details> AND requires a separate 'טען הגדרות' button click, and the jump does NOT pre-filter to that system's keys. The owner is promised 'jump to this system' but lands on a closed drawer with no obvious next step. The promise of mission-control breaks at the click.
  - 📍 **הוכחה:** `jump targets at admin/index.html:4962-4973 all point to 'config-section'; the card click handler (4997-5016) only switchAdminTab + scrollIntoView, it never opens the <details> (964) nor triggers btn-load-all-config (967) nor filters keys.`
  - 🔧 **לעשות:** In the eng-card click handler (admin/index.html:4997), when jumpId==='config-section': also force-open the <details> (set .open=true), click btn-load-all-config if the table isn't loaded yet, and — if you add the search box from the prior finding — pre-fill it with that system's prefix (pass a card.searchPrefix like 'gacha_'). Result: clicking 'Gacha' lands directly on the gacha keys, expanded and filtered, in one tap.
- [ ] **🟡 בינוני · מאמץ `S` · ★★★ · בהירות** — **Stale '(42 מפתחות)' label massively undercounts the real config surface**
  - **הבעיה:** The settings drawer summary reads '⚙️ כל ההגדרות (42 מפתחות)' but there are 200+ live config keys (204 mapped in PRESETS_PER_KEY alone, plus dozens of bool/free-form keys). The owner is told there are 42 settings when there are hundreds — undermines trust and hides how much control actually exists.
  - 📍 **הוכחה:** `admin/index.html:965 hardcodes '(42 מפתחות)'; PRESETS_PER_KEY (2891) maps 204 keys and TIPS_PER_KEY (3281) covers many more.`
  - 🔧 **לעשות:** At admin/index.html:965 remove the hardcoded count from the static summary and instead set it dynamically after btn-load-all-config loads: count rows.filter(r=>!r.key.startsWith('_')).length and write it into the summary text (e.g. 'כל ההגדרות (187 מפתחות)'). Until loaded, show 'כל ההגדרות' with no number. Result: the label never lies and reflects the true breadth of owner control.
- [ ] **🟡 בינוני · מאמץ `S` · ★★★ · נוחות** — **Tap targets too small for thumb use — config toggles/chips ~22px, 8 cramped tabs on mobile**
  - **הבעיה:** The owner manages this on a phone. Config toggle buttons are padding 5px 8px / 11px font (CSS admin/index.html:225) and preset chips similar — roughly 22px tall, well under the 44px thumb-target guideline, so mis-taps on adjacent ✓פעיל/✗כבוי are easy and risky (one wrong tap flips a live global config for all players). The 8-tab nav at 12px font / 8px 12px padding on ≤720px (612-615) crams 8 RTL tabs into a horizontal scroll with no scroll affordance.
  - 📍 **הוכחה:** `admin/index.html:225 (.cfg-toggle-btn padding:5px 8px;font-size:11px), 234 (.cfg-preset-btn), 612-615 mobile tab sizing, 600-605 base tab.`
  - 🔧 **לעשות:** Add a mobile @media (max-width:720px) block bumping .cfg-toggle-btn / .cfg-preset-btn to min-height:40px and font-size:13px so live-config toggles are thumb-safe. For .admin-tabs, add a subtle right-edge fade/gradient mask (or a small scroll hint) so the owner knows the 7th/8th tab scrolls. Result: fewer accidental config flips, easier tab reach on a phone.
- [ ] **🟡 בינוני · מאמץ `S` · ★★★ · יופי** — **Issues-tab stat cards hardcode background:#FFF — break under OS dark mode**
  - **הבעיה:** The 🚨 תקלות stat cards (open/critical/high/24h/compensated) use inline 'background:#FFF' and the grid is a fixed repeat(5,1fr). Under @media (prefers-color-scheme:dark) the page chrome goes dark but these five cards stay white slabs (jarring, off-theme), and five 22px numbers in 5 fixed columns squash badly on a narrow phone.
  - 📍 **הוכחה:** `admin/index.html:2533 (grid-template-columns:repeat(5,1fr)) and 2534/2538/2542/2546/2550 each set style='...background:#FFF...' inline, bypassing --card and the dark overrides used elsewhere.`
  - 🔧 **לעשות:** Replace the inline background:#FFF on the 5 issues stat cards (admin/index.html:2534-2550) with background:var(--card) so they follow the theme, and change the grid (2533) to grid-template-columns:repeat(auto-fit,minmax(90px,1fr)) so it wraps to 2-3 rows on phones instead of crushing. Result: the most-checked owner triage surface is readable in dark mode and on mobile.
- [ ] **⚪ נמוך · מאמץ `S` · ★★ · יופי** — **Animations have no prefers-reduced-motion guard**
  - **הבעיה:** The tab-fade keyframe and eng-card hover transform run unconditionally. Best-in-class admin UIs respect the OS reduced-motion setting; there's no guard, so an owner who set reduced-motion still gets the translateY fade on every tab switch and lift on every card hover.
  - 📍 **הוכחה:** `admin/index.html:610-611 (.admin-tab-panel.active animation tabFadeIn + @keyframes) and 99-103 (.eng-card:hover transform/box-shadow) with no @media (prefers-reduced-motion:reduce) override anywhere in the file.`
  - 🔧 **לעשות:** Add a single @media (prefers-reduced-motion:reduce) block that sets .admin-tab-panel.active{animation:none} and .eng-card{transition:none} (and disables :hover transform). Place near the existing keyframes (admin/index.html:611). Result: respects OS accessibility preference, matches the polish bar set elsewhere in the codebase (the app already guards reduced-motion).
- [ ] **⚪ נמוך · מאמץ `M` · ★★ · נוחות** — **Dashboard has 6 stacked 'טוען…' sections with no skeletons and a generic 'שגיאת טעינה' dead-end**
  - **הבעיה:** The default dashboard tab loads 6 heavy async blocks (engagement, KPIs, DAU spark, funnel, retention, heatmap) that each show a bare 'טוען…' string then either content or 'שגיאת רשת'/'שגיאת טעינה'. On a slow connection the owner stares at 6 plain-text 'loading' lines with no shape, and a failed fetch leaves a dead-end string with no retry button.
  - 📍 **הוכחה:** `admin/index.html:2373/2381/2390/2398/2407/2416 all render '<div class="loading">טוען…</div>'; loadEngagementOverview error path (4988) writes '<div class="loading">שגיאת טעינה</div>' with no retry.`
  - 🔧 **לעשות:** Give the dashboard loaders shimmer skeletons (reuse a .loading::after gradient sweep) instead of bare text, and on any catch render a compact 'שגיאת רשת · נסה שוב' with a retry button that re-calls the loader (e.g. wrap loadEngagementOverview's error path at admin/index.html:4988). Result: faster perceived load and no dead-ends when a stat endpoint hiccups.

---

## 18. In-game gameplay UI — ציון `72/100` (B)

> **הקשר:** in-game — the live 4x6 grid, tier-bar ladder, mode chip, stats row, balance, danger meter, clutch-save banner, above-best pill
>
> **מימדים:** התמכרות **8** · יופי **6** · נוחות **5** · בהירות **8** · פוקוס **7** · סגירוּת **7** · רלוונטיות **9**

**פסק דין:** HUD חי ובאמת ממכר (דלתא מעל-שיא, מד-סכנה, slot-reveal, clutch-save) עם משמעת-שטח טובה — נבלם מ׳יפה ונוח׳ ע״י אומנות-אריח קטנה מדי בתאים גדולים, תנועה שמתעלמת מ-reduced-motion תחת לחץ, וצבעים קשיחים. **כדי להעלות:** הגדל את ה-SVG מ-30px ל-~55% מגודל התא הדינמי, הוסף reduced-motion, החלף ל-tokens, והוסף כפתור יציאה/בית גלוי.

**✅ מה כבר טוב (אל תיגע):**
- Strong dopamine layering on the live HUD: above-best delta pill with 3 escalating tiers (hot/huge) at src/12-tour-info.js:510-534, live new-best swap, near-best gold pulse, and a tactical danger meter ('🚨 2 תאים' / 'תא אחרון!') at src/11-game.js:3336-3374 — these are genuine 'one more drop' levers, not decoration.
- The slot-machine tier reveal (revealNextTier, src/12-tour-info.js:311-379) with ease-in-quad deceleration + cubic-bezier landing bounce (slotLand, base.css:734) is high-quality juice and is admin-tunable (slot_enabled/duration/intensity) — beautiful AND controllable.
- Excellent screen-real-estate discipline: .mode-bar is hidden during play and replaced by a compact .mode-chip in the top row, and dynamic-board target/leader chips are mirrored into a thin #mode-extras strip (syncModeExtrasStrip) so the grid gets maximum cell size — the documented BN.1/TB.1 work paid off.
- Clutch-save falling-edge celebration (showClutchSaveBanner, src/11-game.js:3380-3413) with a rising relief tone deliberately inverse of the danger-enter cue, plus a 5s anti-oscillation cooldown and an empties<8 guard so continue-ad recoveries don't fake-trigger it — thoughtful, addictive, and not spammy.
- fitGrid (src/06-contests.js:1604-1643) correctly constrains the grid on BOTH width and height axes for square cells across browsers, with self-healing gravity-violation detection at render time (src/12-tour-info.js:1394-1415).

**🛠️ משימות לביצוע (7):**

- [ ] **🟠 גבוה · מאמץ `S` · ★★★★ · נוחות** — **Persistent gameplay animations ignore prefers-reduced-motion → discomfort under pressure**
  - **הבעיה:** The danger aura (dangerPulse) and the empty-cell red wash (dangerCellPulse) pulse continuously at 1.1s forever while the player is in the highest-stress moment of the game, and the above-best pill (aboveBestPulse/aboveBestPulseHuge) pulses+rotates indefinitely. Only .danger-meter (base.css:915) and .home-win-banner have a reduced-motion guard; the much more aggressive full-screen aura, cell-flicker, and pill rotation are unguarded. There is no global reduced-motion reset. Motion-sensitive players get a flashing red board with zero relief — a real nausea/eye-strain risk and an uncomfortable ('לא נוח') experience.
  - 📍 **הוכחה:** `base.css:856-875 (dangerCellPulse/dangerPulse infinite, no rm guard), base.css:506+517+528 (aboveBestPulse* infinite), base.css:915-917 only guards .danger-meter; grep shows NO global '@media (prefers-reduced-motion)' wildcard reset in base.css.`
  - 🔧 **לעשות:** In public/css/base.css add the heavy infinite gameplay animations to a prefers-reduced-motion:reduce block: set `body.danger-mode .grid-wrap::after`, `body.danger-mode .cell:not(.filled)`, `.above-best-pill`, `.above-best-pill-hot`, `.above-best-pill-huge`, and `.near-best` to `animation: none`. For danger mode, keep a STATIC red border + static red tint on empty cells (so the danger signal is preserved without flicker). This keeps the loss-aversion lever intact while removing the discomfort.
- [ ] **🟠 גבוה · מאמץ `S` · ★★★★ · יופי** — **Tile SVG is a fixed 30px inside a fitGrid cell that grows to ~60-70px → small, lonely glyphs that waste the board**
  - **הבעיה:** .cell.filled svg is hardcoded to 30×30px (base.css:834) but fitGrid sizes cells dynamically to ~60-70px on a typical phone — so the tile art occupies under half the cell, leaving a large empty colored square around each glyph. The board reads as sparse and toy-like rather than full and juicy, undercutting the 'beautiful' goal. On small phones (<360px) the icons stay 30px too, so the ratio is inconsistent across devices.
  - 📍 **הוכחה:** `base.css:834 `.cell.filled svg { width: 30px; height: 30px; }` is a fixed px; fitGrid computes cell from viewport (src/06-contests.js:1616-1620) yielding much larger cells; no responsive scaling of the SVG to cell size.`
  - 🔧 **לעשות:** Make the tile SVG scale with the cell. In src/06-contests.js fitGrid(), after computing `cell`, set a CSS var on #grid (e.g. `grid.style.setProperty('--cell-svg', Math.round(cell*0.62)+'px')`) and change base.css:834 to `.cell.filled svg { width: var(--cell-svg, 38px); height: var(--cell-svg, 38px); }`. Target ~60-65% of cell width so tiles read as full and substantial. Verify the tier-bar mini icons stay at their own 32px (they have their own selector .tier-icon svg at base.css:714).
- [ ] **🟡 בינוני · מאמץ `S` · ★★★ · סגירוּת** — **No on-screen exit/home control during play — only an icon-buried ⋯ menu**
  - **הבעיה:** In the default (non-legacy) game UI the only persistent top controls are mute, leaderboard, and ⋯ (topmore); the home/back/reset buttons are class `icon-btn-legacy-only` and hidden (index.html:99-110). A player mid-game who wants to bail to home must discover the ⋯ popover. There is no large, obvious back affordance and (per project notes) the global ESC/back handler targets modals, not the in-game surface. For a phone player this is a soft dead-end on the most-used screen.
  - 📍 **הוכחה:** `index.html:98-110 home-btn/reset are `.icon-btn-legacy-only` (hidden by default per base.css:289-290 legacy gating pattern); top-buttons in default mode = mute + leaderboard + topmore only (index.html:88-97).`
  - 🔧 **לעשות:** Add a persistent, ≥40px exit affordance to the in-game top row (e.g. a small ← / 🏠 icon-btn always visible, not legacy-only) wired to showHome(), OR ensure the mode-chip's picker exposes a clear 'יציאה למסך הבית' first option. Confirm a browser-back gesture also routes to home from in-game (add the in-game surface to the popstate handler in src/04-ui-utils.js if not covered). Goal: one obvious way out from the gameplay screen.
- [ ] **🟡 בינוני · מאמץ `M` · ★★★ · יופי** — **HUD widgets are hand-tuned hex colors, not design tokens → visual drift from the rest of the app**
  - **הבעיה:** The entire gameplay HUD uses ad-hoc literals instead of the base.css :root tokens (--color-accent, --radius-md, --shadow-glow, --z-floating). Score card is `#1C1A18→#2C2A28`, balance is inline `color:#BA7517;font-size:15px` in index.html, danger meter is `#E84660→#B91C3C`, tier-cell radius 9px, stat radius 12px, mode-chip its own gold gradient — none reference tokens. Radii are inconsistent (9 / 10 / 12 / 999). This makes the most-viewed screen drift from the token system the project deliberately built (TC.2), and makes a global re-theme impossible without hunting literals.
  - 📍 **הוכחה:** `index.html:124 inline `style="color:#BA7517;font-size:15px"`; base.css:894 danger-meter literal gradient; base.css:709 tier-icon radius 9px vs base.css:473 stat radius 12px vs base.css:828 cell radius 10px; only .danger-meter z-index uses var(--z-floating) (base.css:893) while everything else is bare.`
  - 🔧 **לעשות:** Migrate the gameplay HUD to tokens. Replace the inline balance style in public/index.html:124 with a class. In base.css, map the score/best/streak/shop cards and tier cells to `--radius-md`, accent colors to `--color-accent`/`--color-accent-ink`, and danger surfaces to `--color-danger` (add `--color-danger` to :root if missing). Standardize stat/cell/tier-icon radius on a single token. Keep the dark-theme overrides but source them from tokens too. Result: one re-theme point, consistent corner language across the board.
- [ ] **🟡 בינוני · מאמץ `M` · ★★★ · פוקוס** — **Four equal-weight stat cards dilute the score; 'רצף'+'💎 חנות' compete with the dopamine center**
  - **הבעיה:** The stats row renders four cards — ניקוד (primary, flex:2), שיא, רצף, 💎 חנות (base.css/index.html:120-125). The score is correctly enlarged, but שיא/רצף/חנות are nearly identical-weight pale cards sitting beside it, and the shop card is a tappable monetization surface placed inside the live HUD. During an intense run the eye has to parse 4 boxes; the highest-emotion number (current score) shares the top band with a buy-prompt. This slightly violates the 'one dominant action / don't bury the signal' gate and the 'no buy prompt where it competes with play' principle.
  - 📍 **הוכחה:** `index.html:120-125 four .stat cards incl. `stat-shop id=tile-shop-stat cursor:pointer`; base.css:551-556 primary is flex:2 but base.css:470-478 the other three are flex:1 with the same surface/radius; shop card is a live tappable surface in the HUD.`
  - 🔧 **לעשות:** Increase visual hierarchy: de-emphasize שיא/רצף into a smaller secondary tier (smaller font, more muted background, or merge רצף into a tiny inline chip), and visually demote the 💎 shop card (smaller, lower-contrast) so the dark score card unambiguously dominates. Consider moving the shop tap behind a small 💎 pill rather than a full equal-size card so the top band reads as 'score + minor context' not '4 competing widgets'. Keep all data accessible — just rebalance weight.
- [ ] **⚪ נמוך · מאמץ `S` · ★★ · נוחות** — **Danger meter top position computed from offsetTop with no resize/scroll re-anchor → can drift**
  - **הבעיה:** paintDangerMeter anchors the '🚨 N תאים' pill via `el.style.top = wrap.offsetTop - 14` (src/11-game.js:3370), set only when paint runs. grid-wrap is overflow-y:auto and the live-race path shifts padding-top; on an address-bar show/hide or an internal scroll between paints the pill can sit at a stale Y. It also relies on toggling host.style.position to relative imperatively (src/11-game.js:3364) which mutates layout state. The pill is the key 'survive in N moves' signal, so a misplaced/stale pill at the climactic moment hurts the experience.
  - 📍 **הוכחה:** `src/11-game.js:3370 inline top from offsetTop; the existing resize listener (src/06-contests.js:1646) calls fitGrid only, not paintDangerMeter; paint is driven by updateDangerMode which fires on game events, not on scroll/resize.`
  - 🔧 **לעשות:** Make the danger meter resilient: either (a) repaint it from the existing window resize/orientationchange listener by calling updateDangerMode()/paintDangerMeter() there, or (b) anchor it with a stable CSS approach — mount it as a position:absolute child of #grid-wrap pinned to its top via top:6px (grid-wrap is already position:relative) and rely on the established overflow fix, removing the offsetTop math. Re-test in a live race where the grid fills to 1 cell.
- [ ] **⚪ נמוך · מאמץ `S` · ★★ · בהירות** — **Practice difficulty cue ('⊘' / '🏆') uses tiny inline 11px text with title-tooltip-only meaning — low clarity & not thumb-friendly**
  - **הבעיה:** In free-play, whether the run counts toward the leaderboard is conveyed by a bare '⊘' (muted) or '🏆' glyph at 11-12px with the explanation hidden in a hover title (src/11-game.js:539-541). On a phone there's no hover, so a player has no idea why their score isn't on the board. The difficulty chip itself is fine, but the leaderboard-eligibility signal is effectively invisible and unexplained on touch.
  - 📍 **הוכחה:** `src/11-game.js:539-541 `<span title="לא נספר ללוח המובילים" ...11px>⊘</span>` — meaning lives only in the title attribute, no visible label, font 11px.`
  - 🔧 **לעשות:** Make the eligibility state self-explanatory and tappable: replace the bare glyph with a small labeled pill ('🏆 נספר' / '⊘ לא נספר') next to the difficulty chip, and on tap show a 1-line toast/tooltip explaining that non-default difficulty doesn't submit to the global leaderboard. Bump to ≥12px and ensure a ≥40px tap target if it's interactive. Keep the existing .practice-diff-chip styling language for consistency.

---

## 19. Daily Rewards cluster (Spin + Login Calendar + Checklist + Daily Deal) — ציון `72/100` (B)

> **הקשר:** 🎁 פרסים tab — daily-return engine. Audited 4 surfaces as one cluster: Daily Spin wheel (SVG weighted, src/32-daily-spin.js), 7-day Login Calendar (src/41-login-cal.js), Daily Checklist + 30-day calendar (src/21-calendar.js), rotating Daily Deal (src/18-daily-deals.js).
>
> **מימדים:** התמכרות **8** · יופי **6** · נוחות **6** · בהירות **8** · פוקוס **7** · סגירוּת **6** · רלוונטיות **9**

**פסק דין:** ה-hooks הכי טובים של חזרה-יומית (גלגל משוקלל, jackpot יום-7, deal מעוגן) עטופים ב-4 ממגורות לא-תואמות. **כדי להעלות:** תקן את מבוי ה-ESC ב-login-cal, הוסף reduced-motion+dark-mode, הגדל כפתורי-סגירה, ואחד את הארבעה ל-hub יומי אחד עם זהות אחת.

**✅ מה כבר טוב (אל תיגע):**
- Genuinely best-in-class daily-return hooks: the spin wheel is a real weighted SVG with rarity-aware strokes/sparkles on jackpot/legendary slices (src/32-daily-spin.js:157-211), a streak bonus that escalates +%/day with a clear 'spin again tomorrow for +X%' next-hint (lines 128-131), and 4-tier escalating reveal juice — confetti count + soundMilestone + buzz scale by reward tier, jackpot getting 32 particles + soundMilestone(7) (lines 305-322).
- 7-day Login Calendar nails loss-aversion: day-7 jackpot is visually crowned (👑), the tile surfaces an explicit '⚠️ פספסת יום! המסלול יתחיל מ-1' reset warning (src/41-login-cal.js:67-68), and claim is manual (Clash Royale 'feels earned' pattern) with a jackpot celebration (5.5s hold, 50 confetti).
- Daily Deal anchoring is textbook: absolute 'חסכת N💎' badge (more visceral than %), a '×N ערך!' value-multiplier sticker when ≥3× (src/18-daily-deals.js:160-168), live countdown ticker with last-hour urgency state, strikethrough original value, and a clean insufficient-funds state ('💎 חסר N💎') instead of a dead button.
- Relevance gating is disciplined: spin L12+, login-cal L5+, checklist L5+, deal L8+ — new players never see paid offers or empty lists. Deal banner self-hides when purchased; countdown tickers self-clear when the node leaves the DOM. The IS.5 close-button overlap was correctly resolved (multiplier badge moved to top-right, src/css/home-v2.css:2402-2406).
- Checklist drives completionist behavior with a server-verified all-done bonus, a robust race-safe claim flow (only marks claimed + fires confetti AFTER the server confirms the credit, src/21-calendar.js:152-178), and each item deep-links to its action (gacha/boards/deal).

**🛠️ משימות לביצוע (7):**

- [x] ✅ **בוצע (סבב 1)** · **🟠 גבוה · מאמץ `S` · ★★★ · סגירוּת** — **Login Calendar modal can't be closed with ESC or back-gesture**
  - **הבעיה:** The login-cal modal uses class 'login-cal-overlay' which does NOT contain the 'modal-overlay' substring the global ESC handler matches on (src/04-ui-utils.js:919), and it is NOT in the curated extras allowlist (lines 922-936). So unlike its three siblings (spin/deal/calendar all match '*modal-overlay*'), the login calendar can only be closed via the 32px ✕ or a backdrop tap — ESC and the mobile back-gesture do nothing, which is a dead-end on Android where back-swipe is the primary dismiss gesture.
  - 📍 **הוכחה:** `src/41-login-cal.js:91 sets className='login-cal-overlay'; src/04-ui-utils.js:919 matches [class*="modal-overlay"]; grep for 'login-cal-overlay' in the curated extras (04-ui-utils.js lines 922-936) returns nothing.`
  - 🔧 **לעשות:** In src/04-ui-utils.js, add '.login-cal-overlay' to the curated `extras` querySelectorAll string (around line 925, next to '.gem-bank-overlay'). Confirm it is NOT in the EXCLUDE map (only the celebration overlay 'login-cal-claim-overlay' should be excluded). Result: ESC + back-gesture close the login calendar like every other modal in the cluster.
- [ ] **🟡 בינוני · מאמץ `M` · ★★★ · נוחות** — **No prefers-reduced-motion coverage for the spin wheel, celebrations, or confetti**
  - **הבעיה:** The cluster is animation-heavy — a 4.2s wheel rotation with 14 tick sounds, an infinite jackpot glow loop (spinRewardJackpotGlow), the streak-flame loop, login-cal claim confetti (up to 50 particles), checklist all-done confetti (28), and ribbon pulses — yet the only prefers-reduced-motion block in home-v2.css (line 5792) covers ONLY the friend-notify banner, and dark.css has zero. Motion-sensitive users (and the project's own UX standard, which cites prefers-reduced-motion elsewhere) get no relief on the loudest screens in the app.
  - 📍 **הוכחה:** `grep prefers-reduced-motion: home-v2.css has 1 block (line 5792, friend-notify only), boards.css has 2 (ds-xp-chip + a block at 5215), dark.css has 0. None reference spin-wheel-rotate, spin-reward-card-jackpot, spin-confetti, login-cal-conf, cl-conf, or spinStreakFlame.`
  - 🔧 **לעשות:** Add a @media (prefers-reduced-motion: reduce) block (append to public/css/home-v2.css and the deal/calendar rules in boards.css) that: sets .spin-wheel-rotate transition to a fast 0.4s linear (still lands on the segment so the result reads, but no long spin), disables infinite loops (.spin-reward-card-jackpot, .spinStreakFlame, .dd-modal-ribbon spRibbonPulse, .dd-modal-icon) via animation:none, and hides/static-renders confetti spans (.spin-confetti, .login-cal-conf, .cl-conf { display:none }). Keep the reward card/value visible so reduced-motion users still see what they won.
- [ ] **🟡 בינוני · מאמץ `M` · ★★★ · יופי** — **Daily Deal & Calendar modals have no dark-mode treatment — bright flash on a dark theme**
  - **הבעיה:** dark.css contains zero overrides for any of the four cluster surfaces. The spin and login-cal modals are fine (intrinsic dark gradients), but the Daily Deal card is a LIGHT cream gradient (#FFE5E5→#FFEED4 with dark-brown text, boards.css:4592) and the calendar modal is light cyan — on a dark theme these render as harsh white-bright panels that break parity with the rest of the app and strain the eye at night (the prime daily-return window).
  - 📍 **הוכחה:** `grep of public/css/dark.css for spin-modal-card / login-cal-card / daily-deal-modal-card / dd-modal / calendar-modal-card / cal-day-card returns 0 matches. boards.css:4591-4592 shows the light deal card; boards.css:5661-5665 shows the light cyan calendar close/title.`
  - 🔧 **לעשות:** Add html[data-theme="dark"] overrides in public/css/dark.css for .daily-deal-modal-card (dark surface gradient ~#3A1A1A→#2A1505 with light text, keep the red accent border), .dd-modal-title/.dd-modal-sub/.dd-modal-item-* (light text), .calendar-modal-card + .cal-day-card + .cal-event-row (dark surfaces, keep the per-category accent dots), and .checklist-home-tile if it's light. Verify against the existing dark balance-bar palette so the cluster matches the rest of the tab in dark mode.
- [ ] **🟡 בינוני · מאמץ `L` · ★★★ · פוקוס** — **Cluster is four visual silos with no unified 'daily rewards' identity or cross-linking**
  - **הבעיה:** The tab's purpose is the daily-return engine, but it ships as four independent surfaces with four different visual languages (purple wheel, purple-but-different login calendar, cyan checklist, red deal) and four separate entry points (3 tiles + 1 banner). Critically, the Checklist — whose literal job is 'today's to-do list' — does NOT include 'spin the wheel' or 'claim login day' as items, even though those are the highest-value daily actions. The result is a tab where the player can miss the spin or the day-7 jackpot because nothing aggregates them, and the shared 'this is your daily dopamine' message is diluted across mismatched palettes.
  - 📍 **הוכחה:** `src/21-calendar.js handleChecklistAction (lines 209-224) only routes open_gacha/open_dynamic_boards/open_daily_deal/start_game — no spin or login-cal action. Each surface mounts its own tile/banner independently (32-daily-spin.js:44 mountSpinTile, 41-login-cal.js:49 tile, 21-calendar.js:99 tile, 18-daily-deals.js:62 banner) with hardcoded distinct gradients.`
  - 🔧 **לעשות:** Two-part: (1) In src/21-calendar.js add 'spin_wheel' and 'login_cal' checklist actions (server-side checklist items + handleChecklistAction cases that call window.showSpinModal() / window.__bloomLoginCal.open()) so the checklist becomes the single daily hub that nags + routes to every reward. (2) Establish a shared daily-rewards accent: pull a common gold accent (use --color-accent / --shadow-glow tokens from base.css) for all four tile headers so the cluster reads as one system instead of four. Result: one obvious 'do your dailies' anchor, fewer missed jackpots.
- [ ] **🟡 בינוני · מאמץ `S` · ★★ · נוחות** — **All four close buttons are below the 40px ergonomic tap target**
  - **הבעיה:** Spin ✕ = 36px (home-v2.css:1019), login-cal ✕ = 32px (home-v2.css:4017), daily-deal ✕ = 32px (boards.css:4610), calendar ✕ = 30px (boards.css:5663). All sit below the 40px minimum the project's own UX gate (Q4) mandates for a visible close. On a phone these are fiddly to hit, especially top-left in an RTL layout where the thumb has to reach across.
  - 📍 **הוכחה:** `home-v2.css:1019 (36px), home-v2.css:4017 (32px), boards.css:4610 (32px), boards.css:5663 (30px).`
  - 🔧 **לעשות:** Bump all four close buttons to width/height:40px (or 44px to match iOS HIG) and proportional font-size in their respective CSS rules. Keep the circular shape and current top-left anchor. Result: every modal in the cluster passes the ≥40px close-target gate; touch dismissal becomes reliable.
- [ ] **⚪ נמוך · מאמץ `S` · ★★ · סגירוּת** — **Spin reward overlay has only one exit (the OK button) — no backdrop tap, no ✕**
  - **הבעיה:** After the wheel lands, the reward overlay (spin-reward-overlay, position:absolute inset:0 inside the modal) can only be dismissed by tapping the 'מעולה!' button (src/32-daily-spin.js:343). There is no ✕ and tapping the dark backdrop does nothing; because it's an absolute child of the modal (not a top-level *modal-overlay*), the global ESC handler won't catch it either. A player who wants to dismiss it by tapping outside the card is stuck reaching for the one button.
  - 📍 **הוכחה:** `src/32-daily-spin.js:295-303 builds the overlay with only a .spin-reward-ok button; line 343 wires that button as the sole close path; no backdrop listener and the overlay class isn't in the ESC allowlist.`
  - 🔧 **לעשות:** In src/32-daily-spin.js revealReward, add a backdrop click handler (ov.addEventListener('click', e => { if (e.target === ov) okBtn.click(); })) so tapping outside the card runs the same re-render-claimed-state path. Optionally route a tap-anywhere after a 1s lockout. Result: the highest-dopamine moment of the cluster has a forgiving, obvious dismiss.
- [ ] **⚪ נמוך · מאמץ `M` · ★★ · יופי** — **Hardcoded colors, z-indexes and radii everywhere — no design-token usage, inconsistent stacking**
  - **הבעיה:** None of the four surfaces use the base.css design tokens (--color-accent, --radius-md, --shadow-glow, --z-modal). Every color is a raw hex, every radius is a literal 22px, and z-indexes are hand-picked and inconsistent: spin overlay z=10000 (home-v2.css:988) vs daily-deal overlay z=100000 (boards.css:4587). If two of these can ever co-exist (e.g. checklist deep-links to the deal modal while a spin reward overlay is mid-animation) the stacking is arbitrary. This is maintenance debt and a beauty/consistency miss against the 'יפה' directive.
  - 📍 **הוכחה:** `home-v2.css:988 z-index:10000; boards.css:4587 z-index:100000; literal hex throughout (e.g. home-v2.css:998 #2E1645, boards.css:4593 #B83A3A); literal border-radius:22px in all four cards.`
  - 🔧 **לעשות:** Migrate the four overlay z-indexes to the --z-modal token (base.css :root) so they share one layer, and adopt --radius-lg for cards / --shadow-glow for the accent glow where the design already wants a glow (spin jackpot, login-cal day-7). Leave the intentional brand gradients but route the shared structural values (radius, shadow, modal z) through tokens. Result: consistent stacking + one place to retune the cluster's geometry.

---

## 20. Community cluster (Guilds + Guild Wars + Rivals + Ghost + Inbox) — ציון `72/100` (B)

> **הקשר:** 👥 קהילה tab — social-pressure retention surfaces
>
> **מימדים:** התמכרות **8** · יופי **7** · נוחות **6** · בהירות **7** · פוקוס **7** · סגירוּת **6** · רלוונטיות **8**

**פסק דין:** hooks חברתיים חזקים עם empty-states טובים לסולו — אבל ה-inbox (הכי-נלחץ) לא נסגר ב-ESC, כל ✕ מתחת ל-40px, ופעימות אינסופיות מתעלמות מ-reduced-motion. **כדי להעלות:** חבר את ה-inbox ל-ESC/back, הגדל סגירות, הוסף reduced-motion, ותקן את ה-deep-link של goal-complete.

**✅ מה כבר טוב (אל תיגע):**
- Strong loss-aversion legibility on tiles: rival shows live ahead/behind delta + 24h countdown (29-rivals.js:106-115), guild war shows 'leading by N' vs 'behind by N — go!' (33-guild-wars.js:90-99), guild shows live daily-goal progress bar (28-guilds.js:112-122). These are exactly the FOMO/social-pressure hooks that drive return-tomorrow.
- Empty-state for solo players is handled well: the קהילה tab placeholder gives a friendly title + an actionable suggestion ('invite a friend, join a clan, or play a duel') instead of a blank wall (46-bottom-nav.js:193,202).
- Rival modal has excellent dead-end avoidance: when no active rival, instead of an empty screen it offers an on-demand '🔍 find a new rival now' button so the personal-competition loop never stalls waiting for the 4h matchmaker (29-rivals.js:194-225).
- Inbox is properly the most-tapped hub: red unread badge, deep-link actions per item kind (open duels/guild/challenges/tournament/daily ghost race), and a ?action=inbox push deep-link with one-shot sessionStorage dedup (36-inbox.js:171-196, 244-262).
- Win celebrations are escalated and consistent: guild-war winner fires soundMilestone(7) + 7-pulse buzz vs consolation soundMilestone(4) (33-guild-wars.js:269-277); rival win fires confetti-grade celebration (29-rivals.js:290-307). Dopamine peaks are well-placed.
- Backdrop-tap-to-close is wired on every modal (e.target === overlay) and ESC/back-gesture covers guild/war/rival via the global -modal-overlay matcher (04-ui-utils.js:919).

**🛠️ משימות לביצוע (7):**

- [x] ✅ **בוצע (סבב 1)** · **🟠 גבוה · מאמץ `S` · ★★★★ · סגירוּת** — **Inbox panel cannot be closed with ESC or browser-back**
  - **הבעיה:** The inbox is the single most-tapped surface in the cluster, but its overlay uses class .inbox-overlay — which does NOT end in '-modal-overlay' and is NOT in the curated extras allowlist of the global ESC/back-gesture handler. Verified: grep for 'inbox-overlay' in src/04-ui-utils.js returns 0 matches. So the only ways to close it are the tiny × button or a backdrop tap. The mobile back-swipe — the most natural close gesture — silently does nothing, and on a phone the right-anchored slide-in panel leaves almost no backdrop to tap.
  - 📍 **הוכחה:** `src/36-inbox.js:111 (overlay.className='inbox-overlay'); src/04-ui-utils.js:922-936 (extras list — inbox-overlay absent); panel is justify-content:flex-end width:min(380px,92vw) in public/css/home-v2.css:2766-2771 (minimal backdrop on phone).`
  - 🔧 **לעשות:** In src/04-ui-utils.js __bloomGetCloseableModals(), add '#inbox-panel, .inbox-overlay' to the `extras` querySelectorAll string (line ~922-936). Confirm __bloomDismissTopmostModal's close-button selector finds '.inbox-close' (it matches via [aria-label="סגור"]). Result: ESC + Android/iOS back-gesture close the inbox like every other modal.
- [ ] **🟡 בינוני · מאמץ `S` · ★★★ · נוחות** — **Every community modal close button is under the 40px tap-target minimum**
  - **הבעיה:** The project's own UX gate Q4 requires a visible ≥40px close control, but all five community close buttons are smaller: inbox 30×30px, guild 32×32px, rival 32×32px, guild-war 36×36px. On a phone these are fiddly thumb targets, and they sit top-LEFT in an RTL layout (the far corner from a right-handed thumb). This is a consistent comfort (נוח) regression across the whole cluster — the most-tapped tab has the least-tappable controls.
  - 📍 **הוכחה:** `public/css/home-v2.css:2796-2797 (.inbox-close 30/30); public/css/boards.css:7541-7544 (.guild-modal-close 32/32 top:10 left:12); public/css/boards.css:7930-7933 (.rival-modal-close 32/32); public/css/home-v2.css:1354-1360 (.gw-modal-close 36/36).`
  - 🔧 **לעשות:** Bump .inbox-close / .guild-modal-close / .rival-modal-close / .gw-modal-close to width:40px;height:40px (keep border-radius:50%, scale font-size to ~22px). Cheaper alternative if visual size must stay small: keep the visual circle but add `padding` so the hit-area reaches 44px, or wrap with a 44px transparent ::before. Reference --radius-md / token spacing in base.css for consistency.
- [ ] **🟡 בינוני · מאמץ `S` · ★★★ · יופי** — **Infinite pulse animations ignore prefers-reduced-motion**
  - **הבעיה:** Several community surfaces run perpetual attention-pulses — rival 'behind' tile (rivalBehindPulse), rival icon bounce, guild 'can-claim' glow, and the inbox friend_beat icon (inboxRivalPulse) — but the codebase's reduced-motion guards only cover friend-notify-banner and gacha cards. CLAUDE.md repeatedly states other stages 'respect prefers-reduced-motion'; these do not. For motion-sensitive users the קהילה tab is a field of throbbing tiles, and it also undercuts the Priority-Calmer focus strategy by letting several things pulse at once.
  - 📍 **הוכחה:** `public/css/home-v2.css:2872-2876 (inboxRivalPulse, no guard); public/css/boards.css:7853-7868 (rivalBehindPulse + rivalIconBounce, no guard); public/css/boards.css:7466 (.guild-home-tile.can-claim pulse); the only reduced-motion blocks are public/css/home-v2.css:5792 (friend-notify only) and public/css/boards.css:5215 (gacha only).`
  - 🔧 **לעשות:** Add to the existing @media (prefers-reduced-motion: reduce) block (or a new one) in boards.css/home-v2.css: `.rival-home-tile.rival-tile-behind, .rival-tile-icon, .guild-home-tile.can-claim, .inbox-icon-rival, .gw-tile-badge-countdown { animation: none !important; }`. Keep the static color/border so loss-aversion still reads without motion.
- [ ] **🟡 בינוני · מאמץ `S` · ★★★ · התמכרות** — **Guild goal-complete toast is a dead-end — no deep-link to collect**
  - **הבעיה:** When the clan hits its daily goal mid-game, the toast says 'the clan completed the goal! open the app to collect your prize' — but it is a passive auto-dismissing toast (6s) with no tap action. The single highest-emotion collective moment (peer-pressure payoff) doesn't route the player to the claim button; they must remember, find the קהילה tab, open the guild modal, and hit claim. Every extra tap between dopamine and reward leaks the return-to-claim loop.
  - 📍 **הוכחה:** `src/28-guilds.js:64-74 (showGuildGoalCompleteToast — innerHTML text only, no onclick, setTimeout remove at 6000ms); contrast with inbox items which DO carry data-action deep-links (src/36-inbox.js:160).`
  - 🔧 **לעשות:** In src/28-guilds.js showGuildGoalCompleteToast(), make the toast tappable: add `t.style.cursor='pointer'; t.onclick=function(){ if(window.showGuildModal) window.showGuildModal(); t.remove(); };` and append a small '🎁 קבל עכשיו ›' affordance. Result: one tap from goal-complete to the claim button.
- [ ] **🟡 בינוני · מאמץ `S` · ★★ · בהירות** — **Ghost home-tile over-promises 'a friend's ghost' but usually delivers a stranger**
  - **הבעיה:** The ghost tile sub-copy hard-codes 'race a FRIEND who already played today's daily' (תתחרה ברוח של חבר), but fetchGhost falls back to a random global player when no friend ghost exists — and the confirm modal then correctly shows '🌍 random player'. The player taps expecting a friend, then sees a stranger: a small trust/clarity mismatch that weakens the social-pressure promise (racing a known friend is far more compelling than a random name).
  - 📍 **הוכחה:** `src/44-ghost-mode.js:190 (tile sub: 'תתחרה ברוח של חבר שכבר שיחק את ה-daily היום'); src/44-ghost-mode.js:226 (confirm modal branches data.isFriend ? '🤝 חבר שלך' : '🌍 שחקן אקראי') — proving the random path is the common case.`
  - 🔧 **לעשות:** In src/44-ghost-mode.js maybeShowHomeTile(), soften the tile sub to '👻 רוץ נגד הרוח של שחקן שכבר שיחק היום' (drop the 'friend' claim), OR fetch isFriend at tile-mount and only say 'חבר' when a friend ghost is actually available. Optionally show a 🤝/🌍 badge on the tile itself so the promise matches the modal.
- [ ] **⚪ נמוך · מאמץ `S` · ★★ · יופי** — **friend_beat inbox row uses physical border-left in an RTL panel (wrong side)**
  - **הבעיה:** The inbox panel is direction:rtl, but the highest-emotion 'X passed you' row is highlighted with `border-left: 3px solid` plus a left-to-right gradient. In RTL the physical left edge is the visual END (trailing) edge, so the status accent lands opposite to where the eye starts reading. The intent ('eye lands on it first') is undermined by a left/right physical property in an RTL context.
  - 📍 **הוכחה:** `public/css/home-v2.css (inbox-kind-friend_beat) border-left:3px solid #FF8E3C + linear-gradient(90deg,...) inside .inbox-panel { direction:rtl } at public/css/home-v2.css:2772.`
  - 🔧 **לעשות:** Change .inbox-item.inbox-kind-friend_beat (and its dark variant) from `border-left` to `border-inline-start`, and the gradient from `90deg` to `270deg` (or `to left`) so the accent + fade originate from the RTL reading-start (right) edge. Same swap for any other physical-direction borders in this panel.
- [ ] **⚪ נמוך · מאמץ `M` · ★★ · נוחות** — **Guild-war / rival modal loading state is a bare grey '⏳ טוען' with no skeleton**
  - **הבעיה:** Both the guild-war modal and rival modal mount with a centered grey '⏳ טוען...' placeholder then swap to content after the fetch resolves. There's no skeleton shimmer (which the home itself uses per CLAUDE.md T6.4), so on a slow connection the player sees a jarring empty grey box then a full-content pop. Perceived speed and polish suffer on the exact surfaces meant to feel like a live competition.
  - 📍 **הוכחה:** `src/33-guild-wars.js:121 ('<div style="padding:30px;...color:#999">⏳ טוען...'); src/29-rivals.js:177 (same pattern); inline color:#999 also bypasses design tokens.`
  - 🔧 **לעשות:** Replace the inline '⏳ טוען' divs in showWarModal (33-guild-wars.js:121) and showRivalModal (29-rivals.js:177) with a lightweight skeleton (2-3 shimmer bars reusing the home-skeleton shimmer keyframe). Also swap inline color:#999 for var(--color-text-muted) so dark-mode reads correctly.

---

## 21. Shop cluster (Gem Bank + Starter Pack + Gacha + Bundles + Boosters) — ציון `74/100` (B)

> **הקשר:** 🛍 חנות tab — monetization surfaces
>
> **מימדים:** התמכרות **8** · יופי **7** · נוחות **6** · בהירות **8** · פוקוס **7** · סגירוּת **7** · רלוונטיות **8**

**פסק דין:** אשכול-מונטיזציה עם מכניקות ממכרות חזקות (gacha-reveal מצוין, עיגון-ערך, בנק התנהגותי) — נבלם מחוסר קונפטי-שיא ברגעים הכי יקרים, כפתורי-סגירה <40px, confirm() נייטיב, ופערי dark-mode. **כדי להעלות:** הוסף קונפטי לשיאים (mythic/legendary/starter), הגדל סגירה ל-40px, החלף confirm() במודאל מותג, והוסף dark-mode לבנק+חבילות.

**✅ מה כבר טוב (אל תיגע):**
- Gacha reveal is genuinely best-in-class: overshoot cubic-bezier card pop (boards.css:5145-5152), per-rarity glow/border/sound/buzz scaling (19-skin-gacha.js:328-350), pity progress bar + 'עוד N לאגדי מובטח' hint (19-skin-gacha.js:120-130), and a collection-completion card '12/17' completionist drive (19-skin-gacha.js:135-147). Strong Skinner-box loop.
- Gem bank nails behavioral-economics: loss-aversion withdrawal fee, a live 'מחר תהיה לך X💎' next-day projection (42-gem-bank.js:147-151), and a concrete 30-day compound tip (42-gem-bank.js:179) — turns abstract interest into a visible dopamine number.
- Value-anchoring is present on every paid surface: starter pack shows '~2400💎 ערך · חסכת 79%' (17-starter-pack.js:168-172), bundles show struck-through originalValue + discount ribbon (23-bundles.js:157-160), gacha 10-pull shows 'חסכת N%' (19-skin-gacha.js:163-167).
- Scarcity/FOMO framing is consistent: 'חד-פעמית · לעולם לא תוצע שוב' (starter + bundles), live countdown tickers on starter/bundle banners + modals, and pity-guarantee on gacha. Banners correctly have NO deletable ✕ (IS.4 fix held — 17-starter-pack.js:94, 19-skin-gacha.js:83, 23-bundles.js:84).
- All 5 modals are reachable by the global ESC/back-gesture handler (04-ui-utils.js:919-936 — generic -modal-overlay match + .gem-bank-overlay in extras), and gacha-reveal/rolling are correctly excluded so ESC can't yank a mid-reveal animation.
- Booster strip is a well-engineered floating bottom tray that reclaimed ~73px of grid height (home-v2.css:2541-2562), uses the --z-floating token, has a slide-up entrance, and is correctly torn down on game-over (35-boosters.js:55-56).

**🛠️ משימות לביצוע (8):**

- [ ] **🟠 גבוה · מאמץ `S` · ★★★★ · התמכרות** — **No confetti / climax FX on the highest-value monetization moments**
  - **הבעיה:** The biggest dopamine peaks in the cluster — a mythic gacha pull (0.5% odds), a legendary, the starter-pack purchase, and a bundle purchase — fire only soundMilestone + buzz. There is zero confetti or screen-wide celebration. showConfetti is exposed as window.__bloomConfetti (used elsewhere on home win-return) but is never called here. A 0.5% mythic landing with the same visual weight as an uncommon leaves the single rarest event in the shop under-celebrated, and the purchase-welcome overlays are static cards.
  - 📍 **הוכחה:** `src/19-skin-gacha.js:341-350 (renderCard fires only soundMilestone+buzz, no confetti even for res.rarity==='mythic'); src/17-starter-pack.js:222-223 + showStarterPackWelcomeOverlay (261-281) no confetti; src/23-bundles.js:212-213 + showBundleWelcome (240-264) no confetti. Grep for 'confetti' across all 5 files returned zero matches.`
  - 🔧 **לעשות:** In src/19-skin-gacha.js renderCard(), after the sound/buzz block, call window.__bloomConfetti when present: mythic → ~60 particles, legendary → ~40, rare → ~18, scaled like the LF.1/AD.5 pattern. In src/17-starter-pack.js buyStarterPack success branch and src/23-bundles.js buyBundle success branch, fire window.__bloomConfetti(48) alongside the existing soundMilestone(6). Guard each call in try/catch and skip when prefers-reduced-motion. Result: the rarest/most-valuable purchases get a screen-wide gold-pink burst, matching the celebration weight already used for daily-login jackpot and personal-best.
- [ ] **🟡 בינוני · מאמץ `S` · ★★★ · נוחות** — **Modal close buttons are 32×32px — below the 40px tap-target minimum**
  - **הבעיה:** Every shop modal's ✕ is 32×32px, under the project's own ≥40px tap-target rule (UX gate Q4 / comfort). On a phone in RTL the ✕ sits top-left (the harder thumb corner), making it a small, awkwardly-placed target. IS.5 already fixed an overlap bug here but the size was never bumped.
  - 📍 **הוכחה:** `src/boards.css:4237-4243 (.sp-modal-close width/height 32px), boards.css:4932-4937 (.gacha-modal-close 32px), boards.css:6314 (.bundle-modal-close), home-v2.css:4244-4255 (.gem-bank-close 32px), home-v2.css:2617 (.booster-modal-close).`
  - 🔧 **לעשות:** Bump .sp-modal-close, .gacha-modal-close, .bundle-modal-close, .gem-bank-close, .booster-modal-close to width/height:40px (keep border-radius:50%), and increase the tap area without growing the visible glyph via a transparent hit-padding or min 44px hit box. Verify the badge/ribbon (IS.5) still clears the larger circle. Result: all close buttons meet the 40px+ standard, easier one-thumb dismissal.
- [ ] **🟡 בינוני · מאמץ `M` · ★★★ · נוחות** — **Gem-bank withdraw uses a blocking native confirm() — off-brand and jarring**
  - **הבעיה:** Withdrawing gems pops a native browser confirm() dialog. This breaks the polished branded look, is non-RTL-styled, can be themed inconsistently by the OS, and on iOS PWA looks like a system error. The rest of the app moved off alert()/native dialogs (T0.4), but this confirm survived.
  - 📍 **הוכחה:** `src/42-gem-bank.js:223 — if (!confirm('למשוך ' + amt + '💎? עמלה: ...')) return;`
  - 🔧 **לעשות:** Replace the native confirm() in wireActionButtons (withBtn handler) with an in-modal inline confirmation step: on first tap, swap the withdraw button label to 'אשר משיכה · תקבל N💎 (עמלה M💎)' for ~4s; a second tap within that window calls doWithdraw. Or render a small branded confirm row inside .gem-bank-action-withdraw. Result: no native dialog, consistent RTL branded UX, clear fee disclosure preserved.
- [ ] **🟡 בינוני · מאמץ `M` · ★★★ · יופי** — **Gem bank and bundle modals have zero dark-mode overrides**
  - **הבעיה:** Grep shows 0 dark overrides for .gem-bank-* and .bundle-* (vs full coverage for starter pack). The gem-bank modal card relies on an intrinsic gradient, but its balance rows, rate card, hint text, and the input fields can render with light defaults that read poorly on a dark surface. Bundles render arbitrary admin-set --theme-color backgrounds with fixed dark text, which can fail contrast for light theme colors. The balance bar dark-text-on-tint bug was already a documented hazard (TC.6).
  - 📍 **הוכחה:** `Grep: 'html[data-theme="dark"] .gem-bank' → 0 matches in home-v2.css; 'html[data-theme="dark"] .bundle' → 0 matches in boards.css/home-v2.css. Contrast: src/23-bundles.js:65,145 set --theme-color from bundle.themeColor with no luminance check; .bundle-modal text colors are static.`
  - 🔧 **לעשות:** Add html[data-theme="dark"] overrides for .gem-bank-balances rows, .gem-bank-rate-card, .gem-bank-hint, and the two number inputs (bright text + darker tinted backgrounds) in home-v2.css. For bundles, either add dark overrides for .bundle-modal-* text or compute a contrast-safe text color from --theme-color luminance in src/23-bundles.js (set a --on-theme var). Result: both surfaces are legible in dark mode and across arbitrary holiday theme colors.
- [ ] **⚪ נמוך · מאמץ `M` · ★★★ · התמכרות** — **Gacha 'rolling' overlay is a static 'פותח...' with no anticipation build or skip**
  - **הבעיה:** Between the pull request and the reveal, a fixed 1400ms overlay shows a single orb + the static text 'פותח...'. There's no rarity-teasing build-up (e.g. orb color shifting toward the highest rarity in the batch), no rolling rarity ticker, and no way to skip for a player chaining 10-pulls. The reveal itself is excellent, but the suspense beat — the highest-tension moment in any gacha — is flat.
  - 📍 **הוכחה:** `src/19-skin-gacha.js:228-234 (rolling overlay innerHTML is fixed orb + 'פותח...'); the reveal is gated behind a hardcoded setTimeout(...,1400) at line 267-270.`
  - 🔧 **לעשות:** In doPull, drive the rolling orb's glow color toward the best rarity present in d.results during the 1400ms (e.g. switch a CSS class on .gacha-rolling-orb to the top rarity ~600ms in) so a gold-ish orb pre-signals a legendary/mythic — the classic Genshin tension tell. Optionally add a 'דלג' tap on the rolling overlay that jumps straight to revealSequence. Result: the build-up creates 'is it gold?!' anticipation, sharpening the dopamine peak.
- [ ] **⚪ נמוך · מאמץ `S` · ★★ · יופי** — **Bundle decoration emojis are randomly positioned and can overlap content/each other**
  - **הבעיה:** 8 decoration emojis are scattered with Math.random() top/left 0-88% with no collision avoidance and no z-index management relative to text. They can land directly over the title, price, or buy button, and cluster in one corner — looks unpolished rather than festive, and could (rarely) reduce tap clarity on the CTA.
  - 📍 **הוכחה:** `src/23-bundles.js:133-141 — top/left = Math.floor(Math.random()*88) per emoji, appended into the card with the CTA and text.`
  - 🔧 **לעשות:** Constrain deco placement to the card margins (e.g. top 4-18% or 82-94%, left 4-94%) so emojis ring the edges instead of the center, ensure pointer-events:none and a z-index below the buy button/title, and seed positions deterministically per bundle.id so they don't reshuffle on every open. Result: festive border decoration that never covers the CTA or price.
- [ ] **⚪ נמוך · מאמץ `S` · ★★ · בהירות** — **Booster tray gives first-time players no hint of what 🎯/💥 do**
  - **הבעיה:** The bottom booster tray shows only emoji + 1-word label (בחר / הסר) + price. A first-time player in practice/dynamic mode can't tell what 'בחר' (pick the next piece) vs 'הסר' (pop a cell) actually does in <3s, and there's no micro-tooltip (the home Priority-Calmer micro-tutorial system from stage 39 doesn't reach this in-game tray).
  - 📍 **הוכחה:** `src/35-boosters.js:83-96 (boosterBtnHtml renders emoji+label+price only, no title/aria-description); no first-visit tooltip wiring in maybeMountBoosterStrip (48-70).`
  - 🔧 **לעשות:** Add a one-time gold micro-tooltip next to the booster tray on first mount (dedup via localStorage bloom_booster_tip_seen), e.g. '🎯 בחר את האריח הבא · 💥 הסר אריח תקוע', auto-dismiss 6s, mirroring the stage-39 showMicroTooltip pattern. Also add title/aria-label to each .booster-btn. Result: new players understand the tray instantly, increasing booster try-rate (monetization + utility).
- [ ] **⚪ נמוך · מאמץ `M` · ★★ · בהירות** — **Starter-pack 'value' is computed with a hardcoded fudge that can mislead**
  - **הבעיה:** The 'ערך החבילה' and 'חסכת N%' are derived client-side from (rewardGems + rewardBpTiers*200 + 300) — the +300 (skin) and ×200 (per BP tier) are magic numbers unrelated to actual server values. If admin tunes the pack, the displayed 'value' and savings % drift from reality, and a savvy player could notice the anchor is invented. The same ×200 magic appears for bundle BP framing.
  - 📍 **הוכחה:** `src/17-starter-pack.js:168-172 — ~((rewardGems + rewardBpTiers*200) + 300) and the savings % use the identical expression as denominator.`
  - 🔧 **לעשות:** Have the server return an authoritative anchorValue (sum of real component prices) on the starter-pack status payload and render data.anchorValue + a server-computed savedPct instead of the client fudge. Until the server field exists, at minimum extract the 200/300 constants to named values and document them. Result: the value anchor is truthful and survives admin retuning.

---

## הערות סגירה

- **אפס באגים קריטיים** — אף דף לא שבור. הציון הבינוני (67) הוא בגלל הצטברות של עשרות החמצות בינוניות, לא כשל בודד.
- **ה-ROI הגדול ביותר הוא רוחבי:** תמות 1-4 (tokens, reduced-motion, closability, tap-targets) הן ~4 תיקונים מערכתיים שמרימים את **כל** 21 הדפים בו-זמנית. כדאי לעשות אותם לפני התיקונים הנקודתיים.
- כל משימה כאן מנוסחת כך שאפשר להעביר לי אותה והיא מוכנה לתכנות מיידי (file/function/שינוי מפורט). אמרי לי "תתחיל מ-#N" או "תעשי את כל ה-quick-wins" ואני יוצא לדרך לפי הכללים הרגילים (build + engine self-test + deploy + health-check).
